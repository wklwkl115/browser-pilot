# Changelog

- 修复 **C3 — `browser_artifact` 硬拒通用参数 `detailLevel`（2026-06-07，从真实 CTF web 安全会话日志 `sessions/2026-06-07T04-32-05…jsonl` 挖出,非盲测；MEDIUM,同 C2 类）**。该会话(干净的 bilibili 充电视频逆向实战:execute×19/artifact×12/network×11/http_replay×7/hook×4,跑通 WBI 签名 + 会话绑定 playurl 重放)在 `browser_artifact {path, mode:json, jsonPath:"data.data", detailLevel:"summary", maxChars, redact}` 上硬拒 `Validation failed: root must not have additional properties`。根因(代码确认):`registerArtifactTool` 手写 `strictToolParameters({...})`,声明了 `maxChars`/`redact` 却漏了 `detailLevel`——agent 在所有其它 browser_* 工具都传的通用输出参数在此硬拒。即 C2 缺陷换个工具。修:`browser_artifact` 现接受 `detailLevel` 作为 no-op(其输出形状由 `mode` 决定、非 detail-leveled,同 C2 对 browser_tabs 的处理),忽略而非报错。回归:`check-tools-contract`(browser_artifact schema 必须含 `detailLevel`)。全量 check 绿,tool-contract doc 重生成。会话另两处摩擦经代码核实:`browser_wait {action:navigate, url}` 顶层 url 被 root additionalProperties 拒(需嵌 `params:{url}`,agent 一步自恢复)——同"接受自然输入"类但属更大改动(wait/network/hook/frame 四个 action 工具),记为候选未在本次改;`wait.loadState state:"networkidle"` unsupported——**当前代码已修**(wait.ts `dispatchPiBrowserWait` 已把 loadState 的 networkidle reroute 到 waitForNetworkIdle,2026-06-05 加),会话命中是操作员 daemon 旧 build 未重启所致,重启即解,无需改码(同早先 D1)。

- 修复 **G10 — causal `nextActions` hint 用截断后的 `requests.length` 报"N 个请求 fired since baseline",严重少报真实 delta（2026-06-07，R-G5 真站盲 eval on linux.do，sonnet，新维度 = network/causal 平面,此前 eval 零覆盖；MEDIUM）**。整个 causal 流程端到端跑通：agent `network start` → `observe scan`（基线）→ 只读导航 `/latest` → `observe scan --baseline-snapshot-id <id>` → 读 `causal.requests`（12 条归因）+ 录制汇总;CLI baseline-by-snapshotId 正常;`nextActions` 正确指向 `envelope.causal.requests`。真缺陷：内联 `causal.requests` 预览上限 `MAX_CAUSAL_REQUESTS`(12),而 `causal.requestCount` 才是真实 delta 总数(本例 375);scan hint 写 `${causal.requests.length} request(s) fired` → 报"12 个 fired",实际 375——任何 baseline 后触发 >12 请求的真实 SPA 导航都被少报 30×,也是 agent 最大困惑("看不见的请求能不能翻页")的根源。修:abml-core/causal.ts 新增纯函数 `causalRequestsFiredCount`(`requestCount ?? requests.length`)+ `causalFiredHint`;observeRunners 改用之,报真实计数,且当内联预览被截断时指明完整集在 `browser_network list`(causal 块是有界预览、不可翻页)。通用(任何 >12 since-baseline 请求的 baseline 观察),代码确认。回归:`abml/causal.test`(真实总数、截断才加后缀、unavailable 无 hint)。全量 check 绿。R-G5 其余报告经代码核实非缺陷:`requestCount=375` vs `requests[12]` 是标准"真实总数 vs 显示子集"披露(MAX=12,非 40,非 bug);`data.causal` notFound 是 agent 找错路径(causal 在顶层 + `envelope.causal`,hint 已指 envelope.causal);causal 含 baseline→action 间的 message-bus 背景 POST = WAI(被动 since-baseline delta,非精确事件因果图);`wait --action load` 错误未列合法值 = LOW backlog。全程零超时。

- 文档 **G9 — skill iframe 路由欠说明：`frame evaluate` 的 params + `frameId` 取自哪个字段（2026-06-07，R-G4 真站盲 eval on 菜鸟教程 try-it，sonnet，新维度 = iframe/`browser_frame`，此前 eval 零覆盖；LOW）**。frame 工具路径本身首次即成功（`frame list` → `frame evaluate`，7 次调用全 <1s，零可靠性问题）——agent 读到了顶层 scan 看不见的子 `iframeResult` 帧内渲染的 `<h1>`/`<p>`。唯一摩擦：skill Routes 行只写 `browser_frame list → browser_frame evaluate`,没说 evaluate 需要 `frameId`+`expression`、也没说从 `frame list` 哪个字段取 id,害 agent 绕去查 `--help`。修（纯文档,同 G4）：Routes 行现写明 `frame list` → 读 `frames[].frameId` → `frame evaluate --params '{"frameId","expression"}'`,并注明顶层 scan 不结构化覆盖子帧。对照 `frame.evaluate` schema 核实（需 frameId+expression）。全量 check 绿,skill 重验证通过。**frame/iframe 工具路径结论：稳固,无能力缺口、无可靠性问题。** R-G4 另一报告经代码核实属 WAI：agent 担心顶层 `observe mode=text` 把 iframe 文本以 `--- iframe ---` 标记附加"可能误导"——核实该标记仅 `mode=text` 输出（`buildScanScript` `collectText` 聚合同源 iframe 文本并加带标签分隔符），是刻意且明确标注的便利（一次拿到 iframe 可见文本）,非结构化覆盖,诚实且有用,不改（加 hint 是项目防范的臆测性 token 成本）。

- 修复 **G8 — `wait --action selector` 拒绝自然的 `--selector` flag 且不指向 `--params`（2026-06-07，R-G3 真站盲 eval on bilibili.com，sonnet；LOW）**。agent 试 `wait --action selector --selector ".feed-card"` → `unknown flag "--selector"`,正确是 `--action selector --params '{"selector":"…"}'`。action 工具（wait/network/hook/frame）的 per-action key 走 opaque `--params` JSON（F4 的 `--help` per-action keys 已说明,但 agent 先试 flag）。同"接受自然别名、不硬拒"类（`tool-ergonomics-accept-natural-aliases`）。修（最小,复用 B10 `ABSENT_FLAG_HINTS` 机制）：未知 `--selector` 现给定向提示,点名 action 工具 + `--params` 形状,把死胡同转成一步指引。仅限唯一确认的 key（`selector`）;`--url`/`--state` 等 runs=0 不预加（无臆测改动,守 `eval-fixes-true-defect-no-overfit`）。回归：`flags-render.test` B10 段（`--selector` 错误含 `--params`）。全量 check 绿。任务本身全成（5 卡片+UP+播放量+时长+导航）。**bilibili/MDN 两轮还 live 复验 G2/G7 稳固**：execute 全部 200–400ms 零挂起;artifact `mode=search --query` 干净可用、技能文档清晰。R-G2/G3 其余报告经代码核实**非缺陷**：MDN 的 json-path notFound 是 agent 找错路径（绝对路径允许读、data.headings 本就不在那）;大页面 scan summary 截断/低 node_count = `fitSummaryBudget` 按预算压缩并指向 artifact（WAI,agent 的 node_count:12 与 1.87MB artifact 自相矛盾,非可靠信号）;重 SPA 卡片不在首次 scan AX 树 → wait selector+JS 提取 = 既定 scan-结构/JS-取值分工（WAI）。

- 修复 **G7 — `browser_artifact` 在非 `mode=search` 时静默忽略 `query`（2026-06-07，R-G2 真站盲 eval on github.com/torvalds/linux，sonnet 子代理，加长多步任务；MEDIUM）**。agent（6 项任务全部完成）遇两处 artifact 搜索摩擦：Friction 4 用 `--search`（不存在的 flag，应为 `--mode search --query`）；Friction 6 用 `mode=json --json-path data.markdown --query "…"` 期望在 markdown 值内定位关键词，但 `query` 仅 search 模式生效 → **被静默忽略**、返回文件头，被迫手动 offset 二分。与上轮 G5（regex 在单行 600KB 被跳过——但纯 query 本就能窗口化长行）同属 artifact-搜索可发现性类。根因（代码确认）：`readBrowserArtifact` 已有 `jsonPath`→`mode=json` 自动升级（H2 先例），却无对称的 `query` 处理。修（镜像 H2，两半）：(1) 裸 `query`（无 mode、无 jsonPath）自动升级到 `mode=search`，不再返回 offset 0 文本；(2) `query` 与 json/jsonPath 读组合时抛 `ARTIFACT_QUERY_REQUIRES_SEARCH_MODE` 并给可操作补救（用 `mode=search`——纯 query 经 `contextChars` 对单行巨型值也能窗口化；或用 `jsonPath`+`columnOffset/columnLimit` 窗口化已知值），把静默 no-op 转成可操作指引。skill artifact 段重写：`mode=search`+`query` 是查找文本的方式（纯 query 窗口化长单行，`regex` 仅限短行，query 在他处被忽略）。通用（定位巨型单行值内文本的能力本就存在，这里修可发现性+静默忽略，非加功能），合 `tool-ergonomics-accept-natural-aliases`。回归：`artifactReader-advanced`（裸 query→search；query+jsonPath→报错；纯文本 search 窗口化 600KB 单行）。全量 check 绿，docs 重生成（新错误码）。**G2 修复在本轮真站 live 复验通过**：~12 次 execute 调用全部 200–400ms 返回，零挂起零超时。

- 修复 **G2 — `browser_execute` 在 `chrome.scripting.executeScript` 卡死时整段超时、无结果返回（2026-06-07，R-G 真站盲 eval on linux.do，skill-guided；HIGH）**。盲 agent 的"scan→JS 抽取"标准流程全断：每个 `browser_execute`（连 `document.querySelectorAll('*').length` 这种 trivial）都在整个 timeout 后返回 `BRIDGE_TIMEOUT "ACK received, script may still be running"`，而同一页面的 `browser_observe scan/text/html` 全部成功。根因（代码确认，真·通用缺陷，非 agent 猜的登录弹窗）：`browser_execute` 走 `handleWsExec`（`bridge_src/service_worker/exec.ts`），主路径 `chrome.scripting.executeScript`（MAIN world）+ 持久 CDP `Runtime.evaluate` 兜底——正是 `browser_observe` 可靠使用的同一机制。debugger 已附加时 MAIN-world 注入在某些页面不被 renderer 确认 → executeScript 卡死，兜底就是为此而生；但 `EXECUTE_SCRIPT_TIMEOUT_MS` 被设为**整个 caller `timeoutMs`**（`data.timeoutMs ?? 2500`），它又等于外层 client pending-request 超时——卡死的 executeScript 吃光全部预算，已证可用的兜底在 client 放弃前根本没机会跑。`?? 2500` 默认本是原始快速失败意图，复用传入的大 `timeoutMs` 做 race 静默破坏了它。修：快速路径 executeScript cap 到预算 ≤1/3 且 ≤2500ms（`Math.max(100, Math.min(2500, Math.floor(TOTAL_EXEC_TIMEOUT_MS/3)))`），剩余预算保留给 CDP 兜底（继承全额 `TOTAL_EXEC_TIMEOUT_MS`）。通用，非过拟合。回归：`check-pi-browser-bridge` `testExecHangingScriptFallsBackToCdpWithinBudget`（永不 resolve 的 executeScript 必须在 caller 预算内 fallback 到 CDP 结果，且兜底继承全额预算）。需 `npm run build:bridge`。全量 check 绿。

- 文档 **G4 — skill 补全 `browser_observe mode=html` 的 `htmlMode` 合法值（2026-06-07，R-G 盲 eval；LOW）**：agent 猜 `--html-mode simplified` 报 `must be one of: fragment, raw, text, inner, outer`。修：skill Routes 行补 `htmlMode ∈ fragment|raw|text|inner|outer`（CLI `--html-mode`），对照 `registerObserveTool` schema 核实。纯可发现性。G1/G3/G5/G6 见 `blind-findings.md`（G3=AX 模态遮挡 WAI；G5=artifact regex 对单行巨型 HTML 失效，留 backlog；G1/G6=CLI 别名小项待复现）。

- 完成 **B5 DOM-probe 收尾（2026-06-07，此前 deferred）**：`wait.diagnose` 从"只报全局 tab/bridge 状态"升级为 selector-miss 定向诊断。wait coordinator 给每次 `finishPiBrowserWait` 记录终态（`rememberTerminal`，剪枝至 200 条 / 5 分钟）；`diagnosePiBrowser` 按 `waitId`（active 或 terminal；或显式 `selector`）回查最近 selector wait，跑一次 bounded 主文档 `hook.evaluate` selector 探测（matchCount/visibleCount/firstMatch/sampleMatches/语法校验），返回 `selectorDiagnostics`{selector, expectedState, lastState（超时最后快照）, current（探测）, recoveryCommands} + `recentWaits`（最近 20 条终态）+ 既有 iframe 诊断。**不改公开工具面**（内部 bridge `wait.diagnose` 增强，`browser_wait action=diagnose` 不变）。诚实范围：给 match-count/iframe/last-state 证据，不做模糊"相似 selector"建议（可选、低价值）。回归：`check:pi-browser-bridge` 动态合同（注册 selector wait→finish TIMEOUT→按 waitId diagnose→断言 selectorDiagnostics + recentWaits），需 `npm run build:bridge`（合同读 dist）。全量 check 绿。

- 修复 **C2 — `browser_tabs` 硬拒通用输出参数 `maxChars`/`detailLevel`/`redact`（2026-06-06，从真实会话错误日志挖出）**。扫了全部 `sessions/*.jsonl` 的 `browser_*` isError（19 个），主导的真实跨 agent 类：agent 给 `browser_tabs` 传 `maxChars`/`detailLevel` → `must not have additional properties`，**≥7 次、≥5 个不同会话/agent（截至 2026-06-05）**。根因（代码确认）：其它 browser_* 工具都 spread `sharedTabScopedToolParams()`（含 browserSessionId/tabId/detailLevel/outputPath/timeoutMs/maxChars/redact），唯独 `browser_tabs` 手写 schema 漏了它们，`additionalProperties:false` 把 agent 普遍假设通用的参数拒掉。修：`registerTabsTool` 改 spread `sharedTabScopedToolParams({ includeOutputPath:false })`（tabs 不产 artifact），保留 tab 专有参；`maxChars` 在大输出（list/snapshot）上 honor，`detailLevel`/`redact` 接受但为 no-op（tab 元数据是小且非敏感的 JSON 列表，非蒸馏/脱敏摘要）。通用（去特例、与所有工具对齐，正中"接受自然别名、不硬拒"），非过拟合。回归：`check-tools-contract`（browser_tabs 必须 spread mixin）。全量 check 绿，token-economy 0.0607 未损，docs 重生成。（`redact` 在 command/execute 上的同类缺口在 06-01 后已随 mixin 补上；其余 19 错为故意负测/臆造工具/旧 profile gate/已是好错误，均非当前通用缺陷。）

- 新增 **C1 — `browser_http_replay` 双提交 CSRF 反射（2026-06-06，来自真实 CTF web 安全会话日志分析）**。真实会话证据：`bindBrowserSession:false` 的 http_replay 收 `403 missing csrf token` → agent 弃用工具、改在页内手写 `fetch` 抠 CSRF cookie 反射成头、重复约 10 次，最后绕回 http_replay 仍需手动粘 `x-csrf-token`。根因：`bindBrowserSession:true` 只绑 Cookie 头、不反射双提交 CSRF（极普遍的 XSRF-TOKEN cookie↔header 模式）。修：`sendReplayLikeRequest` 绑定 cookie 后，从已绑定的 Cookie 头派生 CSRF token 注入匹配请求头（`shared/http.ts` `deriveCsrfReflection` + `shared/replay.ts` `reflectCsrfIntoHeaders`）。边界（守 Brain-Hand / 不违反"执行归 agent"）：补全既有工具的会话绑定机械能力、非加 click/type；**默认开（仅 `bindBrowserSession:true` 下生效）+ 可见（summary `csrfReflected` 仅报 cookie/header 名）+ 可覆盖（`reflectCsrf:false` / `csrfCookie` / `csrfHeader`）**；不覆盖调用方已设的 csrf 头；token 值不泄漏（默认头名锁定在 `redactHeaders` 脱敏集内，summary 只出名）；仅作用 `sendReplayLikeRequest`（http_replay），recon/fuzz/crawl 各自注入不受影响；不改 native 协议。回归：`tests/unit/webSecurity/shared/http.test.ts`（`deriveCsrfReflection` 检测/解码/头映射/无泄漏）、`tests/unit/webSecurity/shared/replay-csrf.test.ts`（默认开/不绑不反射/可关/不覆盖/可指定）、`check-summaries`（`csrfReflected` 出现于 summary）。全量 check 绿，token-economy 0.0607 未损，docs 重生成。**B7（cookie 属性）回退 backlog**——同一会话显示 agent 拿 cookie 属性的自然路径是 `browser_command cookies getAll`（用得很顺），不是 `browser_cookie_analyze`，故往 cookie_analyze 加属性的边际价值低于 C1。

- 修复 **B6 + B8 + B9b + B10（2026-06-06，第二批盲 eval n=2 确认；全部 live/contract 验证）**，B7、B9a 经验证后 defer：
  - **B6**（链接 href/origin）：`buildScanScript` 给 `<a>`/`<area>` actionable 附 resolved `href`；scan 摘要 actionables 表加 `href` + 机械 `sameOrigin` 列（`scan.ts` `linkSameOrigin`）——仅身份感知、无排序/抽取（守 B1 边界）。live：article.html 链接带 resolved href + sameOrigin。
  - **B8**（perf/evidence 样本，收窄）：standalone `browser_hook getPerformanceEntries` 加 distiller（`summaries/hook.ts` `summarizeHookPerformance`：initiatorType 计数 + resource/ms 样本 + `data.entries` hint），经 `hookToolDistiller` 路由。多源 evidence **聚合按设计保持 counts-only**——往聚合加样本撑爆 token-economy 守卫(+106%，被 `check:token-economy` 拦下)，聚合用 `data.sources` hint 下钻；hook lifecycle 事件 `detail:null` 属 WAI。live(linux.do)：17 条 perf → 计数+样本，不再 `{type,keyCount}` 折叠。
  - **B9b**（download MIME 不符）：`browser_download` 加 `expectMime`（media 模式默认 `image`），完成后比对 Chrome `download.mime`，不符则给 `mimeMismatch` 诊断（如 image 拿到反爬 text/html）——诊断而非硬失败，文件仍保存。
  - **B10**（CLI did-you-mean）：`parseArgs` 对未知 flag 给最近匹配建议（驼峰↔kebab 归一化 + 编辑距离 ≤2），对"本命令没有的常见 flag"给定向提示（`--detail-level` → 用 `--limit/--offset/--max-chars`）。通用——惠及所有 flag 笔误。live：`--jsonPath`/`--max-char`/`--detail-level` 均给对提示。
  - **B7（cookie 属性）defer**：验证发现属性在上游被丢弃——browser-session cookieProvider 返回扁平 Cookie header（`register/shared.ts` `browserCookiesToHeader`），无 domain/path/secure 等；surface 需改 provider 契约传结构化 cookie（中等、动共享代码），非加列。**B9a（media 候选表）defer**（贴近 B1 语义抽取边界）。
  回归：`check-summaries`（B6/B8/B9b）、`flags-render.test`（B10）；全量 check 绿，unit 全过，token-economy 0.06 未损，docs/protocol 重生成。同 `blind-findings.md` Resolved 段。

- 修复 **B4 + B5 partial（2026-06-06，第二批 10 站点真实盲 eval）**，并关闭 **Rel1**：
  - **B4**：`browser_artifact mode=json --json-path data.content` 命中长标量字符串时被 `compactJsonValue` 固定截为 800 字符，`offset/limit` 只对数组生效。修：标量字符串命中 `jsonPath` 时走字符窗口，返回 `{type:"string", text, offset, limit, nextOffset, originalLength, truncated*}`；短字符串仍原样返回，嵌套对象/数组压缩和默认脱敏不变。
  - **B5 partial**：`wait.selector` 超时只暴露通用 lease/CDP 诊断。修：wait supervisor 的 `WAIT_TIMEOUT` payload 增加 `selectorTimeout`，显式给出 selector、state、已知主文档 0 命中，以及可复制的 `diagnose` / re-observe / html-or-frame 恢复命令。更重的超时 DOM 相似 selector/iframe 探测继续 deferred。
  - **Rel1**：确认与 M2 相同，属于 blind harness 脏状态产物，已并入 M2 关闭，不作为工具 bug 处理。
  回归：`npm run check:artifact`、`npm run check:fake-ws`。同步 `blind-findings.md`。

- 修复 **B2 + B3（2026-06-06，10 站点真实盲 eval）**，并将 **B1** 降级为 RFC/观察项：
  - **B2**：`browser_execute` 长脚本经 CLI 内联易受 shell/CSS/JS 引号影响。修：`pi-browser execute --script-file <path>` 作为 CLI-only flag，读取 cwd-relative/absolute 文件内容写入既有 `script` 参数，禁止与 `--script` 同用，进入 schema 校验前移除 synthetic param；**不改 Pi 工具 API/schema**。
  - **B3**：artifact 路径发现反复误猜 `data.data.*` / `--pick` JSON 数组。修：`browser_artifact` 参数描述明确常见 artifact 根为 `data` / `data.<key>`，CLI `--pick` 需重复传；大/非内联 `browser_execute` 返回值新增 `artifact_hints.preferredReads` 指向 `data` 与最多 3 个顶层 `data.<key>`。
  - **B1 不修语义 extractor**：不实现 headline/card/news/source/time/uploader 推断；如未来再做，仅限通用 viewport-visible text/link row projection。
  回归：`tests/unit/cli/flags-render.test.ts`、`tests/unit/tools/execute-artifact-hints.test.ts`；`npm run check` 全绿。同 `blind-findings.md`。

- 修复 **E1 + H3 + 两个 minor（2026-06-06，盲 eval R7 换能力面 evidence/截图/hook 探索 + 真站验证）**：
  - **E1**：`browser_evidence` artifact hint 漏 `data.` 前缀（`sources`→`data.sources`）——与 N1 同类、不同工具。修 `summaries/evidence.ts`，回归 `check-summaries`。R7 类审计确认：bridge-wrapped 工具(scan/network/evidence/hook)需 data-rooted（已全对）；web-security 工具(crawl/recon/fuzz/domFlow) artifact 是 flat，裸路径正确（domFlow 自检 wrapped）——**不动，避免过拟合**。
  - **H3**：`browser_hook collect` 事件被埋——collect 落到 `domFlowDistiller` 的 generic 分支，events 折叠成 keyCount stub、nextActions 给 `operation.operationId`。修：新 `summaries/hook.ts` `summarizeHookCollectData`（event 类型计数 + seq/type/detail 紧凑样本 + data-rooted `data.events` hint），由新 `hookToolDistiller` 路由 `hook.collect`。回归 `check-summaries`。真站验证：eventTypes/total/samples 内联 + `data.events` hint。
  - **minor 1**：screenshot 缺宽高——`registerScreenshotTool` 现从 data URL 解码 PNG IHDR / JPEG SOF 得 width/height 进 summary（解不出则省略不伪造）。单元测试 `tests/unit/tools/screenshot-dimensions.test.ts`。真站：`data.width:2058,height:1514`。
  - **minor 2**：`hook installTargets` 的 `--params {targets}` 无文档——确认 `hook.install_targets` 无 targets 即报错(`hook.ts:137`)，故 `native_command_schema.json` 标 `required:["targets"]`，F4 per-action help 现显示 `installTargets requires targets`。
  全量 check 绿，unit 519/0，token-economy 0.06，docs/protocol 重生成。同 `blind-findings.md`。

- 修复 **N1 + M1（2026-06-06，盲 eval R6 换能力面 network/causal 探索 + 真站验证）**，并关闭 **M2**（harness 产物）：
  - **N1**：network 结果 artifact-read hint 漏 `data.` 前缀——`summaries/network.ts` `entriesContainerPath` 发 `items`（相对 bridgeResult 的 `.data`），但 `read_saved_artifact jsonPath=…` 是对 **saved artifact**（root 是整个 bridgeResult `{id,tabId,data,…}`）解析的，故 hint 指向不存在的 `items[…]`→notFound（scan 早已正确用 `data.content`）。修：路径加 `data.` 前缀。回归 `check-summaries`（network hint 必须 data-rooted）。真站验证：hint 现为 `data.items[…]`、一次解析成功、且带 H2 的 `mode=json`。
  - **M1**：`browser_observe` 的 baseline（treeDiff/diff/causal）经 CLI 按引用不可达——`--baseline` 只收内联 Array/Object（完整 prior envelope，几十~几百 KB）→ CLI 超 argv（ENAMETOOLONG）；bare snapshotId 被 schema 拒（与 skill 矛盾）。daemon 其实已能解析带 `{snapshotId}`/`{saved:{path}}` 的 baseline 对象，但无文档、JSON-on-CLI 难用。修：加两个 CLI 标量 flag `--baseline-snapshot-id <id>`、`--baseline-path <file>`，薄映射到现有解析，零 daemon 改动；skill 改为"按引用传 baseline、勿内联"。回归 `flags-render.test.ts`（observe 暴露两 flag）。真站验证：`--baseline-snapshot-id` 导航后产出 `diff`+`treeDiff`(74 appeared/354 disappeared)，causal 在（因 baseline 早于 network start 而正确 unavailable）。
  - **M2（关闭，非工具 bug）**：`browser_execute` 整体挂死实为 **harness 脏状态产物**——R6 跑在被 12 个残留 isolated-Edge 污染的浏览器里。修好 teardown 后干净浏览器复现 `1+1→2` 立即正常。
  - **Harness 修复**：`teardown-blind.mjs` 之前 `taskkill /T` 只杀追踪的 launch pid，漏掉 re-parent 的 Edge 子进程→残留 + 锁 temp 目录→脏复用污染信号。改为按本轮唯一 profile 路径命令行匹配杀 msedge/chrome + 等 800ms 再删目录。自测：teardown 后残留 Edge 0（原 12）。
  全量 check 绿，unit 516/0，token-economy 0.06，`docs:generate` 重生成。同 `blind-findings.md`。

- 修复 **H1+H2 — execute nextActions 误导 + artifact --json-path 无 mode 静默失效（2026-06-06，盲 eval n=2 bilibili+linux.do 真站验证确认）**：**H1**：execute 返回小值已内联后，`nextActions` 仍推 `read_saved_artifact jsonPath=operation.operationId`，对已有答案的 agent 是噪声——execute distill 加 `dataInline:true` 标记（`summarizeGenericValue` 的 data 非形状占位时），`artifactReadActions` 检测该标志并抑制关联路径 nextActions。**H2（两个 fix）**：① `artifactReadActions` 的 preferredReads hints 格式改为 `read_saved_artifact mode=json jsonPath=X`（补全 `mode=json`，agent 可直接翻译成 CLI 调用）；② `readBrowserArtifact`（`artifactReader.ts`）在 `--json-path`/`--pick` 存在但未指定 mode 时自动升级到 json 模式（之前静默回退 text，`--json-path` 被丢弃，整个文件返回；n=2 两次 agent 均撞到）。H2 两个 fix 配套：hint 说明用法，tool 也容错（从根本上消除歧义）。回归：`check-summaries`（nextActions hint 包含 `jsonPath=…` 不限于旧前缀，兼容新旧格式）；`check-artifact` 通过；`docs:generate` 重新生成。全量 check 绿，unit 515/0，token-economy 0.06。同 `blind-findings.md` Resolved 段。

- 修复 **F2 — envelope 冗余膨胀 + `[Circular]` 占位（2026-06-06，盲 agent eval n=2 驱动）**：先实测（本地 fixture，结构确定、隔离内容噪声）抓真实证据：envelope 20KB，其中 `summary.preview` 占 **6767B（summary 的 91%）**，含两个 `[Circular]`，summary 已 `summaryTruncatedToBudget:true`。追根溯源三条独立缺陷并全部修：**(A)** 预算兜底路径（summary 超 ~8KB）把整个紧凑 summary 重 dump 成最大 8KB 的 preview 串——但页面模型已 lift 至 envelope 顶层（entities/gist/diff/…）+ saved artifact，preview 只需定向，加 `PREVIEW_FALLBACK_CHARS=800` 限制（`resultMiddleware.ts`）；**(B)** `observeRunners.ts` 把 `diff`/`treeDiff`/`snapshotProjection` 同时放进 `summary` 顶层和 `focus`（共享引用），redaction 把第二次出现折叠成 `[Circular]`——移除 `focus` 的冗余副本（lift 从顶层读，无消费方读 `focus.{diff,treeDiff,snapshotProjection}`）；**(C)** scan 摘要器让 `summary.headings`/`summary.top_layer` 与 `focus` 同字段共享引用——改为独立副本。**实测结果**（新 daemon，真实 fixture 页）：20KB→14KB（−29%），summary 7435→1557B，preview 6767→916B，`[Circular]` 2→0；entities/gist 等真实信号完整保留。**诚实的范围说明**：entity 对象在 `primary_actions[].entity`/`primary_entities`/`envelope.entities` 间仍有意共享（契约要求，`check-abml-scan-entities` 明确断言），redaction 对这些副本仍输出 `[Circular]`（含义是"完整实体在 envelope.entities"，非数据丢失）；按"不为改而改"不修。回归：`check-summaries`（preview ≤800 + 兜底 envelope 无 [Circular]）与 `check-abml-scan-envelope`（headings/top_layer 不得为 [Circular]）。`npm run check` 全量绿，token-economy 0.06 未损。同 `blind-findings.md` Resolved 段。

- 修复 **F4 — action 工具 `--params` per-action 形状无文档（2026-06-06，盲 agent eval 发现 n=2 驱动）**：先按"真·通用缺陷不过拟合"原则定性——根因**非 wait 专属**：`wait/network/hook/frame` 共用松散的 `params: NativeCommandParamsSchema = Type.Object({}, {additionalProperties:true})`，`--params <json>` 对四者都不显形状，wait-only 修法会过拟合。修法（源真、零漂移）：`sync:protocol` 生成器现在把每个 action 与其命令在 `bridge/native_command_schema.json` 的 `required`/`requiredAny`/`notes` join 进 `nativeActionMetadata`，`pi-browser <tool> --help` 渲染 per-action `--params` 键段（`cli/index.ts` `nativeActionParamsHelp`）；对全族每个 action 派生、绝不手写，`check:protocol` 兜底漂移。诚实局限：schema 只编码必填键+notes（覆盖 wait.selector→selector、wait.navigate→url、hook.evaluate→expression、frame.evaluate→frameId,expression），不含完整可选/类型（network 命令无必填→显示为空、不编造）；完整参数规格是更大的源扩充，按需再做。回归 `tests/unit/cli/flags-render.test.ts`（`F4: nativeActionParamsHelp …`）。纯可发现性，`--params <json>` 行为不变。同 `blind-findings.md` Resolved 段。

- 修复 **F1 — `browser_execute` 小返回值渲染（2026-06-06，盲 agent eval 头号发现 n=3 驱动）**：`src/tools/summaries/generic.ts` 的 `compactSample` 是 depth-2 + 5-样本 + 240-字符限制，导致嵌套在对象/数组里的值（R1 rows→cells、R3 `{count,rows}`）即使整体只有几百字节也被折叠成 `{type,count/keyCount}` 占位，逼 agent 二次 `browser_artifact` 取回自己刚算出的数据。修法：`summarizePlainValue` 现在对**整个返回值**做大小判断，序列化在 `INLINE_VALUE_CHARS`(4KB) 内则 **JSON 克隆后原样内联**；真正的大 payload 仍折叠为紧凑形状（样本保持小）且全量落 saved artifact，`fitSummaryBudget`(~8KB) 仍是硬上限；脱敏在 distill 之后照常生效。回归锁定在 `tests/contracts/tools/check-summaries.mjs`（`generic.inline.*`）。经重建 daemon 在真实中国大陆可达站点（linux.do）现场验证：`{count,rows:[{title,replies}…]}` 抽取现在**一次调用内联返回**，无需二次 artifact。**不是**用执行类帮手"修"的（那会是 WAI）——纯输出工程。同 `blind-findings.md` Resolved 段。

- 新增 **常驻盲 agent eval 机制（2026-06-06，成熟维护期优化驱动）**：在既有 deterministic runner（`runner.mjs`，人手写序列、防回归但发现不了新摩擦）之上新增**盲 agent 发现层**，作为项目正式组成部分。盲 subagent **先读 `pi-browser-tools` skill**、在**真实、中国大陆可达的网站**上**只读**完成任务、产出 command-log + 三分类摩擦报告（`fixable | WAI | reliability`，外加 skill↔tool 一致性）；"盲" = 只对实现源码盲，skill 必读。隔离：独立 `PI_BROWSER_DAEMON_STATE_DIR` + 强制 bridge 端口（18801+），盲 agent 经 `pb-blind.mjs` 包装器物理锁死该舞台，绝不触碰用户真实浏览器。新增 `evals/browser-workflows/{launch-blind,teardown-blind,pb-blind}.mjs`、`blind-agent-prompt.md`、`blind-tasks-realsite.md`、滚动 backlog `blind-findings.md`，npm 脚本 `eval:blind:launch`/`eval:blind:teardown`，编排 skill `skills/pi-browser-blind-eval/`。三分类规则：感知/输出工程/参数可发现性/可靠性 = 可修；"无 click/type 帮手、手写 form-fill" = 设计如此（WAI 不修）。验证：两次独立盲跑（login→orders 抽取 / spec 02）+ 一次真站 skill-guided 跑（linux.do），全程隔离、安全约束保持，收敛到头号可修项（`browser_execute` 返回值渲染：默认折叠/截断，逼二次 artifact，n=3 且扛过 skill）。不属于 `npm run check`（operator/cron 驱动）。边界 `evals/browser-workflows/future-runner.md`，治理 `CURRENT.md`。同步更新 README/evals README/CLAUDE/package.json。

- 完成 **Browser-tools skeptical-eval 修复线（2026-06-05，n=1 真 agent 评估反馈）**：`browser_execute action.click/type` 默认去掉 before/after 全量 `readStructure`，改为目标语义状态 probe verify，完整 entity diff 通过 `diff:true` 显式 opt-in；click/type/scroll 共用 action deadline，scroll 默认 bounded/probe-only 且虚拟列表结构收集改 `scroll.collect:true`；URL query 脱敏覆盖 PII-looking 值和通用 human-query 参数；templates suppress 冗余 text-leaf groups 但保留 text-only accessible structures；`diff.summary` 增加 salience 视图并保留 raw arrays，`treeDiff` 排序优先 value/state/name 变化；observe artifact 镜像 live envelope 顶层 `diff/treeDiff/templates/snapshotProjection/causal`；baseline expired 错误增加 recovery/nextActions，bridge execute 透传 `timeoutMs` 到扩展脚本执行。同步更新 README、coverage map、skill、CURRENT/TODO、执行合同与相关 unit/contract。

- 完成 **ABML R3 Runtime Events / temporal diff**：新增纯核 `src/abml-core/diff.ts`（appeared/disappeared/state-changed/name-changed/focusedRef）+ `src/abml/diff.ts` shim + barrel/boundary 清单；`runAbmlRead` 支持 `baseline` 并返回 `diff`，`browser_observe mode=scan` 可传上一轮 entity list / scan summary/envelope / snapshotId 计算顶层 `diff`，result middleware 与 `EntityDiffSchema` 保证预算免疫；click/type ABML runtime 成功结果附带 post-action diff。为让两次快照可按 `pi-ref://` 匹配，ref registry 改为基于同页语义 locator hash 稳定铸 ref。R2 推理扩展 `form-dependency`，在 disabled→enabled + focused editable field 时输出 `{enabledRef, requiredRef}`。新增 `tests/unit/abml/diff.test.ts`、`tests/contracts/tools/check-abml-diff.mjs`、`check:abml-diff` 并接入 grouped contracts；更新 README/skill/docs。验证通过 `check:src:types`、R3/R2/scan envelope 局部门禁。

- 完成 **ABML R1 关系图谱 batch 2（currentIn + 遮挡）+ 真机验证 → R1 全线收口**：补齐 batch 1 因真机 AX 限制推迟的两族,统一走已有 `materializeRelations`。**currentIn（DOM 源)**:Chrome `getFullAXTree` 不暴露 `aria-current`,故改由 `buildScanScript` 读取 → `actionEntityState` 写入 `state.current`(顺带补上 P1 `state.current` 在真机只有合成测试覆盖的隐性缺口);`axRuntime` 改为 stash 最近 nav/list 容器键 `hints.currentContainerKey`(不再在 AX 侧发 anchor);纯核新增 `deriveStateRelationAnchors` 在 merge 后据 `state.current` + 容器键派生 `currentIn`。**遮挡（hit-test 源)**:不猜 z-order,复用扫描已有的 `elementFromPoint` 命中测试——`visibleInfo` 在 actionable 自身命中失败时记录压在中心点上方的元素 selector(遮挡者),`state.occluded` 保留 + stash `hints.occluderSelector`;`deriveStateRelationAnchors` 按 selector(新增 `s:<selector>` 键)匹配实体 → `coveredBy`(被遮→遮挡者)+ `occludes`(逆向),`source:"geometry"`、显式 `confidence`;遮挡者若非已扫描实体则关系丢弃(但 `state.occluded` 仍在),无 CSS 堆叠猜测、负样本不误报。`runtime.ts` 把 AX anchors 与派生 anchors 合并后物化。fixture 增遮挡叠放 + 负样本;smoke gate 纳入 currentIn + 遮挡。**真机 8 族全绿**:labelledBy 2/describedBy 1/controls 1/expandedTarget 1/cellOf·rowOf 6/columnOf 4/headerFor 2/currentIn 1/coveredBy 1/occludes 1,目标全 `pi-ref://`。`browser_abml_*` 公开面不变;`npm run check` 全绿、`test:unit` 394、live smoke PASS。R1 关系图谱执行契约标记 COMPLETE。

- 完成 **ABML R1 关系图谱（batch 1）+ 真机验证**：在 `browser_observe`（无页面 JS）上把 AX 关系显式化为实体上的 typed `EntityRelation[]`，并在 envelope 顶层新增预算免疫的 `relations`（`summary` 类型计数 + 确定性截断 `highlights`），与 `gist`/`outline` 并列。覆盖 labelledBy/describedBy/controls/owns/expandedTarget（AX relation 属性）+ 表格 cellOf/rowOf/columnOf/headerFor（AX 层级，行/列号写入 `EntityStructure`）。两段式管线：纯核 `src/abml-core/relations.ts`（anchor→ref 物化、去重、按类型确定性 cap、summary builder）+ `ax.ts` 逐节点属性 anchor 抽取；运行时 `axRuntime.ts` 出层级 anchor，并在 DOM↔AX merge 铸 ref 后由 `runtime.ts` 物化——pre-ref 的 backend/AX node id 绝不外泄，目标一律 `pi-ref://`。真机硬化：aria-labelledby/describedby/controls 的 `relatedNodes` 指向目标*元素*，而该元素 AX 节点常是 `generic` 包裹（文本在子 StaticText），故 `resolveAnchorTargets` 把目标重定向到最近的已建后代，否则指向纯文本的 labelledBy/describedBy 会静默丢失。`currentIn`（R1.5）抽取代码已落但**推迟**：Chrome `Accessibility.getFullAXTree` 不在节点属性里暴露 `aria-current`（顺带暴露 P1 `state.current` 仅合成测试覆盖的隐性缺口），需 DOM 源回退，连同几何遮挡 `occludes`/`coveredBy`（R1.6）一并归入 batch 2。新增纯核模块（清单 15→16）+ shim + barrel + 边界白名单更新；`EntityRelationSchema`/`RelationSummarySchema` 进 outputSchema；`check:abml-relation-graph` 合同 + `smoke:browser:abml-relations` live smoke（真机：labelledBy 2/describedBy 1/controls 1/expandedTarget 1/cellOf·rowOf 6/columnOf 4/headerFor 2，目标全 `pi-ref://`）。`browser_abml_*` 公开面不变（内部 substrate）；`npm run check` 全绿、`test:unit` 390 通过。详见 `docs/abml-relationship-graph-execution-plan.md`。

- 完成 **CLI + Skill 前端迁移并移除 MCP shell**：外部前端从 MCP server 切换为 `pi-browser` CLI（由用户级单例 bridge daemon 驱动，自动启动 + `daemon start|stop|status`，lockfile 在用户级状态根 `~/.pi/browser-daemon.json`）；解析/帮助/注册本地化、不启浏览器，仅工具执行经 daemon `/invoke {tool,params,cwd}` 委派，调用方 `cwd` 随每次调用透传以保证 artifacts/memory/OAST 等请求级路径落在调用方 `.pi/`。整目录删除 `mcp/`（server/bin/adapter/handleResolver/prompts/resource·memory store/toolVisibility·Annotations/structuredEnvelopeSchema）+ 14 个 MCP 协议合同；可复用前端/资源基础设施早已迁到 `src/frontend/` 与 `src/resources/`。`package.json` 去掉 `./mcp` export、`pi-browser-mcp` bin、`mcp`/`check:mcp-*` 脚本与 `@modelcontextprotocol/sdk` 运行时依赖（lockfile 减 82 包，生产依赖收敛为 js-yaml/typebox/typescript/ws/zod），`pi-browser` 成为唯一 bin；`tsconfig.build` 与 eslint glob 由 `mcp/**` 切到 `cli/**`，`files[]` 改 `cli/`。**Pi-native `index.ts` 不变且验证通过**（注册全部 22 工具、零 mcp 依赖）；新增 `docs/cli.md`、`check:cli-parity`、`check:cli-migration-drift`（永久禁止 active code 再现 MCP-only 标识）、`tests/unit/cli/*`、`tests/unit/frontend/middleware.test.ts` 与 live `tests/smoke/smoke-cli.mjs`（真实浏览器端到端通过）；SKILL/README/AI_INSTALL/CURRENT/CLAUDE 切到 CLI 口径，历史 MCP 文档加 HISTORICAL 横幅，移除两个针对 MCP 行为的过时 eval（28/29）。`browser_*` 工具面、artifact/redaction 行为与能力 profile 全程不变；`npm run check` 全绿、`npm run smoke:cli` 通过。

- 完成 **ABML 内核解耦**：纯核 15 模块 + barrel 物理分层到 `src/abml-core/`（零浏览器/Node 依赖），运行时层留 `src/abml/`，经 re-export shim 保住全部现有 import 路径；新增 `check:abml-core-boundary` 锁定"纯核只能 import 纯核 + 白名单纯 utils、绝不 import driver/tools/scan/resources/node"，清单见 `docs/abml-kernel-manifest.md`。

- 新增 `browser_memory` v1：本地 `.pi/browser-memory/` 的 browser memory 工具，支持 `record | recall | read | validate` 与 `scopeKind:"origin"|"task"|"project"`；`record/validate` 允许无 evidence 直接结晶本地经验，`evidenceRefs` 作为可选 provenance，提供时必须可解析；写入 per-entry SOP/fact + 派生 `index.json` + tombstone；新增持久 `browser-memory://index|sop/{id}|fact/{id}` MCP resource、shared file freshness helper、`browser-result` evidence resolver 注入，以及 observe exact-origin 命中的一条轻量 memory `nextActions` auto-surface。同步更新 registry/budgets/MCP annotations/visibility、contracts、README、tool boundaries、skill 与 generated tool docs。

- 完成 MCP Phase 10 延后问题实际解决：10A network summary 现在发 bounded per-entry `kind:http-request` artifact hints，MCP section resource 保留 jsonPath、etag 与 selected-slice sha256，`resolveIngressHandles()` 可把 section URI 直接展开给 `browser_sqli.request`；10B middleware 新增 `on_list_prompts` / `on_get_prompt` hooks，`on_message` 保持 fallback-only；10C token economy 从 3 个扩到 9 个 deterministic fixtures，写入 `.pi/browser-artifacts/token-economy-summary.json`。同步将 eval 28/29 sample results 改为 passed。

- 完成 MCP 延后问题 eval 起步：新增 `28-mcp-network-request-handle` spec/fixture/result，记录 network raw-result 到 `browser_sqli.request` 缺 per-entry `kind:http-request` typed handle；新增 `29-mcp-middleware-coverage` spec/fixture/result，确认 `on_message` 是 fallback-only，后续只需显式 prompt hook call sites，不需要 transport wrapper；更新 manifest/README/`check:eval-workflows`。

- 完成 MCP Phase 9 stretch：新增 MCP-only `browser_tool_discovery` lightweight discovery helper、`PI_BROWSER_MCP_TOOL_VISIBILITY=full|compact|minimal`、`tools.listChanged:true` 与 discovery `revealGroup` 驱动的 `notifications/tools/list_changed`；新增 prompts capability 与 4 个静态 prompts（`browser-first-observe`、`browser-evidence-capture`、`browser-web-security-scope`、`browser-artifact-read`）。保持 Pi 公开 `browser_*` 工具面不变，prompts 只产出模板不自动调用工具。新增 `mcp/toolVisibility.ts`、`mcp/prompts.ts`、`check:mcp-prompts`、`check:mcp-dynamic-tools`，更新 `check:mcp-list-changed` 为 capability+notification 成对合同。

- 完成 MCP 执行合同的 6 项验收残留（A–F）：A 统一 structured-envelope schema（`mcp/structuredEnvelopeSchema.ts`，整封 envelope schema，与 per-tool summarySchema 区分；DistilledEnvelope 增 `sections` 可选字段）；B resource/ingress etag 与新鲜度（`statSync` 同步 etag 覆盖全部 kind、`http-request` 另加 sha256 内容哈希；read 路 `RESOURCE_STALE`、ingress 路 `HANDLE_ETAG_MISMATCH`）；C Layer-1 section 子资源（三个 distiller 经共享 `artifactHints` 发 `artifact_hints.preferredReads`，jsonPath 指向真实 raw artifact；adapter 注册 section 子资源 + resource_link + `envelope.sections`，section URI 默认 json 模式回读切片）；D 接通 `on_initialize`(→`server.oninitialized`) 与 `on_message`(→fallback request/notification handler，仅未注册方法，仍以 MethodNotFound 拒绝未知方法)；E 以 contract 锁定 list_changed 的"有意缺席"（后续 Phase 9 已改为真实 dynamic-list capability）；F token 经济基线（`tests/contracts/tools/fixtures/token-economy-baseline.json`，确定性内联 fixtures，Layer-0 vs raw 字符比，medianRatio 0.52，断言 layer0<raw + 非回归，`--update` 重生成）。新增 check:mcp-structured-envelope/etag/sections/list-changed 与 check:token-economy 合同；验收期另修复 structuredContent 超预算不符 outputSchema、Phase-2 测试测错对象、tools/call 静默日志路径，并补建 check:mcp-e2e 真实 stdio fixture。已记边界：网络→sqli 单请求 http-request 句柄需 per-entry 子资源（延后）；`on_message` 仅覆盖未注册方法（SDK 无通用逐消息钩子）；token 指标为 3-fixture guardrail。Pi adapter 行为全程不变；`npm run check` 全绿。

- 完成 MCP 标准化 + 渐进式披露执行合同（Phase -1 → Phase 8）：Phase 0 补 TypeBox 参数校验 + onUpdate → progress 映射；Phase 1 工具 annotations + `./mcp` library export + tools/list snapshot contract；Phase 2 DistillerDefinition{summarySchema,distill} + 3 工具 outputSchemas spike（browser_evidence/network/hook），conformance contract；Phase 3 structuredContent egress + outputSchema in tools/list；Phase 4 browser-result:// resource store + resources/read/list/templates + resource_link injection；Phase 5 nextActions MCP adapter 转换（browser_artifact path= → resources/read uri=）；Phase 6 ingress handle resolution（browser_sqli/http_replay/fuzz request 字段两阶段校验）；Phase 7 protocol middleware outer ring（on_list_tools/on_call_tool/on_read_resource/on_log + timingLogHook）；Phase 8 browser_artifact capability-gated MCP-path retirement（modern client 隐藏，PI_BROWSER_MCP_KEEP_ARTIFACT=1 可保留）。Pi adapter 行为全程不变；新增 check:mcp-parameter-contract/conformance/tools-list/resources/ingress-handles/middleware/output-schema-conformance 合同；npm run check 全部通过。

- 冻结 MCP 标准化 + 渐进式披露正式执行合同：新增 `docs/mcp-standardization-progressive-disclosure-plan.md` 作为 source of truth，并将 `.plan/mcp-standardization-progressive-disclosure.md` 收敛为评审输入指针；合同修正评审中发现的关键边界：不把 TypeBox 顶层参数迁到 Zod、Phase 0 先补 MCP 入口等价参数校验与 conformance、outputSchema 改用显式 distiller schema registry、`browser-result://` 先落 opaque resource store、`browser_artifact` 仅允许 MCP 路按 capability gate 退役且 Pi 路保留。同步 `CURRENT.md` 与 `TODO.md` 记录执行顺序与非目标。

- 完成 ESLint 遗留债务 ratchet 执行合同：将实测基线 `0 errors / 172 warnings / 56 files` 清零到 `npm run lint` 的 `0 errors / 0 warnings`，按 W1-W4 顺序修复 async Promise 安全、错误链、fallthrough/sparse/control-regex、低风险机械规则、silent-catch 与 unused-vars 债务；`eslint.config.js` 中本合同内规则族已由 `warn` 收紧为 `error`，`.github/workflows/check.yml` 的 `Run ESLint` 已移除 `continue-on-error: true`，`lefthook.yml` staged lint 增加 `--no-warn-ignored`。保持边界：未引入格式化器，未开启额外 `no-unsafe-*` / `@typescript-eslint/no-explicit-any`，未执行全仓 `eslint . --fix`，未改变 `check:lint` 的 boundary/contracts 职责。验证通过 `npm run lint`、`npm run check:all:bridge`、`npm run check:web-security`、`npm run test:unit`、`npm run check`、`npm run quality:local`。

- 完成 M-001 第一轮深拆：新增 `src/driver/BrowserBridgeCommandService.ts` 承载 command validation / target planning / payload transport，新增 `src/driver/BrowserBridgeClientMessageService.ts` 承载 WebSocket client register/unregister/message route，`src/driver/BrowserBridgeServer.ts` 收敛为更薄 facade 并保持 public API、错误码、lease/queue/session 语义不变；同步修复 `src/driver/BrowserBridgePendingRequests.ts` 中 timer/pending 先引用后赋值的脆弱初始化顺序。另补共享 helper 收口：`src/utils/records.ts` 去除泛型 `recordValue<T>()` 的不安全断言，`src/tools/webSecurity/shared/replay.ts` 与 `src/tools/webSecurity/shared/template.ts` 清零 `as any`。验证通过 `npm run check:src:types`、`npm run test:unit`、`npm run check:fake-ws`、`npm run check:lifecycle`、`npm run check:bridge`、`npm run docs:generate`。
- 完成高收益窄项收口：M-003 将 `bridge_src/service_worker/types.ts` 的 `PiBridgeResponse.error` 收口为显式 `PiBridgeResponseError` 联合，去掉过宽 `JsonRecord & ...` 顶层扩展；M-006 让 `runWebSecurityTool()` 使用真实 `TPrepared` 泛型，去除 `as unknown as TRunParams`，并把 `attachOperationToError()` 从 `Object.assign(new Error(...))` 改为稳定 `BrowserBridgeError` 包装；M-004 清理 TODO 189 历史口径，contracts 统一只接受 `// ESM module metadata`。
- 完成剩余延后债收口（按收窄边界）：helper/parse/drift 新增 `parseJsonOrThrow()` 并把 `BrowserBridgeClientMessageService`、`webSecurity/shared/replay.ts`、`oastWorkerManager.ts` 收口到共享 parse helper，同时在 `check-boundaries` 限制 `src/` 的 `JSON.parse` 只允许留在审核过的 parse roots；H-002 第二批在 runtime state map storage 失败与 `ws.close` 失败路径补 `console.warn`，剩余 targeted silent catch 明确保留为 A 类 best-effort cleanup/probing；M-010 以 core-only 边界为 `network.ts` / `network_model.ts` 新增显式 `NetworkRecorderSummary` / `NetworkClearResult` / `NetworkHarContent` / `NetworkHarEntry` 类型，减少 summary/clear/HAR 路径对泛化 `JsonRecord` 的依赖。
- 完成 Phase 1 当前轮收口：`src/` 内 `Type.Any()` 与 `as any` 已清零；`browser_command`、`browser_http_replay`、`browser_fuzz`、`browser_template`、`browser_cookie_analyze`、`browser_callback_oast`、`browser_sqli` 以及 Web Security shared register helpers 的开放对象输入改为 `Type.Object({}, { additionalProperties:true })`、显式联合类型或记录类型，同时保留既有运行时 Zod 校验与工具契约。验证通过 `npm run check:src:types`、`npm run test:unit`、`npm run check:web-security`、`npm run check:bridge`。
- 完成 Phase 3 低风险半步：仓库 scripts 已从 `--experimental-strip-types` 迁到 `tsx` 运行 TypeScript/MJS 测试、contract、runtime fixture、smoke 与 MCP 入口，不改 `index.ts` / `pi.extensions` 源码入口；同步更新 `tests/contracts/drift/check-package-files.mjs`、`check-boundaries.mjs`、`check-bridge-files.mjs` 与 `tests/smoke/smoke-browser-isolated.mjs`。验证通过 `npm run test:unit`、`npm run check:web-security`、`npm run check:fake-ws`、`npm run check:lifecycle`、`npm run check:page-scripts`、`npm run check:runtime-fixtures`、`npm run check:bridge`、`npm run check`。
- 完成完整 Phase 3 核心迁移：新增 `tsconfig.build.json`，把 `index.ts` / `src/**/*.ts` / `mcp/**/*.ts` 的 Node ESM 相对导入切到 `.js` 后缀，`npm run build` / `tsc -p tsconfig.build.json` 已可成功生成 outer `dist/`；npm/package `main`/`types`/`exports` 与 `pi-browser-mcp` bin 已切到编译产物，`prepack` 会先 build outer dist 再 quiet build bridge dist，同时保留 Pi runtime 的 `pi.extensions:["./index.ts"]` 源码入口。同步更新 package/contracts/docs 边界，验证通过 `npm run build`、`npm run check:package`、`npm run check:bridge`、`npm run check`。
- 清理废弃 TypeScript loader：删除 `scripts/register-ts-extension-loader.mjs` 与 `scripts/ts-extension-loader.mjs`，并同步 `check-boundaries` 与文档口径；仓库运行链已完全依赖 `tsx` 与 `tsc build`，不再保留旧 loader 兼容路径。
- 完成 Phase 5 依赖事实收口：生产依赖 allowlist 现为 `@modelcontextprotocol/sdk`、`js-yaml`、`typebox`、`typescript`、`ws`、`zod`；其中 `typebox`/`typescript`/`zod` 由源码运行路径直接消费，因此保留为 runtime dependencies，而不是机械迁入 devDependencies。同步更新 `check:deps` 契约与安装文档，验证通过 `npm run check:deps`、`npm run check`。

- 完成 Web Security 工具 Type.Any() 运行时验证迁移：为 `browser_http_replay`、`browser_fuzz`、`browser_cookie_analyze`、`browser_template` 四个工具添加运行时参数验证，使用 Zod schema 验证 `request`、`multipart`、`mutations`、`variables`、`jsonValues`、`cookies`、`claimMutations`、`templates`、`tech` 等复杂对象参数；保持 MCP 协议兼容性（TypeBox schema 未修改），在 execute 函数中通过 `validateOptionalParams` 中间件进行验证；新增 `TemplatesSchema` 和 `TechHintsSchema` 到 `src/validation/schemas.ts`；更新依赖白名单添加 `@modelcontextprotocol/sdk`；所有检查通过（类型检查、单元测试、contracts）。
- 规划 Web Security affordance / validation / recovery 收口：冻结实现边界为”补 agent-first 接缝信息而非引入固定 workflow”。下一轮仅允许补并列 `possible/common follow-ups`、集中式参数组合校验、已有 `recovery` 透传与高频 schema 收敛；明确禁止工具内部自动串联 `crawl -> fuzz -> sqli`、自动升级 mode/engine/action、或从 crawl 伪造请求模板。同步 `CURRENT.md`、`TODO.md`、`NEXT_PHASE.md`、README 记录执行合同与非目标。
- 归档 legacy `tmwd_cdp_bridge`：删除 `bridge/tmwd_cdp_bridge/` 工作树源码副本，不再通过 npm/package 分发该遗留桥；新增 `docs/archive/tmwd-cdp-bridge-legacy.md` 与 `.full.md` 记录归档决策、风险、边界与 git 历史取回方式，`docs/asset-sync.md` 与 contracts 同步更新。
- 规划 bridge runtime hardening / command access schema / silent-catch governance：冻结 H-005 `PendingRequest` 初始化顺序修复、H-003 state store per-kind 写锁、H-004 CSP bypass TTL 从 `setTimeout` 收口到 MV3 可靠机制、H-001 command `accessMode` 下沉 protocol schema 单源，以及 H-002 对 bridge 侧静默 catch 的 A/B/C 分类治理；同步 `CURRENT.md` 与 `TODO.md` 记录实施顺序、非目标与验证计划。
- 规划 H-002 第二批静默 catch 分类治理：范围限定在 wait/cdp/ws/network/intercept/transport/runtime 的 cleanup 与 recovery 主路径；selector probing、serialization fallback、regex fallback、best-effort removeListener/clearTimeout 保持 A 类保留，不做机械全量替换。同步 `CURRENT.md` 与 `TODO.md` 记录优先级与暂缓范围。
- 冻结 ESLint 遗留债务 ratchet 执行合同：新增 `docs/lint-debt-ratchet-plan.md` 取代 `.plan/lint-debt-ratchet.md` 作为唯一执行源；基于当前仓库实测 `0 errors / 172 warnings / 56 files` 基线，明确只分批收口已启用的 warn 规则族，按“正确性类 → 低风险机械类 → no-empty / silent-catch → no-unused-vars → 最终 CI lint 阻断”顺序推进；同时冻结非目标：不引入格式化器、不把 `no-unsafe-*` / `@typescript-eslint/no-explicit-any` 混入本轮、不执行全仓 `eslint . --fix`、不改 `check:lint` 的 boundary/contracts 职责。同步 `CURRENT.md` 与 `TODO.md` 记录验证矩阵与 ratchet 边界。
- 规划 M-001 ~ M-011 中严重度工程债收口：冻结执行顺序为 M-003 bridge response error 类型收窄、M-008 session registry 禁止隐式创建、M-005 wsSession 终态清理/上限、M-011 bridge WebSocket 连接数上限、M-002 network recorder 事件分支拆分、M-001 BrowserBridgeServer 继续拆薄、M-006 toolAdapter 去双重断言与原地错误改写、M-010 network model 摆脱 `JsonRecord` 基类，以及 M-004 去除 TODO 189 口径遗留；M-007/M-009 作为已由 H-001 关闭的问题归档，不重复改代码。同步 `CURRENT.md` 与 `TODO.md` 记录边界与验证计划。
- 激活 M-001 深拆子项：本轮仅拆 `BrowserBridgeServer` 内 command planning/payload transport/client message routing 到独立 driver helper，保持 facade public API、错误码、lease/queue/session 行为和既有 contracts 不变。

- 完成 TODO 197 service worker 基础层真实 ESM 迁移：`config/protocol/patterns/cdp/runtime/wait_*` 通过 ESM import/export 进入 dist bundle，`build-manifest.json` 记录 `foundationImported` 与 foundation/legacy 清单；本组 `@ts-nocheck` 清零，新增 `runtimeEnv.ts`/`types.ts` 基础类型边界，contracts 锁定 foundation 不再文本拼接。
## Unreleased

- 完成 ABML P8/P9 收口：DistilledEnvelope 新增 `entities/error`，`sections[].handle` 支持 `pi-ref://data-slice/*`，`nextActions` 去除本地 artifact path 泄露并改为动词/`read_saved_artifact` 意图；MCP `resources/read` 现在可直接读取 `pi-ref://data-slice/*`。同时定稿 surface convergence：当前版本不新增公开 `browser_*` 动词工具，公开 callable surface 继续以既有 `browser_*` 为准，ABML verb face 保持 internal/MCP envelope 语义。同步 README / tool boundaries / skill / contracts / token-economy gate。

- 冻结 MV3 runtime state recovery 执行合同：新增 `docs/mv3-runtime-state-recovery-plan.md`，明确 `chrome.storage.session` 只存 bounded/redacted 结构元数据，Node artifact 承载证据数据；恢复策略按 network/intercept/hook/ws/CDP/wait/queue 分级，新增 `RUNTIME_STATE_RECOVERED`、`RUNTIME_STATE_RECOVERED_WITH_HISTORY_LOSS`、`RUNTIME_STATE_LOST` 目标错误语义；hook 默认不自动 reinstall，WebSocket 默认不自动 reconnect，不新增公开工具面或 orchestration。
- 修复浏览器桥接工程债：driver 端 WebSocket 消息处理现在记录 JSON/运行时错误；`rejectAllStopped()` 保证所有 pending Promise 终结；新增 driver heartbeat/stale client 清理；CSP bypass 从全局规则收缩为按目标 tab 临时启用；写命令队列增加 `QUEUE_FULL` 深度上限；snapshot registry 主动清理 TTL 过期项；`browser_tabs` 错误统一为 `BrowserBridgeError`；`normalizeTabId/isRecord` 迁入共享参数工具；`browser_callback_oast` 的 trigger 等待参数改为 `triggerTimeoutMs`；枚举参数改用 TypeBox literal union；扩展端 WS session 失败/关闭路径清理 listener；补 `BrowserBridgePendingRequests`、`BrowserTabSessionRouter`、`BrowserBridgeClientRegistry`、`bridgeUtils` 单测与 `test:coverage` 入口；工具注册 contract 改用 TypeScript AST 读取注册名。
- 修复第二轮深度缺陷：callback OAST `sessionId` 路径收口为安全 slug、HTTPS 证书改为启动时动态生成、state lock stale-break 增加 token 校验、worker 增加 `maxRuntimeMs` 生命周期上限；旧 content page-script DOM 命令通道改为只做遗留节点清理；`location=` / `GM_openInTab()` / `browser_tabs create` 统一阻断 `javascript:` 和 `data:` 导航；WebSecurity `wordlistPath` 收口到 cwd/.pi 有界本地文件、HTTP fetch/TLS 探测默认阻断 private/link-local/metadata 目标并要求显式 `allowPrivateTargets`、mature bridge 显式 launcher override 要求 `allowLauncherOverride:true`；`browser_template_check` 增加 `maxRequests`，`browser_fuzz_params` / `browser_sqli_probe` / `browser_fuzz_paths` / `browser_fuzz_vhosts` 改为有界 case/candidate 累积；summary budget 增加最终 hard-cap；bridge runtime 持久化 network/intercept/ws 轻量 session descriptor，在 MV3 restart 后 `status` / missing-session 错误不再静默丢失状态；CI 新增 `check:lint` 并去掉重复 full-check job，npm 包不再包含 `bridge/tmwd_cdp_bridge/` legacy 目录。
- 启动 cross-tool evidence correlation metadata 收口：决策已同步到 `CURRENT.md` / `ROADMAP.md` / README；本轮不新增公开 `browser_*` 工具，只增强现有 distilled result envelope，让 `browser_observe`、`browser_execute`、`browser_command`、`browser_wait`、`browser_network`、`browser_hook`、`browser_evidence` 与 follow-up 工具稳定上浮 `operationId`、`snapshotId`、`requestId`、`waitId`、`listenerId`、`sessionId`、`selectionVersion*`、`sourceMode` 等对账线索，降低 agent 手工拼接跨工具证据链的负担。
- 继续补 artifact-side correlation 导航：`browser_artifact mode=json` 现在会从 artifact summary 中上浮 `operationId`、`snapshotId`、`requestId`、`waitId`、`listenerId`、`browserSessionId`、`selectionVersion*`、`sourceMode` 等对账线索，并附 `correlationPaths` 指向推荐 `jsonPath`；`resultMiddleware` 的 `nextActions` 也会优先建议按这些 correlation jsonPath 做定点 artifact 读取，而不是直接打开整份证据。
- 补齐 cross-tool correlation workflow 约束与验收：新增 `evals/browser-workflows/21-cross-tool-correlation-chain.md` 与 `results/21-cross-tool-correlation-chain.result.json`，并新增 opt-in runtime smoke `npm run smoke:browser:correlation-chain`，用本地 `interactive.html` fixture 验证 observe → execute → wait → artifact 的 `operationId` / `snapshotId` / `waitId` / `selectionVersion*` 与 targeted artifact jsonPath read。
- 冻结下阶段开发目标规划：新增 `docs/next-phase-web-reversing-and-security-primitives-plan.md`，把 JS AST / 反混淆、请求/响应拦截与热补丁、DOM 事件链 / sink-flow 分析、Wasm 逆向桥接、stateful WebSocket replay/fuzz primitives 纳入下一阶段 problem-area 规划；明确其是否属于 foundation capability、应先补 internal primitive 还是 mature bridge、以及何时才允许升级成公开 callable surface。
- 正式开启首个下阶段 active workstream：新增执行合同 `docs/request-response-interception-and-hotpatch-plan.md`，把“请求/响应拦截与热补丁原语”从 problem-area 规划提升为当前执行队列；同步 `CURRENT.md`、`ROADMAP.md`、`README.md`、`TODO.md` 入口，但不提前承诺新的 callable interception 工具。
- 完成请求/响应拦截与热补丁原语 phase 1：已接入 `intercept.*` internal/native primitives，并通过真实浏览器 runtime smoke 验证手工 fulfill、自动 fulfill、`replaceScript` 与 uninstall fail-closed 行为；继续保持 public surface 不扩张、artifact-first transcript 与显式 matcher 边界。
- 推进并完成请求/响应拦截与热补丁原语 phase 2：`intercept.continue` / auto-`continue` 已支持有界 request mutation（method/url/headers/body）归一化与 `mutationSummary`；`intercept.install` 已支持 `stages` 选择；driver 已把 interception 写命令纳入 lease/queue 语义；真实浏览器 smoke 已验证 request-mutate、tab-close-cleanup、lease-conflict 三条路径。
- 启动并完成 JS AST / 反混淆分析原语 phase 1：新增执行合同 `docs/js-ast-and-deobfuscation-primitives-plan.md`，落 internal / artifact-first parse、summary、suspicious-pattern facts、compact summary adapter、artifact output convention、internal slash-command path `/browser-js-ast`，以及 deterministic narrow reductions（string-array / decoder inline / constant folding / alias propagation / object-dispatch）；不新增公开 `browser_*` 工具。
- 启动并完成 DOM 事件链 / sink-flow 分析辅助 phase 1：新增执行合同 `docs/dom-event-chain-and-sink-flow-plan.md`，在既有 `browser_hook` canonical surface 下落 `getNodeListeners`、`getListenerChain`、`getSinkHints` 三类 internal/native actions，并补 compact summary adapter、local fixture、eval spec 与 sample result；保持 evidence-assist 边界，不新增公开 `browser_*` 工具，也不承诺 full taint engine。
- 启动并完成 Wasm 逆向桥接 phase 1：新增执行合同 `docs/wasm-reversing-bridge-plan.md`，落 internal / mature-bridge-first / artifact-first Wasm metadata、可选 WAT artifact、internal slash-command path `/browser-wasm` 与对应 eval/sample result；不新增公开 `browser_*` 工具，也不承诺 in-repo Wasm decompiler。
- 启动 Stateful WebSocket replay/fuzz primitives phase 1：新增执行合同 `docs/stateful-websocket-replay-and-fuzz-plan.md`，当前只承诺 internal / artifact-first session/transcript primitive，不新增公开 `browser_*` 工具，也不承诺黑盒 fuzz orchestrator。
- 推进 Stateful WebSocket replay/fuzz primitives phase 1 第一批：新增 internal/native `ws.open` / `ws.status` / `ws.send` / `ws.wait` / `ws.collect` / `ws.close`，补 tab-scoped session/transcript model、Node-side explicit WS shell/helper、compact summary adapter、internal slash-command path `/browser-ws`、unit/contracts 与 `27-websocket-session-transcript` eval/sample result；继续保持 internal-first，不新增公开 `browser_*` websocket fuzz 工具。
- 推进 Stateful WebSocket replay/fuzz primitives phase 1 第二批：补显式 bounded `ws.replay` message-sequence primitive，支持 step-by-step send、optional matcher wait、compact replay-step summary 与 transcript artifact 复用；继续保持 explicit input / no state-machine inference / no public websocket fuzz tool boundary。
- 收口 WS replay recoverable diagnostics：失败结果现在显式返回 `stepIndex`、`lastSeq`、`partialSteps`、`partialTranscript`；Node-side `/browser-ws replay` 失败时自动落 partial transcript artifact，便于后续定点读取和恢复。
- 同步 `27-websocket-session-transcript` eval spec/sample result：显式写入 replay-failure summary、partial transcript artifact、recoverable diagnostics 与“从失败 step 继续”边界，避免文档/样例仍停留在首批 send/wait-only 口径。
- 新增 opt-in runtime smoke `npm run smoke:browser:websocket-session`：使用独占临时扩展目录和本地 `WebSocketServer` fixture，验证 internal/native `ws.open` / `ws.send` / `ws.wait` / `ws.replay` / `ws.collect` / `ws.close`，以及 replay failure 的 `stepIndex` / `partialTranscript` 诊断与结果 artifact。
- 归档 Stateful WebSocket replay/fuzz primitives phase 1：该主线已完成 session lifecycle、bounded replay、replay failure recoverable diagnostics、partial transcript artifact、`27-websocket-session-transcript` eval/sample-result 以及真实浏览器 opt-in runtime smoke；当前不再保留 active queue。
- 推进工具层参数契约执行合同：新增 `strictToolParameters()`、`enumParam()`、`enumOrEnumArrayParam()` 到 `src/tools/toolShared.ts`；核心 tool register 顶层参数对象改为统一 `additionalProperties:false`；WebSecurity register 层新增共享枚举 helper(`webSecurityDefaultSchemeParam`、`webSecurityCookieModeParam`、`fuzzLocationParam`、`sqliProbeTypesParam`)，并将 `cookieMode`、`payloadMode`、`defaultScheme`、`locations`、`probeTypes`、`baselineStrategy`、`sniMode`、`knownFiles` 收紧为显式枚举；`registerSqli.ts` 补齐 `request` / `mutations` 的 Zod 内层校验，`registerCrawl.ts` 核对后保持无额外复杂对象校验。同步新增 `tests/contracts/drift/check-tool-parameter-contract.mjs` 与 `check:tool-parameter-contract`，并接入 grouped contracts 门禁。
- 完成架构与工程化设计改进执行合同：新增 `src/tools/distillerRegistry.ts` 与 `src/tools/summaries/registerBuiltinDistillers.ts`，把 `resultMiddleware` 的 fallback 摘要分发改为注册表 + coverage contract；`registerTools.ts` 显式初始化 distiller，direct-import contracts 也能稳定拿到内建摘要器。`BrowserBridgeCommandService.ts` 增加 write-path 复合不变量断言，`BrowserLeaseRegistry.ts` 增加 `peek/touch/sweepExpired/releaseLeasesForTabSessions/releaseUiLocksForBrowserSessions`，`BrowserBridgeClientMessageService.ts` 与 `BrowserBridgeClientHeartbeat.ts` 打通 disconnect cleanup + TTL sweep，`BrowserWaitSupervisor.ts` 改为动态 bridge grace / reconnect budget。同步新增 `tests/unit/driver/BrowserWaitSupervisor.test.ts`、扩充 lease/heartbeat/resultMiddleware 单测，contracts 重组为 `tests/contracts/{protocol,tools,runtime,drift}`，新增 `check:distiller-coverage`，并引入 `lefthook` + `lefthook.yml` 实现 protocol sync pre-commit hook。另新增 `tsconfig.base.json`、`docs/driver-architecture.md`，并更新 `CURRENT.md` / `TODO.md` / `README.md` / `package.json` 验证入口。
- 完成 TODO 257-261：新增 `docs/maintainer-map.md` 维护入口图；`bridge/native_command_schema.json` 增加 mature bridge 结构化错误码并同步生成 `src/protocol/*`、`bridge_src/service_worker/protocol.ts`、`docs/generated/native-protocol.generated.md`；新增 `src/tools/webSecurity/shared/matureBridge.ts` 收口 sqlmap/nuclei launcher probe、process timeout 和 failure record；`browser_sqlmap_bridge` / `browser_nuclei_bridge` 前移 target/template/launcher 失败并返回稳定结构化诊断；README/contracts/unit tests 同步，验证通过 `npm run sync:protocol`、`npm run docs:generate`、`npm run test:unit`、`npm run check:web-security`、`npm run check`。
- 完成本轮架构/文档/CI 治理收口：新增 `src/tools/toolRegistry.ts` 作为声明式工具注册表，core/security registrars 与 capability profile gating 不再散落在 `registerTools.ts`；补 `tests/unit/tools/toolRegistry.test.ts` 锁定 registry 数量、顺序、唯一性与 security gating；`scripts/sync-doc-indexes.mjs` 改为按 `docs/archive/*.md` 自动收集 summary/full 配对生成 `ARCHIVE.md`/`ROADMAP.md` 索引，`check-doc-structure` 同步取消对固定历史文件名和固定阶段号的强耦合；新增 `scripts/run-check-groups.mjs`，让 `npm run check` 通过 grouped runner 复用 bridge/unit、package/docs、contracts 三组本地门禁，并支持 `--json` 输出 `.pi/browser-artifacts/check-groups-summary.json`；`.github/workflows/check.yml` 直接复用这些 grouped entrypoints，且将重复的 Node/setup/install/build 抽到 `.github/actions/setup-node-build`；workflow 继续保持 `contracts-and-unit` / `package-acceptance` / `full-check` 分层 jobs，并新增 `workflow_dispatch` 下显式启用的 `browser-smoke` / `release-smoke` opt-in runtime jobs，artifact 通过 Actions upload 保留。
- 完成 TODO 250-256 首版工程治理收口：新增 `verify:bridge:dist`，把 `check:bridge:build` 改成只读验证当前 dist，并在 dist 缺失时要求先执行 `npm run build:bridge`；新增 `test:unit` 并接入 `npm run check`，当前使用 Node `node:test` 覆盖 `shared/http.ts`、`shared/replay.ts`、`shared/requestTemplate.ts`、`browserNative/sqliProbe.ts`、`toolAdapter.ts`；`scripts/build-bridge.mjs` 对 service worker entry 试开 tree-shaking，并将 build-manifest 模块清单改为 metadata-only 命名；Web Security 文案统一改成“browser cookie injection into HTTP requests”；补充 `docs/hook-dispatcher-multi-file-evaluation.md`；新增 `.github/workflows/check.yml`，让 PR/push 默认执行 `npm ci -> npm run build:bridge -> npm run check`。
- 冻结 TODO 244-249 工具面治理计划：新增 `docs/tool-surface-consolidation-plan.md`，明确 `browser_observe` 观察层 canonical 迁移、`browser_execute` / `browser_command` 拆分、recovery hints、bounded artifact multi-search、progress/streaming、explicit snapshots/operation metadata、Web Security capability profiles 的决策、边界、非目标、实施顺序、验证和退出标准；同步 `CURRENT.md`、`ROADMAP.md`、`NEXT_PHASE.md`、`docs/tool-boundaries.md`、README、TODO，当前仅规划文档，不改变 callable 工具。
- 完成 TODO 242 `browser_scan` high-entropy summary v2：默认 summary 新增 Scan Manifest `focus.primary_actions/forms/lists/text_signals` 与 `artifact_hints` 精确 JSON path，保留 artifact-first 和 legacy 紧凑 `actionables/list_hints`；`resultMiddleware` 会把 scan artifact hints 提升为可直接执行的 `browser_artifact mode=json jsonPath=...` 后续动作，并新增 `16-scan-high-entropy-summary` eval/fixture/contract 覆盖。
- 完成 TODO 241 jshookmcp 能力原生吸收闭环：新增闭环账本 `docs/jshookmcp-native-absorption.md`、5 个 synthetic eval specs/fixtures、jshook closure contract，并在 `browser_hook` 增加 bounded `listTargets` / `installTargets` 静态显式 hook target 扩展；拒绝新增 `browser_sources`、`browser_debugger`、`browser_intercept`、`browser_storage`、`browser_canvas` 公开工具，不迁移 AGPL 源码/文本/schema/payload/fixture/MCP 架构。
- 修复跨 Pi 对话窗口 bridge 争用：Node bridge 默认从 `18765-18784` 自动选择空闲端口，浏览器扩展从固定 `18765` 单连接改为扫描同一端口范围并同时维持多个 WebSocket；tab sync fan-out 到所有连接，两个 Pi 进程可共享同一个已加载浏览器扩展且分别显示 `browser:<实际端口>`。
- 完成 TODO 216 runtime smoke：`npm run smoke:browser:isolated` 通过，真实 Edge/Chrome bridge 覆盖两个 browser session 分别 execute/read、lease 冲突和 `target.browserSessionId`，artifact 写入 `.pi/browser-artifacts/smoke-browser-isolated-results.json`。
- 完成 TODO 216 阶段 6 browser session 证据贯通：distilled result envelope 记录 `browserSessionId`，native evidence/wait/network/hook 等经共享 result middleware 输出 session 维度，artifact 原始 bridge result 保留 `target.browserSessionId`。
- 完成 TODO 216 阶段 5 per-session tab 写命令队列：新增 `BrowserCommandQueueRegistry`，同一 `(browserSessionId, tabId)` 写命令在 Node 侧串行执行，`snapshot.queues` 暴露队列深度，合同覆盖同 tab 同 session 双 execute 不并发派发。
- 完成 TODO 216 阶段 4 tab lease/UI lock 基础能力：`browser_tabs` 增加 `leaseTab/releaseTab`，写类 tab command 自动短租约，跨 session 写冲突返回 `TAB_LEASE_CONFLICT`；`switch/pick/upload` 接入全局 UI lock，冲突返回 `UI_LOCK_CONFLICT`。
- 完成 TODO 216 阶段 3 browser session 生命周期：`browser_tabs` 增加 `listSessions/createSession/selectSession/closeSession/attachTab/detachTab`，新建 session 初始为空且不继承现有 tab，合同覆盖 attach/detach 后隐式目标变化。
- 完成 TODO 216 阶段 2 显式 browser session 路由：所有 tab-scoped browser 工具 schema 增加高级参数 `browserSessionId`，driver/server/wait/page-script/WebSecurity cookie provider 统一按 resolved browser session 路由；fake WS 合同覆盖相同 tabId 在不同 browser session 下互不覆盖默认选择。
- 完成 TODO 216 阶段 1 多 browser session 基建：新增 `BrowserSessionRegistry` 和内部 `default` browser session，将 `selectedClient`、`defaultSessionId`、`latestSessionId`、`selectionVersion` 从全局状态迁入 session state；旧 `browser_tabs`/隐式 tab fallback 行为和合同测试保持兼容。
- 完成 TODO 240 硬回退 `browser_orchestrate` 工具面：删除 callable tool、orchestration coordinator、target resolver、profile isolation、pre-navigation/session assertions/window-tabGroups orchestration 代码、文档、契约和 smoke 断言；README/generated docs/global skill 恢复 `browser_tabs list` + 显式 `tabId` 默认工作流。
- 完成 TODO 214 本地发布包验收与回滚演练：新增 `release:local` / `release:local:smoke` 与 `tests/release/release-local-acceptance.mjs`，在 clean cwd 执行 pack dry-run/实际 pack、解包校验 manifest dist 路径和 ESM build manifest，保存 current/previous/last-successful tarball 与 summary；isolated smoke 支持解包扩展源和自定义 artifact，回滚 smoke 覆盖最小 tabs/wait/execute，失败诊断输出 pack files/build manifest/Chrome profile/bridge port/smoke artifact。
- 完成 TODO 213 错误码与诊断 taxonomy 收口：`normalizeError`/`compactError` 在保留公开 `code/message/details/name` 的同时新增 `taxonomy` 与紧凑 `diagnostics`，按 schema category/retryable/summary 和 artifact/CDP/page heuristic 归类 driver/tool/native/page/CDP/network/transfer/security/artifact/protocol；`check:errors` 覆盖 nested bridge/native response、artifact reader、transfer、WebSecurity、stack strip 与 secret redaction，`docs:generate` 输出 Structured error taxonomy 表。
- 完成 TODO 212 可重放 runtime fixture 库：新增 `check:runtime-fixtures` 并纳入 `npm run check`/`quality:local`，使用本地 HTTP/WS fixture、fake Chrome/CDP API 与 recorded bridge envelopes 覆盖 network body/postData/HAR/WebSocket frames、hook session/listener、wait immediate selector、transfer download/upload、frame evaluate、screenshot fallback、callback worker state file 与 stale lock recovery；成功摘要写 `.pi/browser-artifacts/fixtures/runtime-fixtures-summary.json`，失败诊断写 `.pi/browser-artifacts/fixtures/runtime-fixtures-failure.json` 并脱敏。
- 完成 TODO 211 Artifact 与证据隐私治理：新增 `src/utils/redaction.ts` 与 `src/tools/artifactPrivacy.ts`，统一本地 artifact 分类、默认 `.pi/browser-artifacts/`、`localOnly:true`、手动清理提示和 cookie/token/authorization/body/postData/websocket payload 脱敏；result summary/details/error 与 `browser_artifact` 默认脱敏，敏感 full 输出改为 `saved_to_artifact`，`browser_artifact redact:false` 才显式读取本地原始证据；`check:token`/`check:artifact` 增加隐私 fixtures。
- 完成 TODO 210 Web Security 子域边界加固：新增 `webSecurity/shared/diagnostics.ts` 统一 domain=`webSecurity` 失败 envelope、敏感字段脱敏与 stack suppress；`executeWebSecurityToolShell` 统一包装工具失败；`check:web-security` 增加 register/browserNative/bridges/shared 分层契约，锁定 cookie provider 单点、`webSecurityCore` re-export 边界、基础工具不反向导入实现层，以及 sqlmap/nuclei bounded process 与 request/stdout/stderr artifact 边界。
- 完成 TODO 209 协议单源化终态设计与实现：`bridge/native_command_schema.json` 作为 native command 单源，`sync-native-protocol.mjs --check` 生成/校验 bridge schema 副本、MV3 runtime protocol、Node validator、wait/network/transfer metadata、错误码表与 `docs/generated/native-protocol.generated.md`；`check:protocol` 锁定 drift、schema 一致、生成文件头、wait/network/transfer 首批消费和 60 个结构化错误码覆盖。
- 完成 TODO 208 统一 Tool Adapter 与结果处理中间层：新增 `src/tools/toolAdapter.ts`，统一共享参数、timeout/maxChars、错误包装、result middleware 与 artifact fallback；基础工具和 WebSecurity shared shell 迁移到 adapter，生成器识别 adapter/helper 后继续保持 callable schema、summary/saved envelope 与 artifact 文档不漂移。
- 完成 TODO 207 `BrowserBridgeServer` 职责拆分：HTTP/WS、client registry、tab/session router、pending request、timeout diagnostics 分拆为 driver 子模块，`BrowserBridgeServer.ts` 保持 thin facade；fake WS/lifecycle/protocol contracts 继续锁定多浏览器 tabId、selectBrowser、断连、pending、MV3 restart 与错误 details。
- 完成 TODO 206 本地依赖与安全审计策略：新增 `check:deps` 并纳入 `npm run check`，校验 lockfile 一致性、生产依赖 allowlist、`npm ls` 与生产 `npm audit --audit-level=high`；摘要写入 `.pi/browser-artifacts/dependency-audit-summary.json`，registry 不可用时记录 unavailable 不阻塞离线开发。
- 完成 TODO 205 协议与工具文档本地生成：新增 `docs:generate` 与 `scripts/generate-tool-docs.mjs`，从实际工具注册元数据、`bridge/native_command_schema.json` 和结构化错误码生成 `docs/generated/browser-tool-contract.generated.md`；`check:tool-docs` 增加生成产物 drift check。
- 完成 TODO 204 本地并发、生命周期与泄漏验证：新增 `check:lifecycle` fake WebSocket fixture 并纳入 `npm run check`，覆盖多浏览器同数值 `tabId`、`selectBrowser`、WS 断连/重连、MV3 service worker restart、pending cleanup、tab command queue、wait/network/hook 长任务；失败 artifact 写入 `.pi/browser-artifacts/lifecycle-fixture-failure.json` 并按字段名脱敏。
- 完成 TODO 203 本地质量门禁脚本：新增 `npm run quality:local`，串联 `npm run build:bridge`、`npm run check`、`npm pack --dry-run --json`，仅打印可选 `npm run smoke:browser:isolated` 下一步，不默认启动 Chrome/Edge；README/AI_INSTALL/TODO 同步失败 artifact 与端口占用处理。
- 完成 TODO 202 支柱二终态收口 gate：manifest 指向 dist module service worker，`build-manifest.json` 记录 `serviceWorkerBuildMode:"esm-import-graph"`、`orderedConcatenation:false`、`legacyServiceWorkerModules:[]`，bridge/page `@ts-nocheck` 清零且 strict/noImplicitAny 生效；`npm run build:bridge`、`npm run check`、`npm pack --dry-run --json` 与 Edge isolated smoke 已通过，artifact 为 `.pi/browser-artifacts/smoke-browser-isolated-results.json`。
- 完成 TODO 200 Bridge TypeScript strict 与 page script 类型收口：`tsconfig.bridge-src.json` 开启 `strict:true` / `noImplicitAny:true`，`bridge_src/service_worker/**/*.ts` 与 `bridge_src/page_scripts/*.ts` 清零 `@ts-nocheck`，page hook/content/disable-dialogs 全部类型化；`check-bridge-files.mjs` 增加 type hygiene contract，VM contracts 改为 TS-aware，`npm run check` 已通过。
- 完成 TODO 199 startup 层真实 ESM 迁移：`router/tab_sync/transport` 改为显式 import/export 与显式 install 函数，`bridge_src/service-worker.ts` 成为唯一 service worker 启动入口；`scripts/build-bridge.mjs` 改为直接从 ESM entryPoints 构建，不再读取源码拼接 `service-worker.generated`；`build-manifest.json` 记录 `serviceWorkerBuildMode:"esm-import-graph"`、`orderedConcatenation:false`、`startupImported:true`、`legacyServiceWorkerModules:[]`。
- 完成 TODO 198 command 层 ESM 迁移：`network_model/network/hook/evidence/frame/html/screenshot/transfer/bridge_info/core_commands/exec` 已从 ordered legacy tail 移入 `serviceWorkerCommandModules`，`build-manifest.json` 新增 `commandImported:true`，runtime 直接 import command handler，`network_model` 通过 `setNetworkWaitNotifier` 解开对 `network.ts` 的反向依赖，command 层 `@ts-nocheck` 清零，batch/router dispatch 通过无 listener 副作用 helper 收口。
- 完成 TODO 196 支柱二终态设计修正：`docs/bridge-esm-bundler-plan.md` 明确 TODO 188-193 只是一期 `ordered-concat-compat` dist runtime 迁移，TODO 196-202 才是 `esm-import-graph` 终态；`dist/build-manifest.json` 记录当前与目标 service worker build mode，契约可区分 ordered source concatenation 是否仍存在。
- 完成 TODO 195 发布包 dist runtime 完整性修复：`prepack` quiet build、`package.json.files` 与 generated `dist/.npmignore` 共同保证 `npm pack --dry-run --json` 包含 manifest 指向的 service worker/content/hook/disable-dialogs dist runtime、source maps 与 build manifest；新增 `check:package` 并接入 `npm run check`，防止干净安装只得到 `dist/.gitignore`。
- 修正 TODO 193 口径为支柱二一期收口 gate：ESM TS source、dist build、manifest dist runtime、页面独立 bundle、旧 importScripts 删除和 ambient globals 删除均由 contracts 锁定；`npm run check` 与 `npm run smoke:browser:isolated` 通过，runtime artifact 为 `.pi/browser-artifacts/smoke-browser-isolated-results.json`。真实 ESM 依赖图、strict TS 与 `@ts-nocheck` 清理继续归入 TODO 196-202。
- 完成 TODO 194 独占 Chrome profile smoke：新增 `smoke:browser:isolated`，自动复制临时扩展目录、patch dist service worker bridge 端口、启动独立 Chrome `--user-data-dir` / `--load-extension`，运行后清理临时 profile/extension，并将结果写入 `.pi/browser-artifacts/smoke-browser-isolated-results.json`；未引入 puppeteer/playwright。
- 完成 TODO 192 旧入口删除：移除 `background.js`、旧 service worker/page 多文件 `.js`、`bridge-globals.d.ts` 与 `tsconfig.bridge.json`；生产 manifest 只指向 generated dist runtime，contracts 改读 `bridge_src` 源模块和 dist bundle，禁止旧 importScripts 入口作为备用路径。
- 完成 TODO 191 manifest/check/smoke 切到构建产物：`manifest.json` 现在使用 `dist/service-worker.js` module service worker、`dist/content.js`、`dist/disable_dialogs.js`，hook 注入常量切到 `dist/hook_dispatcher.js`；`check:bridge` 先 build 再做文件/manifest 契约；`smoke:browser` artifact 增加 dist runtime metadata 与 service worker sha256，并保留端口占用诊断。
- 完成 TODO 190 页面注入脚本独立 bundle 迁移：新增 `bridge_src/page_scripts/`，`build:bridge` 现在生成独立 `dist/content.js`、`dist/hook_dispatcher.js`、`dist/disable_dialogs.js`，并用契约锁定 page bundle 自包含、hook dispatcher 不含 background-only API、service worker 不内联 MAIN-world dispatcher。
- 完成 TODO 189 service worker ESM 兼容 bundle 迁移：`bridge_src/service_worker/` 按当前 service worker 文件顺序承载完整兼容源码，`bridge_src/service-worker.ts` 显式导入每个 source module boundary symbol 并导出 `serviceWorkerModuleGraph`，`build:bridge` 生成包含 runtime/CDP/wait/network/hook/frame/html/screenshot/transfer/router/tab_sync/transport 的 `dist/service-worker.js`；`check:bridge:types` 同时覆盖旧 bridge JS 与 `bridge_src` TS 源，`check-bridge-build` 动态 import dist fixture 验证 listener 注册。
- 修复 `browser_callback_oast` Windows 状态文件保存偶发失败：state JSON 原子替换遇到短暂 `EPERM/EBUSY/EACCES` 时主进程与 worker 现在执行有界 retry，避免 callback 事件写入 tmp 文件后未进入 `state.json` 导致 contract trigger 等待超时。
- 新增 Bridge build pipeline 骨架：`npm run build:bridge` 使用 esbuild 生成 `bridge/pi_browser_bridge/dist/`、source map 和 build manifest；`npm run check` 接入 dist/generated-file/manifest 契约。
- 冻结 Bridge ESM + TypeScript bundler 迁移设计：新增 `docs/bridge-esm-bundler-plan.md`，明确优先 esbuild、`bridge_src/` 源图、`bridge/pi_browser_bridge/dist/` 产物、service worker/content/hook-dispatcher/disable-dialogs 独立入口、manifest 切换和旧 `importScripts` 删除 gate；本项仅设计冻结，不切 runtime。
- 收紧 Bridge ambient/global 类型：`bridge-globals.d.ts` 从 broad `any` 黑盒改为具体 Chrome/runtime/debugger/downloads、persistent CDP、wait record、network recorder、hook command、WS envelope 类型；`router.js` 与 `runtime.js` 接入收窄后的 JSDoc，契约禁止 `declare const chrome:any` / `[key:string]:any` 回流。
- 增强 `npm run smoke:browser` 端口冲突诊断：Bridge 端口 `127.0.0.1:18765` 被占用时，smoke artifact 会写入 `bridge.port` 记录并区分 `agent_occupies`、`orphan_socket`、`unknown_owner`，尽量附带 PID/进程名/命令行/health 探测；文档明确脚本不会自动 kill 用户进程。
- 冻结 `hook_dispatcher.js` 页面注入边界：新增 `docs/hook-dispatcher-boundary.md`，明确 dispatcher 在 bundler 迁移前保持单文件自包含页面 bundle，`chrome.scripting.executeScript({ files })` 与 CDP fallback 共用 `PI_BROWSER_HOOK_DISPATCHER_FILE`；契约锁定无页面 import、无 `chrome.*`、公开全局 `window.__PI_BROWSER_HOOKS__`、session strict-match/seq/redact/listener cleanup 不变。
- 完成 Bridge `wait.js` 分阶段拆分收口：新增 `wait_navigation.js`、`wait_network_idle.js`、`wait_selector.js`，分别承载 navigation/loadState、networkIdle 和 selector 等待子系统；`wait.js` 收敛到约 299 行 facade/dispatch/diagnose glue，所有 wait 子文件行数均低于 450，并用契约锁定职责边界和加载顺序。
- 拆分 Bridge `wait.js` 的 WaitCoordinator/orphan/event subscription 子系统：新增 `bridge/pi_browser_bridge/wait_coordinator.js` 承载 wait registry、timeout/id、tab-scoped listener registry、orphan cleanup 和通用 wait cleanup；`wait.js` 收敛到约 850 行并继续作为 facade/dispatch，契约覆盖 listener scope、orphan cleanup、tab cleanup 和 cancelWaitsForTab。
- 拆分 Bridge `wait.js` 的 CDP refs/subscription 子系统：新增 `bridge/pi_browser_bridge/wait_cdp.js` 承载 CDP domain refcount、subscriber registry、tab cleanup 与 diagnostics；`background.js`、VM 契约和文件边界检查锁定 `runtime.js -> wait_cdp.js -> wait_coordinator.js -> wait.js` 顺序，保持 disable failure retry、tab cleanup 和 diagnostics 结构不变。
- 冻结 Bridge `wait.js` 拆分设计：新增 `docs/bridge-wait-split.md`，记录 wait 顶层状态/函数图谱、外部全局消费者、行为不变式，并让 bridge 契约测试改用可扩展的 wait bundle/helper，为后续 `wait_cdp.js`、`wait_coordinator.js`、`wait_navigation.js`、`wait_network_idle.js` 拆分做准备。
- 完成 WebSecurity/Bridge 重构收口 gate：确认 `registerWebSecurityTools.ts` 与 `summaries/webSecurity.ts` 保持 thin facade，Bridge Network split 有加载顺序、职责边界、文件体量契约和真实扩展 runtime 子集验证；本轮无用户可见 schema/summary/runtime 能力变化。
- 拆分 Bridge Network recorder 单体：新增 `bridge/pi_browser_bridge/network_model.js` 承载 recorder 状态、配置、过滤、record/body 存储与 summary helper，`network.js` 收敛为 CDP 事件、生命周期和命令分发；同步锁定 MV3 `importScripts` 加载顺序与 VM 契约组合。
- 强化 WebSecurity register shell 类型边界：外部 TypeBox 参数在各 register 模块内归一为具名 ToolParams，shared shell 以泛型约束 `augmentParams/run/details/distill`，cookie provider 显式使用 `CookieProvider`，并用契约禁止 `[key:string]:unknown` 和 loose `Record` run 参数回流。
- 重构 WebSecurity summary 层：`src/tools/summaries/webSecurity.ts` 现在仅作为 facade，recon/crawl/fuzz/sqli/bridge/template/OAST/cookie/replay 摘要拆入 `src/tools/summaries/webSecurity/`，共享脱敏、artifact 和表格裁剪 helper，并补 summary 契约覆盖关键证据字段。
- 重构 WebSecurity TS 注册层：`registerWebSecurityTools.ts` 现在仅作为 facade，12 个安全工具的 schema/注册逻辑拆入 `src/tools/webSecurity/register/`，共享 shell/cookieProvider/distillation 保持单点实现，并补注册 facade 契约。
- 修复 `browser_content` / `browser_scan` 大结果 artifact 通道：正文/扫描抽取现在走 direct CDP `Runtime.evaluate returnByValue`，`outputPath` 保存脚本声明预算内的完整 extraction/scan envelope；超过 20 万字符的 Markdown/content 不再先被 `exec.js` smart serializer 截成带 `…` 的伪结果。
- 修复 `browser_artifact` sample/JSON 缺失信号：`mode=sample` 现在去重小文件或重叠 head/middle/tail 区段，并在 summary 暴露 deduped sections；`mode=json jsonPath` 未命中时返回稳定 `{exists:false, notFound:true, value:null}`，`pick` 对每个请求路径返回 `{exists,value}` 或 notFound 占位，避免 `undefined` 序列化后静默丢 key。
- 修复 `browser_html` 采集预算与 summary 失真：顶层 `maxChars` 现在只作为返回预算，不再自动写入 bridge `html.get maxChars`；显式 `params.maxChars/max_bytes` 才限制采集。`detailLevel:"full"`/`outputPath` 会先抓原始 HTML/text 再由 resultMiddleware 保存 artifact；bridge 在截断前返回 links/buttons/inputs/forms/images/text/original bytes 元数据，summary 优先使用这些结构计数。
- 修复 `wait.diagnose` 与 evidence summary 观测缺口：frame 探测不再使用非标准 `document.frames`，改为 `document.querySelectorAll('iframe')` + `window.frames.length` 并返回 `frameCount/iframeCount/id/name/src/visible/rect`；`browser_evidence` summary 保留 `hook_status` 的 state/session/install/version/buffer 关键字段。
- 修复 `browser_frame` new-document script 生命周期：`frame.addNewDocumentScript` 现在合并顶层 `runImmediately/worldName/includeCommandLineAPI` 与 `options` 且顶层优先；CDP bridge 维护 tab/session scoped identifier registry，未知 `removeNewDocumentScript` 返回 `SCRIPT_NOT_FOUND`，正常 remove 与已知 `alreadyRemoved` 都返回稳定 `identifier/removed/alreadyRemoved/sessionKey/cdpSessionName/tabId/method`。
- 修复 performance/evidence timeout 契约：`hook.getPerformanceEntries` 显式 `entryType` 未命中时返回 `count:0` 且不再 fallback 全量 `performance.getEntries()`；`browser_evidence` 会把 `timeoutMs/timeout_ms` 透传给 hook status/collect 的页面 evaluate 与 performance 子源；`hook.status`/`hook.collect`/`hook.evaluate` 也会把命令 timeout 传入 `Runtime.evaluate`。
- 修复 `browser_pick focus:false` 后台 tab 超时语义：工具层现在用用户 `timeoutMs` 作为总 deadline，CDP `Runtime.evaluate` 使用额外 grace；deadline 到期返回结构化 timeout 结果并主动调用页面 cleanup handle 清理 overlay/listeners，避免后台页面 timer throttle 时暴露底层 CDP timeout。
- 修复 `browser_hook` event listener 生命周期：listener registry 改为 `tabId::listenerId` 作用域，同名 listener 可跨 tab 并存，`hook.removeEventListener` 只删除目标 tab 的 registry/page listener；`hook.uninstall` 与 `cleanupPiBrowserTab` 会尝试清理页面 `window.__piBrowserListeners` 中的真实 DOM listener，并返回或记录 cleanup 诊断。
- 修复 `browser_hook` session/collect/clear 契约：status/collect/clear_buffer/pause/resume/uninstall 现在转发并校验显式 `sessionId/session_id`，不匹配返回 `INVALID_SESSION`/`SESSION_NOT_FOUND`；collect 不再向事件 buffer 写入 lifecycle 自噪声；clear_buffer 不再重置全局 `seq`，增量游标在清空前后保持单调。
- 修复 `browser_download` mode 静默降级：工具层和 bridge 层现在都只允许 `click | media | url`；selector 目标拒绝 `mode:"url"`，URL 目标拒绝 `mode:"click"/"media"`，未知或冲突值返回 `INVALID_RULE` 且不会触发页面脚本或 Chrome downloads API。
- 增强 `browser_network` 原始证据保真：recorder 默认对 XHR/Fetch/Document 的 JSON/text/html 响应在 `loadingFinished` 后立即有界抓取 body，默认保留有界 request `postData`，并通过 `bodyAvailability/bodyUnavailableReason` 区分 captured、too_large、binary、expired、not_requested、cdp_failed 等缺失原因；`network.body` 缺 body 时返回结构化 `BODY_UNAVAILABLE` 诊断。
- 当前工具面：`browser_scan` + `browser_execute` + `browser_wait` 为 GA-style 主链路；下方 `browser_query` / `browser_click` / `browser_type` / `browser_dom_*` 记录均为历史阶段，已被“回归 GA 简化工具面”取代，不代表当前注册工具。
- 收敛 Web 实现方向：保留原生工具能力优先，`browser_sqli_probe` 与 `browser_template_check` 作为原生能力继续演进，同时明确深度 SQLi / 深度模板扫描将通过成熟引擎 bridge 深适配接入；不再把原生工具表述成 `sqlmap` / `nuclei` 等成熟引擎平替。
- 新增 `browser_sqlmap_bridge`：通过 Pi-native bridge 调用 `sqlmap`，统一复用 raw/captured/HAR 请求输入链路、浏览器态 cookie 绑定、显式 launcher / PATH/module auto-detect、结构化 findings 与 artifact 归档，并补契约与 runtime 验证路径。
- 新增 `browser_nuclei_bridge`：通过 Pi-native bridge 调用 `nuclei`，统一复用 scoped URL/raw/captured/HAR 请求输入链路、template/workflow/id/tag/severity 选择器、浏览器态 cookie 绑定、显式 launcher / PATH auto-detect、结构化 matches 与 artifact 归档，并补契约与 runtime 验证路径。
- 增强 bridge artifact 输出：`browser_sqlmap_bridge` / `browser_nuclei_bridge` 现在为 request/stdout/stderr 生成 `browser_artifact` 可读描述符，包含 path、bytes、chars、lineCount、sha256 与 summary artifact 表，便于深度排障直接读取日志。
- 修复 `browser_callback_oast` 状态竞态：主进程 clear/stop 与 Worker append/shutdown 共享 `state.json` 的写入改为 lock file + fresh-state update，避免高并发回调丢事件、重复 seq 或控制字段被旧快照覆盖。
- 修复 `browser_callback_oast` stale lock 回收 TOCTOU：state lock 增加 owner token、owner 校验释放和 breaker lock 串行化 stale 删除，避免 stale recovery 误删新锁后并发写 `state.json`。
- 修复 JSON 参数 fuzz 的破坏性重写：畸形 JSON 或 primitive JSON body 现在作为明确 mutation failure 记录，不再静默替换为 `{}` 并丢失原始 payload。
- 修复 raw HTTP 解析重复 header 覆盖：`parseRawHttpRequest` 现在按 header 名 case-insensitive 合并重复项，`Cookie` 用 `; `，通用 header 用 `, `，避免 replay 丢失会话或代理链信息。
- 修复 `browser_http_replay` HAR 依赖图相对 URL 崩溃：`buildHarDependencyGraph` 与 HAR redirect/referer 解析现在支持相对 request/referer/location 与 `baseUrl` 归一化，不再因 `new URL(relative)` 中断整次 replay。
- 修复 native bridge WebSocket 错误帧语义：`router.js` 的 native command 失败现在发送 `type:"error"`，不再把 `{ ok:false }` 错误对象伪装成 `type:"result"` 导致服务端 Promise 误 resolve。
- 修复 bridge batch 内 `tabs` 命令分发：`handleBatch()` 现在复用 `handleTabsCommand()`，保留 `list/create/switch/close` method 语义，不再把 tab 控制静默降级为 list。
- 修复 `browser_wait` / `wait.navigateAndWait` 总超时预算失真：导航阶段耗时现在会从后续 wait 租约预算中扣减；导航后预算耗尽会直接返回 `WAIT_TIMEOUT`，不再继续启动第二阶段 wait。
- 修复 `browser_wait` supervisor 迟到成功语义：短租约在全局 deadline 之后才返回成功时会被丢弃并转成 `WAIT_TIMEOUT` / `WAIT_STATE_LOST`，避免等待结果超过用户声明总超时。
- 修复 `browser_wait timeoutMs=0` 即时检查语义：tool 层与 TS wait supervisor 现在保留 0，向 bridge 发送一次 immediate probe，不再升级为默认长等待。
- 加固 bridge 端 JS 工程类型栅栏：新增 `tsconfig.bridge.json`、ambient bridge 类型和 `check:bridge:types`，对 MV3 importScripts bridge 代码执行 `allowJs/checkJs/noEmit`，并用 contract 锁定脚本接入与 JSDoc 入口。
- 加固 `browser_wait` 长等待 MV3 生命周期语义：Service Worker 侧暴露 `workerBootId/workerStartedAt`，TS 端对 navigation/loadState/selector/networkIdle/navigateAndWait/any/all 使用短租约 wait supervisor 续证；成功结果暴露 leases/workerRestarts/historyLost，不可证明时返回 `WAIT_STATE_LOST`。
- 修复 `browser_content` 带 URL 导航路径：现在复用 TS durable wait supervisor 执行 `wait.navigateAndWait`，并在结果 details 中保留 navigation supervisor 元数据。
- 修复 `browser_content` 抽取超时预算失真：内容抽取阶段现在直接使用用户声明的 `timeoutMs`，显式小于 100ms 的值返回 `INVALID_TIMEOUT`，不再静默扩容到默认 15s。
- 修复 `browser_execute monitor:true` 结果结构：monitor 模式现在保留普通执行结果的顶层 `data/target/newTabs/acknowledged` 等元数据，仅追加 `monitor` diff 证据。
- 加固 `browser_hook` 页面侧 redact pattern：`options.redact_patterns` 现在限制数量/长度并经过安全正则检查，嵌套量词等危险 pattern 降级为 literal 替换，避免 hook dispatcher ReDoS。
- 加固 `browser_artifact search regex:true`：本地 artifact regex search 现在限制 pattern 与单行匹配输入长度，拒绝嵌套量词/回溯引用/lookaround 等危险 regex，并在结果 summary 中暴露 regex 扫描预算。
- 修复 `browser_artifact` 流式读取契约：`text/search/sample` 不再被 JSON 读取大小上限阻断，UTF-8 `summary.chars` 改为真实字符数，空文件/越界 offset 不再返回非法行区间，长单行超预算仍返回可读前缀，search 命中末行时 `nextOffset:null`。
- 加固 `browser_template_check` 模板 regex：body/header/DSL matcher 与 extractRegex/extractor 统一使用有界安全正则，拒绝危险 regex，并对响应体 regex 匹配截断到固定预算且返回诊断。
- 加固 HAR URL pattern 过滤：`browser_http_replay` / `browser_sqlmap_bridge` / `browser_nuclei_bridge` 的 `harUrlPattern` 现在使用 bounded safe-regex 或 substring fallback，并限制候选 entry 与 URL 匹配文本长度，避免大 HAR + 恶意 regex 阻塞。
- 明确高自动化 Web 工具判断增强边界：`browser_crawl` 暴露 `activeGraphqlIntrospection` passive-only 开关并记录默认主动 GraphQL introspection 行为；`browser_template_check` 明确省略模板选择器时运行小型内置 exposure/API baseline；契约检查 scanner/bridge 类工具必须保留 bounded/scoped/artifact/evidence 文案。
- 修复 native bridge WebSocket malformed input 处理：`router.js` 对 `id` / `code` 使用 nullish 检查，空字符串脚本不再被静默丢弃；对象 code 缺失有效 `cmd` 或传入不支持 code 类型时直接返回明确错误帧。
- 修复 native bridge JSON-looking 脚本误分发：WebSocket string `code` 只有在解析为含 `cmd` 的 JSON object 时才进入 command 模式，`[]`、`{"foo":1}` 等脚本字符串保留 JavaScript 执行路径。
- 修复 native bridge cookie 合并维度：`cookies` command 合并 partitioned cookies 时按 name/domain/path/storeId/partitionKey 去重，不再折叠同名不同 path 或 partition 的合法 cookie。
- 修复 native bridge cookie URL 错误边界：`cookies` command 现在先校验 URL；`about:` / `file:` 等非 HTTP(S) URL 返回空 cookie 结果，畸形 URL 返回 `INVALID_RULE`，不再泄漏 `url.match(...)[0]` 空指针异常。
- 修复 `wait.all` 空条件语义：`wait.all` 现在与 `wait.any` 一样要求非空 `waits/conditions`，空复合等待返回 `INVALID_RULE`，不再把 `matched:0` 当成功。
- 加固浏览器扩展 WebSocket transport：onclose/onerror/keepalive 清理改为 identity-guarded，onopen/onmessage 捕获当前 socket，避免旧 socket 晚到事件误清新连接或异步 ready 发错连接。
- 修复浏览器扩展 tab sync 未处理 Promise rejection：tab lifecycle listener 中的 tab 更新发送与 WS 探测现在统一捕获并记录异步错误，避免 MV3 service worker 因 `tabs.query` / `ws.send` 失败出现未处理 rejection。
- 修复 `stableJson` 循环引用崩溃：JSON 输出路径现在把真实祖先循环标记为 `[Circular]`，同时保留非循环共享引用、BigInt 与 Error 序列化行为。
- 修复 `BrowserBridgeServer.closeTab` 会话引用残留：关闭最后一个活动 tab 时同步清理 `latestSessionId`，避免后续无显式 `tabId` 的命令 fallback 到已关闭 tab。
- 修复 `BrowserBridgeServer` 断连会话引用残留：WebSocket client 断开或 `tabs_update` 标记 tab 缺失后同步清理 stale `defaultSessionId` / `latestSessionId`，避免省略 `tabId` 的命令落到断开的旧 tab。
- 修复 `BrowserBridgeServer` 断开 session 历史无界增长：已断开 tab session 现在按 5 分钟 TTL 与最多 128 条 disconnected history 清理，保留短期诊断同时避免长期 tab churn 造成 `sessions` Map 泄漏。
- 补强 tab removed 生命周期契约：`chrome.tabs.onRemoved` 必须走 `cleanupPiBrowserTab`，并释放 tab-scoped waits、event subscriptions、CDP refs 与 network recorders，防止 Service Worker 侧资源清理链路回归。
- 修复 CDP domain disable 失败后引用丢失：wait/network CDP ref 现在只在 `*.disable` 成功后删除；disable 失败会保留 `disablePending` 与 `lastError`，后续 acquire/release 可复用并重试释放；tab removed / tab-gone 场景不保留无意义 stale ref。
- 修复 `BrowserBridgeServer.switchTab` 失败结果污染默认 tab：兼容/异常客户端若返回 `type:"result"` + `{ ok:false }`，现在会抛 `BROWSER_COMMAND_FAILED` 且不更新 `defaultSessionId`。
- 加固 `BrowserBridgeServer` 默认 tab 并发可观测性：selected/latest tab 变更维护 `selectionVersion`，隐式 tab fallback 的执行结果/错误暴露 `target.source`、dispatch/resolve 版本，`switchTab` 返回 selected tab 证据。
- 修复 `BrowserBridgeServer.selectBrowser` 空浏览器选择语义：选择到无活动 tab 的浏览器会清空 default/latest 并返回 `NO_TAB`，tab-scoped 隐式命令不再回落到其它浏览器。
- 修复 `BrowserBridgeServer.selectBrowser` 隐式目标选择：切换浏览器时优先选择该 client 上报的 `active:true` tab；无 active 标记时才回退到有效 latest/首个 live tab，避免命令落到后台 tab。
- 修复 `BrowserBridgeServer` 多浏览器 tabId 碰撞：tab session 改为 browser-scoped 复合 id，`browser_tabs list` 暴露 `browserId`，显式同号 tabId 按当前 selected browser 路由，避免后上报 client 覆盖前一个 session。
- 修复 `BrowserBridgeServer` stale tab 回退选择：default/latest 指向断开 session 时，刷新引用优先选择同 browser 的 `active:true` tab，再使用有效 latest，避免回退到较早后台 tab。
- 修复 `BrowserBridgeServer` 显式 stale tabId 回退：指定 `tabId` 找不到 live session 时本地返回 `TAB_NOT_FOUND`，不再把请求发到任意打开的 extension client 后超时或误路由。
- 修复 `wait.navigation timeoutMs=0` 假阳性：即时探测现在读取当前 URL 与 load state，只有声明条件已满足才成功，否则返回 `TIMEOUT`，不再无条件 ok。
- 修复 `wait.any/all` 子条件命令名归一化：组合等待现在接受完整 native wait command 名（如 `wait.loadState` / `wait.selector` / `wait.navigation`），不再把带点号的标准 `cmd` 判为 unknown。
- 修复 `browser_wait` supervisor 瞬时 lease timeout 重试风暴：底层立即返回 timeout 时现在按最小退避节流续租，并在 lease 诊断中记录 `retryDelayMs`。
- 修复 `browser_screenshot` visible-tab fallback 格式漂移：fallback 实际只捕获 png/jpeg，返回 metadata 与默认 artifact 扩展名现在使用真实捕获格式和 mime，而不是请求的 unsupported format。
- 修复 `browser_download` 结果 envelope 丢失：工具层现在保留完整 `BrowserBridgeExecutionResult` 顶层元数据（id/tabId/acknowledged/target/newTabs），仅在 summary 中解包下载 payload。
- 加固 `BrowserBridgeServer` WebSocket origin 边界：upgrade 阶段拒绝普通网页 `http(s)://...` origin，保留 Chrome extension origin、`Origin:null` 与无 origin 本地客户端连接。
- 修复 `browser_tabs` 缺失 tabId 的空字符串 fallback：`switch` / `close` 现在在工具层显式要求有效 `tabId`，缺失或非法时返回 `TAB_ID_REQUIRED`，不再启动 bridge 后向 server 传空字符串。
- 加固 `browser_tabs create` URL 边界：create 现在要求绝对 URL 或默认 `about:blank`，并显式拒绝 `javascript:`；direct bridge `tabs.create` 同步返回明确 `INVALID_RULE`。
- 修复 `tabs.create` 默认 `about:blank` 后不可见：bridge tab tracking/list/sync 现在保留 `about:blank` 新建 tab，同时继续过滤 `chrome://` 等内部页。
- 加固工具错误 stack 抑制：bridge/tabs/transfer 结构化错误不再使用严格模式下可能抛错的 `delete error.stack`，改用共享 `suppressErrorStack`，避免 stack 清理掩盖原始错误。
- 加固 `buildScanScript` 脚本字面量边界：scan 脚本内嵌 JSON 统一转义 HTML-sensitive 字符，并对非有限 `maxChars` / `maxNodes` 回退默认值，避免生成 `null` 预算。
- 加固 Network recorder pattern 匹配：用户提供的 URL/MIME/body/ws/sse pattern 现在有长度和安全正则检查，嵌套量词等高风险 pattern 不再直接编译执行，避免 Service Worker ReDoS。
- 加固 `wait.networkIdle` URL pattern 匹配：`includeUrls` / `ignoreUrls` 复用共享有界 matcher，嵌套量词等高风险 pattern 不再直接编译执行。
- 加固 `browser_pick` 页面卸载路径：picker 现在监听 `pagehide` / `beforeunload`，标签页导航或关闭前会以取消结果结算并清理 overlay/listeners，而不是只等待交互超时。
- 修复 `browser_screenshot` 缺失目标 tab 的错误语义：截图前先确认 tab 仍存在；Chrome 返回 missing-target 时直接返回 `TAB_NOT_FOUND`，不再把不存在的 tab 归类为截图超时。
- 加固 screenshot data URL artifact 写入：`saveDataUrl` 现在严格校验 base64 payload，非法字符或 padding 不再被 `Buffer.from(..., "base64")` 容忍后写成损坏文件。
- 加固 `browser_screenshot` debugger cleanup 观测：`chrome.debugger.detach` 失败不再静默吞掉，成功截图结果会携带 `cleanup.debuggerDetach` 警告并写扩展日志。
- 修复正整数参数归一化向下截断：`asPositiveInt` 现在对正浮点数向上取整，避免 timeout/maxChars 等用户预算被静默缩短，并保证 `0.1` 归一化后仍为正整数。
- 调整观测型工具默认 timeout：新增 `DEFAULT_OBSERVATION_TIMEOUT_MS=35_000`，用于 `browser_evidence` 与 `browser_network list/body/exportHar/wait`，避免复杂页面证据聚合或大缓冲读取过早超时。
- 修复 `browser_network` 录制器契约：显式 `sessionId` 不再回退 default，`network.list` 末页返回 `nextOffset:null`，`network.get` 缺失请求返回 `REQUEST_NOT_FOUND`，二进制 body 按原始字节截断后重编码为合法 base64，`exportHar(format:"json", includeBody:true)` 的 bodies 只包含过滤命中的记录，network summary 保留 recorder tab/session 与 body url/status/mimeType。
- 收敛结果契约层：`detailLevel:"preview"` 与 summary 统一返回 compact envelope；envelope 超预算时自动保存 raw/artifactValue 并返回 `saved.path`；`browser_content` / `browser_scan` / `browser_html` / native action / evidence 的 outputPath artifact 保存完整可复原 envelope；`browser_scan` 不再私自增加返回预算，长文本节点裁剪会标记 `truncated`。
- 统一 selector/empty 语义：`browser_content` 与 `browser_html` selector 未命中返回 `SELECTOR_NOT_FOUND`，无效 selector 返回 `INVALID_SELECTOR`；`browser_content` 命中空节点时返回结构化 `empty:true`，不再抛通用抽取失败。
- 收敛 hook/wait/frame 作用域契约：hook status/collect/list_sessions 等只读命令不再隐式 auto reinstall，显式 tab 只返回目标 scope；wait.diagnose 过滤目标 tab listeners/CDP/debugger 数据；frame.list/evaluate 返回稳定 tabId/frameId 元数据。
- 收敛剩余 summary/detail 字段：generic bridge summary 上浮 `tabId/frameId/count/total/nextOffset/sessionId` 与 target source，evidence hook 聚合沿用只读 no-reinstall 语义，smoke fixture 覆盖 content empty、selector miss、frame list 与 hook no-install 读路径。
- 修复 `browser_network action=wait timeoutMs=0` 工具层透传：network wait 现在与 bridge 即时检查语义一致，不再把 0 扩成默认长等待。
- 修复 `disable_dialogs.js` 的 `prompt` 抑制返回语义：自动确认时保留空字符串 default，并按原生返回类型把 falsy default 转为字符串，不再用 `def || null` 混淆确认空输入与取消。
- 修复 WebSocket 断连 pending Promise 挂起：`BrowserBridgeServer` 现在记录 pending 请求所属 client，并在 client unregister 时立即 reject/清理对应 pending 请求。
- 修复 multipart 解析数据损坏：`parseMultipartBody` 改为识别行首 boundary delimiter，不再用全局 split 误切 payload 内 boundary 文本，也不再裁剪真实 payload 尾部 `--`；`parseRawHttpRequest` 同步保留 body 原始 CRLF，避免 multipart delimiter 被 header 解析步骤改写。
- 修复空 favicon/空响应相似度哈希碰撞：`simHash64` 现在对空 Buffer 返回固定零值，不再落成全 1 的 `ffffffffffffffff`。
- 修复 DNS OAST 16-bit 问题类型序列化：DNS 响应问答区改用 `writeUInt16BE` 写入 QTYPE/QCLASS，避免 CAA/URI 等高位字节被截断。
- 修复 `browser_callback_oast` DNS trigger 超长名称兼容性：`buildDnsQuery` 现在显式校验单 label <= 63 bytes、整条 QNAME 线长 <= 255 bytes，避免超长 label 长度字节截断后生成畸形 DNS 报文。
- 修复 `browser_callback_oast` DNS A 响应地址校验缺口：`dnsResponseAddress` 现在要求合法 IPv4；当前 A-record 响应不再接受 IPv6/非 IPv4 输入并静默构造错误 `0.0.0.0` 类结果。
- 修复 OAST worker 日志句柄泄露：派生 detached worker 后父进程立即关闭继承前打开的 stdout/stderr log 文件描述符。
- 优化 Rails Cookie 验证：Rails PBKDF2 派生切到异步缓存路径，避免 `browser_cookie_analyze` 在大量 secret candidates 下同步卡死事件循环。
- 增强 `browser_sqli_probe`：新增 `stopOnFirstMatch`，可在同一参数确认命中后短路后续 probe，减少无意义发包。
- 增强 `browser_crawl`：补 source map 反向源文件归档与 service worker cache 版本摘要，结果增加归档路径/计数与版本聚合字段，并补契约回归。
- 固化 Web 安全单包分层：原生执行模块归入 `src/tools/webSecurity/browserNative`，成熟引擎适配固定在 `src/tools/webSecurity/bridges`，共享解析保持在 `src/tools/webSecurity/shared`，并补边界契约防回流。
- 增强 `browser_http_replay`：补 `multipart.fileFieldMatrix`，支持基于单模板文件的 focused multipart 文件字段变体矩阵、重复同名文件部件、嵌套 multipart case、基线 diff 与结果聚类，并补契约回归。
- 增强 `browser_cookie_analyze`：补 JWE compact 解析与 `dir` + `A128/A192/A256GCM` 直接解密、PASETO 元数据/footers、Django/Flask/Rails 签名 session 格式、现代 Rails AES-GCM encrypted cookie、legacy Rails AES-CBC signed wrapper、Marshal/binary 明文证据、Rails direct-key signed cookie 的原生解密/验证/metadata 暴露/mutation 重加密、跨格式 claim mutation token 生成，以及基于浏览器 session cookie 的 bounded claim replay 验证。
- 增强 `browser_callback_oast`：补 detached worker 持续化、reload 后状态/事件恢复、HTTP/HTTPS/DNS 多 listener、self-signed HTTPS 元数据、external metadata、trigger helper 与持久化事件收集。
- 增强 `browser_recon_probe`：补结构化 tech hints / fingerprint 字段，以及 favicon mmh3 与 simHash 结果字段。
- 增强 `browser_fuzz_paths`：补递归目录 fuzz、最大目录深度控制、baseline cluster 过滤策略与基线聚类摘要字段。
- 增强 `browser_fuzz_vhosts`：补 HTTPS fixture 支撑、SNI 证书差异摘要、baselineStrategy 与 baseline cluster 过滤结果字段。
- 替换 template YAML 手写 parser：改为依赖 `js-yaml`，保留现有 YAML template contract 与 artifact 行为。
- 优化工具结果 token 路径：默认 summary 蒸馏、artifact 可追溯、`details` 紧凑化。
- 新增 artifact 局部读取内部约束：相对路径限定 `.pi/browser-artifacts/`，其他文件需绝对路径。
- 拆分 summary 逻辑并新增 `check:summaries` / `check:artifact` 契约验证。
- 收敛错误归一化与诊断契约，新增 `check:errors`。
- 完成真实浏览器 smoke：tabs/create/loadState/scan artifact/network/screenshot/close 通过；截图写入 `.pi/browser-artifacts/smoke-screenshot.png`。
- 收敛性能维护项：artifact 文本读取流式化、network 摘要增强、scan 噪声规则外置、预算表驱动、check:bridge 拆分。
- 去重 Web 工具注册执行壳：`registerWebSecurityTools.ts` 统一 budget、lazy `ensureStarted` cookieProvider、artifact/distill 与 errorResult；保留每个工具显式 schema、summary 与 command，并补回归契约防止重复外壳回流。
- 补齐原始数据蒸馏覆盖：execute/wait/hook/frame/html fallback 统一经过 `resultMiddleware`，默认保留 preview 并支持 artifact。
- 新增 `browser_pick` 与 `browser_content`：参考 `badlogic/pi-skills/browser-tools` 的交互点选和正文提取能力，复用现有 bridge/CDP/蒸馏/artifact 体系。
- 借鉴 Curio 补齐 `browser_query` / `browser_click` / `browser_type` 一等 DOM 工具，并增强 `browser_execute` 对 Map/Set/Error/BigInt/DOM/循环引用的结果序列化。
- 性能质量收敛：smoke 覆盖新工具链、query visibleOnly 单次摘要、exec 序列化预算、统一 page-script 契约、结构化元素错误、artifact 原子写入、工具文档漂移检查。
- 文档职责重分层：`AI_INSTALL.md` 负责安装/环境 SOP，README 作为项目入口，Pi skill 只面向正式浏览器使用场景。
- 借鉴 Codex Chrome 补齐 `browser_download` / `browser_upload`：下载回传 Chrome 本地路径，上传经文件选择器和显式确认。
- 补齐上传/下载全量测试：纯参数校验、bridge runtime 模拟、summary、错误映射、cleanup 与显式 transfer smoke。
- 修复 media 下载真实浏览器超时：先提取媒体 URL，再用 Chrome downloads API 下载并回传稳定路径。
- 收敛测试/正式文件边界：契约测试迁到 `tests/contracts/`，smoke 迁到 `tests/smoke/`，`scripts/` 仅保留生成器/loader，并新增 `check:boundaries`。
- 统一过滤翻译类插件噪声：Read Frog、Google Translate、Immersive Translate 等 wrapper/overlay/属性不再污染 scan/content/html summary/元素摘要。
- 完成重载后的最终全量真实浏览器回归，覆盖所有浏览器工具、上传下载和翻译噪声过滤；结果写入 `.pi/browser-artifacts/final-full-browser-smoke-results.json`。
- 修复审查发现：`browser_execute` 禁止绕过 `browser_upload` 校验，遗漏契约纳入 `npm run check`，click 下载优先 tab-scoped 关联，`browser_pick`/`wait.selector` 补齐翻译噪声过滤。
- 重载 smoke 后继续收敛：工具错误结果默认不再返回 stack，`browser_pick` 命中翻译 wrapper 时归一到最近真实父元素。
- 最终 smoke 发现并修复 `browser_execute` 生成脚本中 `/\s+/` 被模板字符串误转成 `/s+/` 的问题，保留返回字符串的 `s` 与换行。
- 最终全量真实浏览器 smoke 通过：tabs/wait/scan/content/html/artifact/execute/query/type/click/pick/network/hook/evidence/frame/screenshot/download/upload；结果写入 `.pi/browser-artifacts/final-smoke/results.json`。
- 修复复审发现：`browser_content` 导航返回 `ok:false` 时立即失败并清理嵌套 stack，Pi extension 启动失败后清空 `startPromise` 以允许重试。
- 新增 Visible / Semantic DOM 节点层：`browser_dom_snapshot` 返回当前视口可见语义节点与短期 `nodeId`，`browser_dom_click` / `browser_dom_type` 基于 DOM selector/framePath 执行动作，不使用屏幕坐标；动作摘要保留 framePath/root-relative bbox，拒绝跨 tab 复用 nodeId，DOM 失效错误会解包为 `DOM_NODE_*` 且默认剥离 nested stack。
- 修复 semantic DOM 复审问题：`browser_dom_type` 使用 native value setter + `InputEvent` 兼容受控输入，`browser_dom_click` 重算 clickability 并对纯语义节点返回 `DOM_NODE_NOT_CLICKABLE`。
- 完成 semantic DOM 修复后的完整真实浏览器回归：tabs/wait/scan/content/html/artifact/execute/query/type/click/dom/pick/network/hook/evidence/frame/screenshot/download/upload 全通过，结果写入 `.pi/browser-artifacts/final-smoke/results.json`。
- 按 skill-creator 要求收口文档：全局 skill 保持 runtime-only 触发契约，`AI_INSTALL.md` 聚焦安装/reload/check/smoke/诊断，README 保持项目入口与维护指针。
- 修复 `browser_dom_snapshot` iframe 可见性：同源 iframe 内节点按祖先 iframe 可视区域裁剪，不再返回 iframe 当前不可见的子节点。
- 修复 wait/hook 复审问题：`wait.navigation` 注册 webNavigation/tabs/CDP 完成/失败监听，避免空 URL 误匹配 targetUrl，并加入 current URL / readyState 轮询兜底；`hook.getPerformanceEntries` 返回解包后的 `entries`，`hook.add/removeEventListener` 实际保存并移除页面 handler。
- 修复工具参数与契约漂移：native/evidence/transfer/screenshot 将 `timeoutMs` 写入 bridge command，`browser_scan` textOnly 遵守 `maxNodes` 且截断时保留已收集文本，`browser_html` 支持 `fragment` / `raw` mode 别名。
- 修复 `exec.js` 序列化对 scan/content 大文本字段的 1000 字符嵌套截断：`content` / `markdown` / `html` 使用全局字符预算，确保 artifact 能保存完整抽取结果。
- 修复 CDP/download/port 配置复审问题：`frame.evaluate` 使用正确 `grantUniversalAccess`，click 下载不再用全局下载 fallback 误配其它 tab，bridge 默认 host/port 改为生成配置并加入漂移检查。
- 修复 tabId 与 CDP alias 复审问题：server 顶层 `tabId` 与 command payload 冲突时报 `TAB_ID_CONFLICT`，CDP `releaseIdle` 按 Map entry 释放 logical alias，补 fake WS / Chrome mock / exec 行为契约。
- 修复 `persistent_cdp.releaseIdle` 默认值处理：`maxIdleMs:0` 现在表示立即释放；补充 scan/content/pick/action 的 DOM mock 行为契约，降低 substring-only 测试脆弱性。
- 优化默认 summary token 消耗：数组摘要改为 `columns` + `rows` 表格格式，加入确定性摘要预算裁剪；`preview/full` 与 artifact 保留原始数据。
- 优化 semantic DOM 摘要：默认只回 nodeId/role/label/state 等操作字段，selector/path/bbox 留给 `preview/full`；新增 `browser_dom_snapshot.diffSince` 增量快照，默认不传时保持完整快照行为。
- 修复复审发现的 tab/diff 正确性问题：非法显式 `tabId` 不再 fallback 默认 tab，semantic DOM diff 使用稳定结构键识别 changed，`diffSince` 禁止跨 tab snapshot。
- 补齐 `executeJavaScript` 路径显式非法 `tabId` 防护：`browser_execute`/scan/content/query/dom snapshot 等脚本执行路径不会把非法目标 fallback 到默认 tab。
- GA-style 浏览器操作内核升级（不新增工具）：`browser_dom_snapshot` 优先保留交互/图标/命中目标节点，`browser_click`/`browser_dom_click` 内部改为 CDP 真实鼠标点击优先并保留 DOM fallback，`browser_query/click/type` 支持 `text=`/`role=`/`label=`/`placeholder=` smart selector 前缀。
- 对齐 GenericAgent `simphtml.py` 的可操作观察层：`browser_scan`/`browser_dom_snapshot` 识别 top-layer/modal 并压制 covered 背景内容，限制 icon 语义向巨大祖先冒泡，`browser_query` 摘要暴露 hit-target 状态。
- 回归 GA 简化工具面：移除 `browser_query` / `browser_click` / `browser_type` 与 semantic DOM nodeId 工具注册和契约，默认心智改回 `browser_scan` 观察 + `browser_execute` 执行 JS/CDP + `browser_wait` 验证。
- 修复 `wait.selector visible` 在复杂布局中被 IntersectionObserver 误判不可见的问题：可见性以 CSS + viewport rect 为准，IO 与 hit-target 作为诊断。
- GA 简化后补强 `browser_scan` 主观察层：默认返回 actionables 候选表（selector/label/role/rect/hitOk），并将 smoke 改为 `browser_execute` 完成 input/contenteditable/click 状态链路。
- 提升真实站点 actionables 质量：识别框架 delegated handlers、`data-e2e`、卡片/视频点击目标，summary 暴露 action 与 CDP-ready 中心点；收紧 top-layer，避免普通播放器被误当 modal root。
- 抖音 Draft.js 评论链路回归：`browser_execute` + CDP 鼠标打开 `#comment-input-container`，`Input.insertText` 可更新 editor 状态；未提交评论并清空草稿。补充 id/class comment input container 的 actionables 契约。
- 收敛 GA 回归后的维护债：actionables 规则抽到 `src/scan/actionableRules.ts`，补 scroll container / Draft-like editor / data-e2e / delegated card / 普通播放器非 modal 契约，真实浏览器 smoke 增加 CDP mouse / insertText / key clear 闭环。
- 完整对齐 GenericAgent 浏览器内部体验：`browser_execute monitor:true` 可选返回执行前后 scan diff；`browser_scan` 增加 `list_hints` 重复列表提示与 `:-webkit-autofill` 保护态标注。对照记录见 `docs/ga-browser-parity.md`。
- 新增 CTF Web 下一阶段基础执行面：`browser_recon_probe` 提供小范围 URL/路径/端口/scheme 指纹探测、响应 sha256、favicon sha256 与 TLS 证书元数据，`browser_crawl` 提供有界同源 crawl / known files / JS endpoint hints / OpenAPI endpoint 展开 / OpenAPI schema 参数摘要 / active+passive GraphQL introspection 结构化 / manifest / service worker cache route / source map 内容解析，`browser_fuzz_paths` 提供有界 path/extension fuzz matcher/filter/rate 控制、multi-FUZZ tuple、auto baseline 与响应聚类，`browser_fuzz_vhosts` 提供 Host header vhost fuzz、Host/SNI 模式、多 baseline host、响应 hash、聚类与 baseline filtering，`browser_sqli_probe` 提供 SQLi boolean/error/time/union oracle 探测、DBMS payload pack/指纹、ORDER BY/UNION 列数 hint、UNION 回显位枚举、布尔盲注抽取循环与证据归档，`browser_template_check` 提供内置/自定义 HTTP 模板、JSON/YAML 模板文件、DSL matcher/extractor、变量替换、结果去重与证据归档，`browser_callback_oast` 提供本地 HTTP callback listener、correlation ID 与请求日志归档，`browser_cookie_analyze` 提供 Cookie/JWT/session 解析、HMAC secret candidate 验证与 claim mutation token 生成，`browser_fuzz_params` 提供 query/JSON/form/multipart/header 参数 fuzz、nested JSON path、set/add/delete、JSON values、multipart 文件字段矩阵、同名多文件字段、嵌套 multipart、Content-Type boundary variants、parser 差异聚类与 delta classifier，`browser_http_replay` 提供 raw/captured/HAR request 重放、method/header/body mutation、multipart/binary body、请求序列变量提取/注入、更多 extractor 类型、变量作用域控制、HAR 依赖图、响应 diff 聚类、baseline diff 与浏览器 cookie 绑定；新增 `check:web-security` 契约。

## 0.3.0 - 2026-05-16

- 发布 `Pi Native Browser Bridge`（`version_name: 0.3.0-pi-native`）。
- 切换 Pi 工具到原生桥：tabs/execute/scan/wait/network/hook/frame/html/screenshot。
- 拆分桥模块：`cdp.js`、`runtime.js`、`network.js`、`frame.js`、`html.js`、`screenshot.js`、`compat.js`、`transport.js`、`hook_dispatcher.js`。
- 新增静态契约、scan 回归、fake WS 测试。
