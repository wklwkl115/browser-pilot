# Changelog

> 本文件只保留近期活跃条目，体量上限由 `tests/contracts/drift/file-ceilings.json` 的 `maxBytes` 把关（`npm run check:file-ceilings`）；超限时运行 `npm run changelog:rotate` 把最旧条目滚入 `docs/changelog-history.md`。发布包完整性仍由 package gate 的 `npm pack --dry-run` 覆盖。

- 完成 **ABML identity lattice diagnostics + product evidence（2026-06-14；MEDIUM，ABML identity/recoverable diagnostics）**。`docs/abml-identity-lattice-architecture.md` 从 design draft 收口为 execution contract：`identityGraph` 统计 entityCount、backendNodeIdCount/coverage、sourceCounts、semantic anchors 与 triggered edges，完整 `byRef` 图继续 artifact-only；`browser_observe mode=scan` 顶层提升 compact `identity` 摘要，并把 `envelope.identityGraph` / `envelope.snapshotProjection` / `envelope.relations` / `envelope.relationGraph` / `envelope.collections` 写入 `artifact_hints.jsonPaths` 供 `browser_artifact` 精读。追加执行线把 DOM scan `backendNodeId` best-effort bootstrap、bounded listener hints、DOMSnapshot paint-order `coveredBy`/`occludes` 关系、`growthProbe` scan->collections 数据通道和 OOPIF `(targetId, backendNodeId)` 复合 key 接入产品/运行时路径；对应 fixture eval `32-abml-identity-bootstrap-evidence`、`33-layer-paint-occlusion-boundary`、`34-oopif-composite-key-boundary`、`35-abml-growth-probe-evidence` 已持久化 artifact。剩余 closure 追加把完整 `RelationGraph(edges/bySource/byTarget/byType)` 保持 artifact-only、`entity.relations[]` 作为 capped 兼容字段，并让 eval runner 持久化 `observeEstimatedTokens` 与 artifact read mix；最终 fixture eval 33/33 passed，temporal profile 175 样本 `historyLostCount=0`，因此 ledger 容量和 projection/frontier 预算保持现值。OOPIF 路由走 `Target.setAutoAttach` / `Target.attachedToTarget` flat session，保留裸 backend ref 兼容，missing target fail-closed 为 `OOPIF_SESSION_UNSUPPORTED`；不新增公开 ABML verb、不恢复动作面。

- 修复 **browser_tabs tab 身份归一化摩擦（2026-06-14；MEDIUM，agent-facing target contract）**。`browser_tabs create` 的 model-facing 结果不再把 bridge request id 暴露为最显眼的 `id`：顶层 `id` 归一为可直接复用的稳定 `targetRef/tabHandle`，原请求 id 改名为 `requestId`；`browser_tabs list` 的 compact tab 结果同样让 `tabs[].id` 指向稳定 `targetRef`，旧 browser-scoped session key 仅保留为 `tabSessionId` 诊断字段。底层 `BrowserTabSessionRouter.resolveTargetRef()` 现在也能消化完整 create/list/tab result 对象、`createdTarget` / `createdTab` / `data.targetRef` 嵌套字段和旧 `tabSessionId`，避免 agent 第一次 create 后必须先 list 才能 wait/observe。回归：新增/更新 tabs tool、command service create target 和 router alias 单测。

- 治理 **CHANGELOG 体量上限 + 滚动归档机制（2026-06-14；LOW，doc governance）**。针对「治理文字逼近能力代码体量」的体检结论，给只追加的 CHANGELOG.md 设字节上限并止住膨胀：`tests/contracts/drift/check-file-ceilings.mjs` 扩展为可选 `maxLines`/`maxBytes` 双轨（向后兼容、含字节自测），`file-ceilings.json` 为 CHANGELOG.md 设 `maxBytes:65536`（64KB）；新增 `npm run changelog:rotate`（`scripts/rotate-changelog.mjs`）按空行块把最旧条目滚入 `docs/changelog-history.md`（只追加、不设上限、与顶部倒序衔接），使「gate 触顶 → 一条命令修复」成立，并登记进 `scripts/check-graph.mjs` 排除表。首轮把活跃文件从约 203KB 压到约 48KB（保留最新约 37 个段落块，其余约 72 个滚入历史归档）。回归：`check:file-ceilings`、`check:check-graph`、`check:package`、`check:doc-structure`、`check:doc-paths`、`npm run check`。

- 修复 **`browser_observe url` 导航竞态与 stale handle 恢复摩擦（2026-06-13；MEDIUM）**。`BrowserWaitSupervisor` 的 `wait.navigateAndWait` 不再在 `wait.navigate` 后接 URL-blind `wait.loadState`，改为先用 `wait.navigation targetUrl=<url>` 等到目标 URL（selector/networkidle 再追加最终 wait），并把桥层 `{ok:false}` wait lease 视为可诊断失败/超时重试，避免旧页面已 `complete` 时误判导航成功后继续提取旧页；`browser_observe mode=content url=...` 增加回归覆盖，scan/html 共用同一 URL-aware 导航路径。`BrowserTabSessionRouter` 现在能处理同 `extensionId` 重连中新 socket ready 先于旧 socket close 的 overlap 时序，复用旧 `tabHandle` 并把旧 session 标记为 superseded/disconnected；不同 extensionId/歧义场景仍不自动跨 browser fallback。stale `tabHandle` 错误在只有一个 live handle 时新增 `recovery.suggestedTargetRef` 与直接 retry action，减少扩展重连后必须先 list 的中断。回归：`test:unit:driver`、`test:observe-abml-integration`、`check:content-pick`、`check:fake-ws` 通过。

- **Living tab session acceptance-audit remediation（2026-06-13；HIGH）**。Pi-host render-contract heartbeat crash fix：`toolAdapter` operation heartbeat update 改为发送 `content: []`，解决 Pi interactive host 收到无 content 心跳时崩溃（regression test `tests/unit/tools/toolAdapter-onupdate.test.ts` + `tests/unit/toolAdapter/toolAdapter.test.ts`）。ExtensionId reconnect rebinding：`BrowserTabSessionRouter.findReconnectIdentity` — 同 extensionId + 同 numeric tabId + 唯一 disconnected candidate ⇒ 旧 `tabHandle` 重绑新连接；ambiguous/unknown ⇒ fresh identity（MV3 SW idle/wake 不再杀 handle）。新增 invariant/identity 测试：I3 (`tests/unit/tools/execute-i3-nonblocking.test.ts`)、I7 (`tests/unit/tools/standing-perception-i7-honest-loss.test.ts`)、zero-poll wait (`tests/unit/tools/wait-selector-zero-poll.test.ts`)、dirty-root cap I4 (`tests/unit/tools/dirty-root-cap-i4.test.ts`)、reconnect positive/negative/two-hop/hop-bound/cap+TTL (`tests/unit/driver/BrowserTabSessionRouter.test.ts` + `tests/contracts/runtime/check-fake-ws.mjs`)。Plan-doc §12/§13 诚实化：S3 §6.6 speculative refresh / §6.7 DOMSnapshot-as-primary 记为未执行关闭（无代码/实验/artifact）+ 显式 reopen 条件；§12 S1 `signals.fingerprint` 说明 pre-scan readPageFingerprint round trip 仍保留用于 render-cache gate（部分融合非完整）；§12 S2 shared-scan 目标未实现（N session 仍各跑全量 scan）；§4.3 memory warm-start 补入关闭记录；§8 metrics 标注未系统测量；§13 invariant coverage 修正（I3/I7 非 red-first）；§7 MV3 失效行更新为新实况（tabHandle 经 extensionId rebinding 存活，per-connection browserId 为原始缺陷类）。Blind-findings RSF3/RSF4/RSF5 新增条目；LTS1 追加 2026-06-13 mechanistic explanation。`generate-tool-docs.mjs` generator 修复：`SHARED_TOOL_PARAMS` 加入 `targetRef`、native action 工具 fallback 路径加入 `targetRef`、`sharedTabScopedParamKeys` 处理 `includeTargetRef:false`；`npm run docs:sync` 后所有 tab-scoped 工具 contract doc 现已含 `targetRef`。

- 完成 **Living tab session architecture full closure（2026-06-13，identity/standing perception/feedback；HIGH）**。`docs/living-tab-session-architecture-plan.md` 从 DESIGN 收口为 COMPLETE：S0 稳定 `tabHandle`/`targetRef`、replacement/activation/lineage、target resolver、lease/queue/perception-ledger migration 和 connection readiness diagnostics；S1 落地 scan 同调用 `signals.fingerprint`、ABML prefetched scan reuse、AX `DOMSnapshot.captureSnapshot` bulk geometry join 与 observe timing，自动 viewport-first / full fused visitor 作为未证明 byte-safe 胜出的实验关闭；S2 落地 dirty-root/overflow/drain bridge protocol、event-clean standing render cache、`PI_BROWSER_STANDING_PERCEPTION=0` kill switch、objective substrate metadata 和 session-delta/serialization canaries；S3 落地 execute effect dirty roots 与 stale-act feedback（`targetObservedAt`/`targetRegionDirty`），并锁定 `browser_wait selector` Runtime binding + MutationObserver subscription / polling fallback。全量 workflow fixture eval 28/28 通过，artifact：`.pi/browser-artifacts/eval-browser-workflows/2026-06-12T18-26-53-853Z-23d3f64c/browser-workflow-eval-summary.json`；fresh blind real-site eval 在隔离 `https://linux.do/` stage 完成 top-5-topic 任务，artifact：`.pi/browser-artifacts/eval-blind/living-tab-linuxdo-blind-report-bypass.md`，usage report：`.pi/browser-artifacts/eval-blind/usage-1781290055745-33796-report.json`，残余 `browser_execute` 后 `TAB_NOT_FOUND`/stage disconnect 已按 n=1 reliability hypothesis 记为 `LTS1`，未从单次样本直接开修。

- 完成 **Agent development harness optimization（2026-06-12，dev-harness；HIGH）**。完整执行 `docs/agent-dev-harness-optimization-plan.md` 五阶段：新增 fail-closed impact map、`query:markers`、DAG closing gate/group aliases/CI artifact upload、unit shards、v2 per-node cache scopes、`docs:sync` umbrella/managed blocks、generated README tool index、registration-derived code map、concept ownership/summary field maps、agent dev handbook、new-check scaffolder、workstream scope sentinel、audit lifecycle index，以及五个 ledger `--propose` 流程；`check:lint` legacy aggregate 已移除，`classBBaseline` 101→97。回归覆盖 package/docs、contracts、bridge/unit、src 分组、docs sync 幂等、marker/scaffolder/scope/propose 控制，最终以 `check:serial` 与 `npm run check` 收口。

- 新增 **Agent harness optimization plan（2026-06-12，agent-facing harness/maintenance；HIGH）**。完整执行 `docs/agent-harness-optimization-plan.md` ACI-1..12：operation heartbeat 改为 liveness-only；错误恢复统一到 `recovery.nextActions`；lease/UI lock 冲突输出 process-local salted hash、expiry 和 remainingMs；新增 input-surface budget gate、usage-log runId distiller、skill/tool drift 检查、file ceiling ratchet；`browser_observe fresh:true` 可单次跳过 session delta/render cache 做 context-loss re-anchor；bridge dist 以 placeholder-safe sha256 `buildId` 指纹覆盖 manifest + browser-loaded bundles，CLI/status/doctor/tabs 暴露 `extensionStale`/expected/reported build；observe/CLI entrypoint 已拆为 façade/模块入口并接入结构 gate。回归：skill validation、`test:unit`、`check:check-graph`、`lint`、`build:bridge`、`verify:bridge:dist`、`check:all:contracts`、`smoke:cli`、`smoke:browser:scan-summary`、最终 `npm run check`、复跑 `npm run lint`、`git diff --check` 全绿；fresh observe runtime artifact：`.pi/browser-artifacts/fresh-observe-runtime-1781240457763.json`，observe artifact：`.pi/browser-artifacts/observe-scan-1781240458624.json`。首次 `smoke:browser:scan-summary` 曾因隔离扩展未连接 bridge port 瞬态失败，复跑通过。

- 新增 **Check acceleration graph runner（2026-06-12，repo verification；MEDIUM）**。`scripts/check-graph.mjs` 成为本地 check 分组的单一机器清单，`run-check-groups.mjs` 改为读取该 graph 并记录 per-script duration/command/exit code 到 `.pi/browser-artifacts/check-groups-summary.json`；新增 `npm run check:trace`、`npm run check:dag`、`npm run check:dag -- --cache`、`npm run check:smart`。DAG runner 直接 spawn 本地 `node`/`tsx`/`tsc`/`eslint`，分解 compound npm scripts，加入 ESLint graph node 与 TypeScript incremental build info，输出 `.pi/browser-artifacts/check-dag-summary.json`；cache 使用粗粒度 repo fingerprint，命中只允许同 fingerprint + 同 node command 的 passing record；smart 写 `.pi/browser-artifacts/check-impact-summary.json`，未知影响保守扩展；miss recorder 写 `.pi/browser-artifacts/check-misses/*.json`。新增 `check:check-graph` drift gate 锁 graph/package script 覆盖、exclusion reason、artifact paths、synthetic miss artifact。回归：`check:check-graph`、`check:boundaries`、`check:package`、trace src run、`check:dag` no-cache 通过（85 nodes，约 111s，对比 185s baseline 约 60%）、`check:dag -- --cache` 85/85 hit（约 1s）、`check:smart -- --cache` 当前 dirty graph 下保守 full selection 通过、synthetic miss recorder 通过；最终仍需 full `npm run check` 收口。

- 修复 **Real-session friction plan（2026-06-11，真实 skill-guided 会话摩擦；MEDIUM，scan/artifact/execute/skill/eval）**。消费 Pi session `019eb646-84a4-7cf5-a648-dc70a8861ef2` 的 Huawei SRC 表单填写摩擦，不新增公开 `browser_*` 工具、不加 click/type/action sugar。scan 现在对 SVG hit target/top-layer class 走 `SVGAnimatedString.baseVal`，wrapper-pattern input 只通过 `displayLabel` 借用 hit target 的 placeholder/name/aria-label，保持 entity identity/ref-id 稳定；`browser_artifact` JSON 空数组保留为 `[]`，missing jsonPath 返回 nearest parent path/type/keys 并给出 `read_saved_artifact ... jsonPath=<nearestPath>` 恢复动作；`browser_execute` effect 在 fingerprint 不可用时输出 `signals:"partial"` 而不是伪造 `mutations:0/settled:true`，并新增 internal-only `content.fingerprint` fallback，优先 content-script responder，失败后用持久 page-world MutationObserver state。两份 frontend skill 同步 shared-popper 方法：组件库 select/dropdown 用 trigger 的 `aria-controls` + `aria-expanded` 锚定 popup，不按“第一个可见 popper”猜。Eval 02 fixture 现在点击后惰性插入 `#activation-popup[role=listbox]`，并断言 execute artifact `effect.mutations > 0`。回归：scan/page-script/artifact/unit/runtime fixture/tool/summary/token/ABML/salience/contracts/bridge gates、动态 workflow eval、`smoke:browser`、skill quick validate。

- 修复 **agent audit top10 已确认缺陷收口（2026-06-11，audit-fix；MEDIUM，runtime/memory）**。消费 `agent-audits/runs/2026-06-11-top10-defects.md` 后落地 5 个已复核缺陷：`withTrackedOperation` heartbeat interval 现在 `unref()`，避免只因心跳 timer 保活进程；`BrowserOperationRegistry` 增加 5 分钟 stale sweep 与 256 条 max eviction，防止未 finish operation 在 daemon 中无限积累；`BrowserBridgeHttpServer.stop()` 会先关闭已接受 WebSocket client，避免 stop 后 socket 句柄滞留；`verifyMemoryAnchors()` 在 recorded anchor 缺少 live counterpart 时判 `stale`，不再误报 `fresh`；`.pi/browser-memory/.secret` 改为进程内创建锁 + exclusive create + persisted reread，并发首次创建返回同一磁盘 secret。回归：新增/扩展 toolAdapter、BrowserOperationRegistry、BrowserBridgeHttpServer、memory staleness 与 memory profile store 覆盖。

- 修复 **G12 — `browser_execute` 结果缺少页面 URL，导致 strategic-memory `record candidate:` nudge 结构性不可达（2026-06-11，memory/write ignition；MEDIUM）**。`appendMemoryAutoSurface()` 只会对非观察类工具、且 envelope 能解析出 page origin 时追加 `record candidate:`；此前 `browser_execute` distiller 只输出 generic data / operation / effect，没有 `summary.url` 或 `snapshot.url`，所以 M5 盲测里所有 execute 结果都是 `recordNudgeShown:false`，战略记忆写入只能靠 operator 手动记录。修复：不改 nudge 逻辑、不自动 record、不新增浏览器读；`withExecutionEffect()` 复用已经读取的 content fingerprint 留存 `effect.url`（quiet/after/before 回退），`browser_execute monitor:true` 复用 ABML monitor 已读到的 URL，distiller 将该 URL 抬到顶层 `summary.url`，现有 result middleware 自动同步到 `target.url`。无 URL（如 effect disabled / 无 fingerprint）时字段缺省且保持旧行为。回归：execute effect URL presence/absence 单测、execute summary URL helper 单测、`check-execute-tool` 静态合同、`check-memory-autosurface` 的 execute with/without URL 对照。D6 nudge-removal clause 仍需下一轮真实 blind eval 以 `recordNudgeShown:true` 后再裁决（shown-but-unused 删除；shown-and-used 保留）。

- 新增 **Agent audit inbox workflow（2026-06-11，repo workflow；LOW）**。根目录新增 `agent-audits/`，作为其他 agent 做静态/动态代码审计的只读审计收件箱：审计 agent 可以读仓库、跑有界非破坏性检查，但只能在 `agent-audits/runs/` 写报告，不能改源码、生成文件、测试、包元数据、git 或其它文档。新增 `skills/pi-browser-audit-fix/SKILL.md` 定义用户异步指定的两个角色：审计 agent 只记录缺陷假设，修复 agent/维护者消费报告、独立复核并按正常 workflow 修复；该 skill 不负责启动子代理。内置 `AGENTS.md`、run/finding 模板和 README，报告必须带证据、复现、影响、置信度和建议验证；维护者复核后再按正常 workstream 修复。`docs/document-structure.md`、README、TODO、CURRENT、root AGENTS/CLAUDE 同步该机制，`check:doc-structure` 增加目录/模板/audit-only/skill 规则锁定。

- 新增 **Memory kernel — retain as fourth kernel（2026-06-11，browser memory/runtime；HIGH）**。`browser_observe mode=scan|text` 现在会在默认感知路径上自动利用本地 `.pi/browser-memory/`：机械 profile 从 PerceptionLedger frame 自动持久化为 per-origin profile（白名单 `PersistableMemoryTerm`、canonical path URL、local-secret HMAC stamp、serialized read-merge-write、shutdown drain），relevance 增加 cap-isolated source `F`（recurrence + live agreement gate，永不进入 trace ring），匹配的 strategic memory 由 runner 预构建 `MemoryAugmentationPlan` 并以 `envelope.memory` 注入，带 structural-anchor verification `fresh|unverified|stale`、conversation-once inline→handle collapse 和 `livePlaneSignature()` displacement ladder；M5 后收紧为必须有当前 URL/intent token overlap，仅同源不会弹出旧任务记忆；disabled/no-hit 保持 byte-identical 且不物化 memory 目录。`browser_memory record` 仍是显式战略写入，record nudge 保留；旧 `nextActions relevant memory:` recall hint 已删除。新增纯核 `src/memory-core/` 与 runtime `src/memory/`、profile store/service/HMAC、memory plane contract、profile privacy/concurrency tests；`PI_BROWSER_MEMORY=0` 关闭 kernel 自动读写但不影响显式 `browser_memory` 工具。回归：新增 `check:memory-core-boundary`、`check:memory-plane`，并通过 profile store/service、verification、relevance F、result middleware 单测、`check:task-conditioned-salience`、`check:session-delta-long-conversation`、`check:summaries`、`check:summary-boundary`、`check:token-economy`、`check:memory-autosurface`。

- 完成 **Debt zeroing — trigger-gated backlog 清零（2026-06-10，用户决策执行；MEDIUM，scan/summary/contracts/docs）**。B9a 在既有 scan entry 内落地 bounded `data.media_candidates`，summary 新增紧凑 `media_candidates` 表与 `data.media_candidates` artifact hint，严格限定为 visible media identity（selector/src/poster/alt/dims/geometry/sameOrigin），不做 headline/ranking 语义；B11 为 fixed/sticky edge utility controls 增加机械 `edgeUtility`/`position` 标记，并只在 `focus.primary_actions` 排序中降权，完整 `data.actionables` 仍保留侧栏/悬浮控件。summary grandfather 31 处接受为项目边界，不改输出本体，`check:summary-boundary` 以 31 为上限做 shrink-only ratchet；ABML public verb surface、web-reversing phase 2、renderer line 粒度、capture esbuild migration 改为已关闭项目决策，仅保留 reopen-evidence bar，不再登记为 trigger-gated backlog。同步清理 `docs/debt-clearance-plan.md`、`ROADMAP.md`、`CURRENT.md`、`blind-findings.md`、README/skill 口径。回归：`check:scan`、`check:summaries`、`check:capture`、`check:summary-boundary`、`check:output-schema-conformance`、`check:web-security`、skill quick validate、`npm run lint`、`npm run check`、`smoke:browser:scan-summary` 全绿；`git diff --check` 退出码 0（仅 CRLF 归一化提示）。

- 完成 **Debt clearance（2026-06-10，goal 执行；MEDIUM，ledger/doc/runtime cleanup）**。`docs/debt-clearance-plan.md` 已从 DESIGN 收口为 COMPLETE。D2：`ROADMAP.md` 诚实化为已关闭决策 / 当前非激活路线 / 近期质量建议；D3：W1 默认安全词表外部化继续通过 `readWordlist()` 管线并由 web-security 合同锁默认字节恒等；D4：recovery grandfather ratchet 文档化与 shrink-only baseline 收口，剩余例外逐项标注原因；D5：kernel optional bench 正式 written off，blind findings 的 B1 改为 resolved，B9a 明确依附 D1 的 bounded substrate；D1：scan 在既有 entry 内提供 `data.rows`、summary rows 与 artifact hints，真实 `https://linux.do/latest` sentinel run 通过 `browser_observe mode=scan` + `browser_artifact jsonPath=data.rows` 直接返回 14 条 DOM 顺序 visible rows，无需 custom JS。回归：`npm run check`、`npm run lint`、`check:scan`、`check:summaries`、`check:token-economy`、`bench:distill`、`check:recovery-boundary`、`check:errors`、`smoke:browser:scan-summary` 全绿。

- 完成 **capture-core + fact allocator closure（2026-06-10，goal 执行；MEDIUM，capture/distill kernels）**。新增 `capture-src/entries/*Template.ts`、`scripts/sync-capture.mjs`、`src/capture/inject.ts` 与 committed `src/capture/generated/*Bundle.ts`，`buildScanScript`、`buildContentScript`、`buildPickScript`、ABML 7 个 probe builder 与 `viewportScript` 均改为 generated template 注入；pick 采用 scan-canonical selector 语义并继承 B3 sibling cache。新增 `check:capture` / `check-capture-core-boundary.mjs`、package/check/lefthook 覆盖，锁定 `src/` 不重新拥有 page-world 大模板或 O(S²) sibling 扫描。同时收口前序悬空项：默认 salience summary/preview 路径现在运行 `factify → allocateFacts → renderFacts`，`FactSalience.relevance` 已进入 allocator currency，allocator diagnostics 只进 tool result `details`，不增加 model-facing envelope 字节。回归：`check:capture`、`check:scan`、`check:content-pick`、`check:page-scripts`、`check:abml-verb-runtime`、`check:summaries`、`check:package`、`check:task-conditioned-salience`、`bench:distill` 与 resultMiddleware/allocate-render 聚焦单测通过。

- 完成 **Task-conditioned salience v3（2026-06-10，goal 执行；MEDIUM，token economy / observe relevance）**。新增纯核 `src/distill-core/relevance.ts`、`relevanceTuning.ts`、`relevanceTaps.ts`，实现 NFKC/CJK-aware relevance matching、depth-1 结构传播、集中调参和 source tags。`browser_observe mode=scan` 现在一次性构建 relevance context，URL cold-start、既有 inference intents、行为 trace 与内部 `params.intent` 只在同 rank 内重排 scan actions / ABML primary entities；no-signal 路径保持中性，`PI_BROWSER_RELEVANCE=0` 关闭。行为 trace 仅由 `registerTools.ts` chokepoint 和 declarative `src/tools/relevanceTaps.ts` 收集，`PerceptionLedger` 新增 session-keyed capped trace ring 与 frame LRU；trace terms 不进入 envelope，debug terms 仅在 `PI_BROWSER_RELEVANCE_DEBUG=1` 的 saved artifact 出现。V6 fact-level relevance 已随 capture-core closure 在 production fact allocator 接线后完成。回归：`check:src:types`、`check:task-conditioned-salience`、relevance/ledger/observe 聚焦单测、`npm run check`、`smoke:browser:scan-summary` 全绿。

- 完成 **ABML kernel optimization（2026-06-10，goal 执行；MEDIUM，pure compute/sensing）**。`observeRunners.ts` 删除重复 `buildSnapshotProjection`；新增 `tests/unit/abml/snapshotProjection.test.ts`。`src/abml-core/ax.ts` 收敛 AX 热点：`isInterestingAxNode` 改为 lazy `axValue`、角色分类 hoist 为 module-level `Set`、`buildAxEntityFromNode` 用单次 property map 复用状态/结构/值读取、`mergeDomAndAxEntities` 预提取 name/role/box/point。`src/abml-core/refId.ts` 把 `stableHash24` 改为单遍三累加器。新增共享纯核 `src/abml-core/grouping.ts` 与 shim `src/abml/grouping.ts`，`templating/treeDiff/snapshotProjection/semanticRefAnchor` 统一复用，`snapshotProjection` 直接走 `buildTemplate()`，不再对已分组成员二次分组。`src/abml/verbs/axRuntime.ts` 单次 ancestor walk 同时拿 nearest container 与 current-container fallback 链。`src/scan/buildScanScript.ts` 的 `selectorFor` 加入按 parent 缓存的 sibling `nth-of-type` 索引，去掉宽容器 O(S²) sibling 扫描且 selector 输出不变。回归：`check:all:bridge`、`npm run check`、`smoke:browser:scan-summary` 全绿。

- 完成 **Renderer default flip P3 — session-delta 默认翻转（2026-06-10，owner goal 执行；MEDIUM，token economy）**。`browser_observe mode=scan` 在同一 session/tab/url 的后续观察默认复用 PerceptionLedger 上一帧，输出 `delta:"session"` 与 `baselineSnapshotId`；`PI_BROWSER_SESSION_DELTA=0` 强制旧 I-frame 行为，`detailLevel:"full"` 仍是刷新逃生。默认翻转保持公开工具 schema 不变，只改变既有 summary envelope 的默认压缩路径；P-frame 继续通过 saved artifact 和 `read_saved_artifact` recovery 暴露紧预算下的细节。新增 `check:session-delta-long-conversation`，跑 6 次 observe 的长会话 fixture，覆盖默认 P-frame 链、缺失 baseline snapshot 自动退回 I-frame、context-loss 后恢复 inline refs、`PI_BROWSER_SESSION_DELTA=0` 逃生舱和 F3 recovery fan-out cap，并接入 `npm run check`。

- 完成 **Renderer default flip P1/P2（2026-06-10，owner 决策执行；MEDIUM，token economy）**。默认 summary renderer 从兼容 ladder 翻为 salience：`renderer:"salience-v1"` 继续自描述，`PI_BROWSER_RENDERER=ladder` 强制旧路径，`PI_BROWSER_RENDERER=salience` 保持可接受 no-op。翻转前先落通用回归修复：salience 分配加入边际密度停止和 salience-vs-ladder 接受门，统一 omission/truncation marker 统计，session-delta recovery target fan-out cap 只在 `delta:"session"` 时生效。`bench:distill` 现在对每个 fixture 同测 ladder/salience/default，断言 salience chars ≤ 1.05× ladder、事实覆盖 ≥ ladder、截断 marker ≤ ladder，且默认路径等于显式 salience。默认翻转过程中 full check 暴露并已修复两类真实回归：salience 紧预算必须保留 `nextActions` 指向的实体，且不得丢 `diff/treeDiff/snapshotProjection` 连续性平面。回归：`npm run check`、`eval:browser-workflows -- --fixture-server --eval 16-scan-high-entropy-summary`、`smoke:browser:scan-summary` 全绿。

- 优化 **简单 token 型 array flag 支持逗号分隔（2026-06-09,dogfood 时撞出;LOW,CLI 易用性,accept-natural-aliases 类）**。`pi-browser cookie-analyze --secret-candidates "a,b,c"` 被静默当成**1 个**候选(`cli/flags.ts` 仅当 FlagSpec 有 `split:"comma"` 才拆分,而此前**只有 `hook installTargets --targets`** 一次性设了)。`buildCommandFlagSpecs` 现对一组**确定不含内部逗号的简单 token array flag**(`secretCandidates/secrets/wordlist/words/paths/urls/templates/templateIds/templatePaths/workflowPaths/paramNames/hosts/tags/excludeTags/severities/authors/schemes/ports/knownFiles/probeTypes`)设 `split:"comma"` 并在描述里注明,agent 传逗号或重复 flag 都行。用 **allowlist 而非 denylist**:漏列只是保持旧行为,绝不会误拆;`headers/values/jsonValues/mutations/*payloads/raw*` 等含逗号的值**不在**列表、不受影响。回归:`tests/unit/cli/flags-render.test.ts`(+1:secret-candidates 逗号拆分、重复 flag 仍累加、comma-unsafe 的 `values` 不拆);`check:cli-parity`、CLI local/json 单测通过;dist 重建后 live 验证 `--secret-candidates "a,b,c,d,e"` 测 5 个候选(原 1)。`evals/browser-workflows/blind-findings.md` 记为 G11。

- 修复 **后台 tab 的异步 `browser_execute` 不再因计时器节流卡死成 `BRIDGE_TIMEOUT`（2026-06-09,dogfood 真实站点逆向时撞出;MEDIUM,execute 可靠性）**。非前台/隐藏 tab 被浏览器有意节流页面计时器(`setTimeout`/`setInterval` 及带超时的 fetch/轮询),导致 MAIN-world `chrome.scripting.executeScript` 的异步脚本永不 resolve、`browser_execute` 撞 15s `BRIDGE_TIMEOUT`(同步脚本不受影响)。这也是部分早期会话 Turnstile 轮询 `BRIDGE_TIMEOUT` 的根因。实测确认:启动 flag(`--disable-background-timer-throttling`)要重启浏览器、和"附着日常浏览器"模型冲突;单纯 chrome.debugger 附加**不**解开节流;而 CDP `Emulation.setFocusEmulationEnabled({enabled:true})` 让单个 tab **模拟前台**、解开节流(`setTimeout(800)→809ms`),**不抢用户真实焦点、不重启、不动全局策略**。修复(`bridge_src/service_worker/exec.ts`):① 检测目标 tab `active===false` 时直接走 CDP 路径(跳过会节流且可能双执行 side-effect 脚本的 executeScript);② CDP `Runtime.evaluate` 前先 best-effort `Emulation.setFocusEmulationEnabled` 解开节流。异步 fetch/轮询/带超时 execute 从此在后台 tab 也能完成,agent 不必把 tab 切前台。代价:后台 tab 的 execute 经 CDP → chrome.debugger 附加 → 浏览器顶部出现"正在被调试"提示条(仅后台 execute;前台 tab 不受影响)。回归:`check-pi-browser-bridge.mjs` 新增 sandbox 行为测试(后台 tab 跳过 executeScript、focus emulation 在 eval 之前、返回结果);`npm run check:bridge:types`、`build:bridge`、`check:all:bridge` 通过;扩展重载后 live 验证后台 krill tab 上 `setTimeout`/`fetch+AbortController`/轮询 execute 均完成(原为 BRIDGE_TIMEOUT)。同修一处早先 skill 重写遗留:`check-pi-browser-bridge.mjs` 的 tabId-flow 断言接受对象式 `browser_tabs {action:"list"}`(该契约不在 `check:all:contracts` 组,a103f2a 未捕获)。`evals/browser-workflows/blind-findings.md` 记为 T1。

- 修复 **大 JSON 工件读取被 text 截断器拦腰砍成不可 parse（2026-06-09,dogfood `pi-browser` CLI 逆向真实站点时撞出;MEDIUM,artifact-读取 ergonomics 类）**。`browser_artifact mode=json jsonPath=data.items`(正是 network/evidence 结果 `nextActions` 自己建议的读法)在"请求数多"的捕获上返回**不可 `JSON.parse`** 的结果:`compactJsonValue` 按 item 数 + 每串 800 字符预览结构化窗口(产出合法),但结果再经 `jsonResult`→`jsonPreview = truncateText(stableJson(value))`——这是 **text 模式**的 head/tail 截断器,被施加到 **JSON** 序列化结果上,在 token 中间切断并插入字面 `[truncated N chars]`;CLI 因无法 parse 而 fallback 成 `{message}` 形状,与小读取不一致。双前端均受影响(Pi-native 也拿到被截断的 `content[0].text`)。修复(局部在 artifact/screenshot 专用的 `inlineJsonToolResult`,不动共享 `truncateText`):新增 `fitInlineJsonToBudget` —— 序列化超预算时**结构化收缩**(二分搜索最大可容的 array `items` 前缀 / object key 前缀,保留 `count`、置 `nextOffset`/`budgetTrimmed`/`truncatedKeys` 供分页),并把序列化预算下限抬到收缩后大小,使 text 截断器**永不**切割 → 输出**始终是合法 JSON**。小读取/screenshot 不变。回归:`tests/unit/tools/inline-json-budget.test.ts`(+4:大数组读可 parse 无 `[truncated]`、二分取最大窗口、小值不动、object 按 key 收缩);`check:src:types` 干净;dist 重建后 live 验证 `data.items` 大读取可 parse(`items` 窗口化 + `nextOffset`/`budgetTrimmed`,`count` 保留);`check:all:contracts` 通过。`evals/browser-workflows/blind-findings.md` 记为 A1。

- 修复 **`wait.loadState {state:"networkidle"}` 的 reroute 改为 Node 侧、对 stale 扩展鲁棒（2026-06-09，从真实会话日志挖出；MEDIUM，accept-natural-input class）**。真实 CTF 会话 `2026-06-09T02-37-51…jsonl`（bilibili web-sec，205 工具调用）里 `browser_wait {action:"loadState", params:{state:"networkidle"}}` **连续 3 次**硬失败 `INVALID_RULE: wait.loadState unsupported state` —— 而该 reroute（loadState+networkidle→networkIdle）2026-06-05 起已在扩展侧 `bridge_src/service_worker/wait.ts` 落地、且已编进 built dist。根因：reroute **只在扩展侧**,会话里加载的 service worker 是 stale 旧版（呼应同期的扩展掉线/重载），旧扩展把已修复的 friction 复活了。修复：Node 驱动永远是最新的（daemon 重启即生效），故在 `BrowserWaitSupervisor.executeBrowserWaitWithSupervisor` 入口加 `rerouteLoadStateNetworkIdle` —— `wait.loadState` 的 state 归一化为 `networkidle` 时,派发前改写为 `wait.networkIdle` 命令,镜像扩展归一化逻辑,使 reroute **版本无关**、Pi-native 与 CLI 同覆盖。回归：`tests/contracts/runtime/check-fake-ws.mjs` 新增断言（派发出去的命令为 `wait.networkIdle`、不带 `state` key、正常 resolve）；`tsc -p tsconfig.json` 干净。会话另两条信号已 triage：`browser_execute BRIDGE_TIMEOUT`×4（轮询 Cloudflare Turnstile token 的 `for(40×){await sleep}` 长脚本超 15s bridge 窗口 —— 倾向 WAI/agent 技法,记为候选）、`TAB_NOT_FOUND`×5（Cloudflare 挑战页重载换 tabId —— 已 retryable+recover,skill 已警示 tabId 不稳定,WAI）。详见 `evals/browser-workflows/blind-findings.md`。

- 优化 **缺必填参数报错改为友好可自纠（2026-06-09，agent-UX 治理；LOW 但通用）**。落地 blind-eval 开放项 F3：`pi-browser tabs --json` 等漏传 `--action` 时，原本返回原始校验器噪音 `Invalid parameters — /: must have required properties action`，迫使 agent 解码 TypeBox 路径。`src/frontend/validation.ts` 早已把"未知 key"翻译为友好提示（`describeUnknownProperties` 点名 + 列 accepted），但对称的"缺必填"分支漏了。新增 `describeMissingRequired(schema,value)`：点名每个缺失的必填参数，并附其 `description`（action 类工具的 description 携带 `One of: …` 值表），消息现为 `Invalid parameters — missing required parameter "action" (One of: list, snapshot, switch, …)`（network 为 `(start | stop | status | …)`）。改在共享校验层，Pi-native 与 CLI 同时受益；unknown-key 格式与优先级保持不变；schema 派生、无契约改动。回归：`tests/unit/frontend/validation.test.ts`（+2），`npm run check:src:types` 通过，dist 重建后 `tabs`/`network` 实测输出友好消息。`evals/browser-workflows/blind-findings.md` F3 标记 RESOLVED。

- 修复 **agent CLI 与 offscreen 连接稳定性审计收口（2026-06-09，agent CLI/bridge reliability；MEDIUM）**。严格审计后落地低风险确定缺陷：natural action subcommands 现在拒绝混用 legacy `--params`，避免隐藏 JSON 覆盖自然 flag；`schema <cmd> <natural-subcommand> --json` 返回收窄后的自然 flag schema，不再暴露 root `action/params` 形状；daemon `/connect` 启动 bridge 失败时返回 `CLI_BRIDGE_START_FAILED`，CLI envelope 保留该分类和 `bridge` taxonomy，不再误报为扩展未连接；offscreen 单端口 probe 只探请求端口，service-worker stale adapter 在 offscreen `sent:false`/send error 后清理，已有 open adapter 的 probe/status 同步不再重复发送 `ext_ready`，清理后重连仍会重新 ready；`BrowserOperationRegistry.update()` 不再把已脱敏的 `leaseOwnerHash` 二次 hash；agent-native 文档状态从 active/draft 口径校正为 completed/current-reference，并加文档漂移 guard。回归：`node tests/contracts/protocol/check-pi-browser-bridge.mjs`、`npx tsx --test tests/unit/cli/local-commands.test.ts tests/unit/cli/flags-render.test.ts`、`npx tsx --test tests/unit/driver/BrowserOperationRegistry.test.ts`、`npm run check:bridge:types`、`npm run build:bridge`、`npm run verify:bridge:dist` 通过；真实浏览器长空闲重连 smoke 仍按 `ROADMAP.md` 的 opt-in runtime 证据路径执行。

- 优化 **Scan focus entity refs-v1 输出瘦身（2026-06-08，agent-facing scan contract；MEDIUM）**。按 `docs/performance-overhead-audit.md` 3.3 先跑 targeted blind eval：两个实现盲 subagent 在隔离 real `https://linux.do/` stage 上完成 read-only top-5 topic/reply 任务，均明确未消费 `summary.focus.primary_actions[].entity` / `summary.focus.primary_entities` / `summary.focus.referenced_entities` 整对象，主要使用 scan 结构、artifact `data.list_hints` 与只读 JS 行提取；证据写入 `evals/browser-workflows/blind-findings.md`，stage 已 clean teardown。基于证据落地 refs-v1：`summary.focus.entityShape:"refs-v1"` 标记迁移形状，`primary_actions` 改为 `entityRef`，`primary_entities` / `list_entities` / `visual_regions` / `referenced_entities` 均改为 `pi-ref://...` 字符串，`referenced_entities` cap 40→12；完整实体对象仍通过 top-level `envelope.entities` 显式披露并镜像进 saved artifact `envelope.entities`，ABML runtime 改用内部完整 entity set，不再依赖 focus 对象；baseline 完整 envelope 不再因 focus ref arrays 被误判 partial baseline。`controlsSelectors` / `ownsSelectors` / `expandedTargetSelectors` hint 数组 cap=8。高熵 scan summary golden 从 11793 chars 降到 3522 chars。回归：`check-abml-scan-entities`、`check-abml-scan-envelope`、`test:observe-abml-integration`、`check:summaries`、`check:abml-inference`、`check:abml-causal`、`check:output-schema-conformance`、`check:src:types`、`check:token-economy` 通过。

- 优化 **Performance audit 剩余项收口与 agent CLI 输出瘦身（2026-06-08，runtime/CLI performance；MEDIUM）**。基于 eval 子代理复核关闭剩余 follow-up：0.3 不在缺少 cheap `recorder.state` 协议时跳过 network/hook status，避免丢 seq baseline；1.1 不把 `textOnly` / `includeIframes:false` / `maxNodes` 非默认 scan 强行 superset-capture-and-clamp；1.7 不复用 stream signal ref，避免改变 drain 刷新 `captureRef` 合同；2.2 不缩短默认 5s extension wait，保留 event-driven wait + negative cache。落地安全子集：CLI-tagged daemon `/invoke` 成功响应省略 transport-only `details`，`browser_tabs` 在 CLI omit context 下不构造 details-only observation snapshot diagnostics；CLI artifact enrichment 移除冗余 `readArgv`，以 `artifacts[].readCommands` 为 canonical，`cliNextActions[]` 只保留非重复 follow-up，generic common jsonPath 收敛到 `data` / `data.content` / `data.actionables` / `data.list_hints`；`browser_tabs action=list` 默认返回 compact tabs + top-level shared `bridge`，`includeBridgePerTab:true` 保留旧 per-tab bridge 兼容。3.3 scan focus entity-ref shape change 当时按规则保留为 targeted blind-eval gate，已由上方 refs-v1 后续项取证并落地。回归：`npx tsx --test tests/unit/tools/tabs-tool.test.ts tests/unit/cli/daemon-lifecycle.test.ts`、`npx tsx --test tests/unit/cli/flags-render.test.ts tests/unit/cli/local-commands.test.ts tests/unit/cli/json-envelope-contract.test.ts`、`npm run check:src:types`、`npm run check:tool-parameter-contract`、`npm run check:tool-docs`、`npm run check` 通过。

- 优化 **Performance & overhead audit 低风险批次（2026-06-08，runtime/CLI performance；MEDIUM）**。根据 `docs/performance-overhead-audit.md` 落地非 agent-facing contract 的性能优化：bridge dist 对 service-worker/offscreen 启用 whitespace minify 且保留 identifier（service-worker 从约 476KB 降到 399,864 bytes，并由 `<405KB` 合同预算锁定）；AX merge 的每节点 `DOM.getBoxModel` 改为并发批处理；observe 的 network/hook seq 状态读取改为 `Promise.all`；默认 `browser_observe mode=scan` 将首个 `scan_extract` payload 传给 ABML read，避免第二次 `abml_read_scan` DOM eval（text / iframe-disabled / maxNodes 路径保留独立 ABML capture）；`containsSensitiveEvidence` 从 redact+双 `stableJson` 改为 first-hit predicate，并用 redact-then-compare oracle 回归；CLI JSON render 在 `renderResult` 内只 parse 一次 envelope；network recorder diagnostics 写入 cap=100；intercept paused Map cap=500，溢出时 oldest request 自动 `Fetch.continueRequest` 并记录诊断；`browser_execute` 普通路径不再固定等待 200ms，仅疑似开新标签脚本保留 50ms 观察窗；CLI command registry/tool defs memoize，实际 `pi-browser --help` bin 入口改为轻量动态导入 `cli/help.ts`，避免只打印 help 时加载 dispatcher/registry graph；nested runtime validation 从 `zod` 迁到 TypeBox-compatible local wrapper 并移除 `zod` 依赖；daemon compatibility 改为只按 `DAEMON_PROTOCOL_VERSION` 判断；offscreen bridge 端口探测改为 primary-first 并发 fan-out，单端口 timeout 500ms；`resourceStore`/`refStore` 增加 10k entry cap 与 amortized prune，容量淘汰走既有 not-found 恢复路径；`summarizeScanData` 跨 budget rung 复用 content/actionables/list/entities 与 ranked action 预计算，在 `check:summaries` 加入高熵 scan byte/shape golden，并继续 collapse 剩余 per-rung loops（sorted action ranking、form summary、headings、text-signal candidates、interactive rows、list summaries、action entity lookup、referenced entity slices 均准备一次后复用）；`check-token-contract` 新增 CJK budget fixture，锁住 `String.length` 而非 UTF-8 byte accounting；`fitEnvelopeBudget`/`fitSummaryBudget` 落地 serialize-once safe subset：复用 120/5/5 compact summary、每次预算检查只构造一次 marked envelope、lifted value 长度每侧只 `stableJson` 一次；extension readiness 从 100ms poll 改为 `ext_ready`/`tabs_update` 事件唤醒，并对连续 no-extension 命令加 500ms negative cache；`browser_artifact` text/search/sample 减少重复文件读取并保持 char/line 语义。实测：重建后实际用户入口 `node dist/cli/bin.js --help` 六次 median 约 56ms，内部 dispatcher 入口 `node dist/cli/index.js --help` median 约 315ms；dev/tsx 入口仍受 loader 主导但从 `tsx cli/index.ts --help` median 约 799ms 降到 `tsx cli/bin.ts --help` median 约 389ms。未触碰 Tier 3 输出瘦身；full/syntax minify 因会改变 dist 字符串合同，留作单独后续项；extension 默认 5s 等待暂不缩短，避免无慢启动证据时回归；stream signal ref reuse 已确认触碰 captureRef-refresh 合同，改为 contract/eval-first follow-up。回归：`npm run build`、`npm run build:bridge`、`npm run verify:bridge:dist`、`npm run check:deps`、`npm run check:src:types`、`npm run check:summaries`、`npm run check:token`、`npm run check:token-economy`、`npm run smoke:browser:scan-summary`、`node tests/contracts/protocol/check-pi-browser-bridge.mjs`、`npx tsx tests/contracts/tools/check-abml-ref-registry.mjs`、聚焦 validation/redaction/CLI render/daemon-control/driver connection/observe-abml-integration/artifactReader/resourceReader/CLI local 单测通过。

- 优化 **Agent-facing CLI connection readiness 的状态体积与冷启动竞态（2026-06-08，agent CLI readiness；MEDIUM）**。`/status`、`/connect` 与 `pi-browser status/connect --json` 默认不再返回完整 `tabs[]`，改为紧凑 `tabCount` + `activeTab`；需要完整列表时显式传 `?tabs=1` 或 CLI `--tabs`。同时把 bridge 已维护但未透出的健康字段接到 status envelope：`lastPingAt`、`lastPongAt`、`connectedForMs`、`tabSyncAgeMs` 等，agent 可以区分“连接存在”与“连接可能不健康”。`ensureDaemon()` 增加 user-local start lock，覆盖从 spawn 到 daemon lockfile 可达的窗口，避免两个 agent 同时冷启动时各起一个 detached daemon 抢端口。未加入 idle TTL、owner/lease、repair 子命令等无 eval 证据的扩张。回归：CLI unit 覆盖默认紧凑、`--tabs` 展开、健康字段、start lock 等待并发启动；`check:src:types` 通过。

- 新增 **Agent-facing CLI connection control protocol（2026-06-08，agent CLI readiness；MEDIUM）**。新增 `pi-browser connect --wait --timeout-ms <ms> --json` 与只读 `pi-browser status --json`：agent 可显式启动/复用 singleton daemon、启动 bridge、等待扩展连接，并获得 `ready`、daemon/bridge/extension/tabs/activeTab 与 recovery commands；`status` 不 auto-start、不启动 bridge，无 daemon 时返回 `ready:false` + exit 0。daemon 增加 token-guarded `/connect` 控制端点，直接启动 `BrowserBridgeServer` 而不伪造工具调用；失败时 `connect` 返回单 JSON envelope（如 `CLI_EXTENSION_NOT_CONNECTED`，exit 3）。`daemon stop` 保留为高级生命周期命令，不再作为普通 agent 收尾 SOP。回归：CLI unit 覆盖 status read-only/no lockfile、stale lockfile 不泄 token、fake daemon connect success/timeout；JSON envelope contract 覆盖 connect/status；full CLI runtime smoke 改为先 `connection.status.initial` → `connection.connect-wait` → `connection.status.ready` 再跑 22 命令全矩阵。

- 新增 **全量 CLI 真测门禁 `smoke:cli:full`（2026-06-08，agent CLI runtime；MEDIUM）**。新增 `tests/smoke/smoke-cli-full.mjs` 与 `npm run smoke:cli:full`：启动隔离 Edge/Chrome profile、临时 patched extension、临时 `PI_BROWSER_DAEMON_STATE_DIR` 和本地 fixture server，然后用真实 `pi-browser` CLI 子进程逐项验证 22 个公开命令、所有命令 help/schema、local validate、wait/network/frame/hook natural routes、`command --command @file`、`execute --script-file`、artifact read/search、memory record/recall/read、callback-oast、cookie/jwt、crawl/fuzz/http-replay/sqli/template、upload/download/screenshot/evidence/selftest。结果写 `.pi/browser-artifacts/smoke-cli-full-results.json`；本轮实跑 106 步全绿，证明当前 CLI surface 可用。文档与 contracts 已锁定该入口。

- 文档 **全项目当前口径校对与 TODO 系历史压缩（2026-06-08，文档治理；LOW）**。按 `docs/document-structure.md` 重新校对当前入口和历史层级：`CURRENT.md` 压缩为当前状态/无激活执行线/少量仍影响规则的完成线，详细完成历史迁入 `ARCHIVE.md` 摘要；`ROADMAP.md` 修正 eval runner 已落地主干、ABML R1 relationship graph 已完成的过期未来口径；`NEXT_PHASE.md`、`docs/tool-surface-consolidation-plan.md`、`docs/architecture-design-improvements-plan.md`、`docs/cli-parity-testing.md`、`docs/cli-skill-frontend-migration-plan.md`、`docs/maintainer-map.md`、`docs/driver-architecture.md`、`docs/tool-boundaries.md` 与 eval README 修正 capability profile / future runner / legacy observe tool 等已漂移描述；`scripts/sync-doc-indexes.mjs` 修正 TODO 入口的文档结构行语义去重，避免同步后重复插入。无 runtime 行为变更。

- 完成 **agent CLI 产品化最终收口审计（2026-06-08，agent CLI 标准化；LOW）**。P0/P2/P5 剩余弱证据已补齐：`local-commands.test` 现在覆盖 `pi-browser --help` 与所有 `<cmd> --help` 均为 local/no-daemon、`execute --script-file ... --json` 通过 fake daemon 真实进入 `/invoke` 并把文件内容映射到 `script`、以及 repo-local `npm run cli` 会污染 stdout 而 `npm --silent run cli -- ...` 保持单 JSON 输出；`resourceReader.test` 覆盖 browser-result 与 pi-ref artifact staleness 返回稳定 `RESOURCE_STALE` 且不泄露本地路径；`check-errors` 覆盖 `RESOURCE_STALE` 的 fresh resource recovery。`docs/cli.md` 与 skill 同步明确机器 JSON 应使用安装后的 `pi-browser`、直接 entry 或 `npm --silent`，普通 `npm run` 只适合人工调试。执行计划 `docs/agent-native-cli-execution-plan.md` 更新为 completed，`CURRENT.md`/`TODO.md` 去掉过期的 frame/hook blind eval 与 release gate 待办口径。

- 修复 **CLI stale-state/artifact recovery envelope 与长驻 daemon 刷新（2026-06-08，agent CLI 标准化；MEDIUM）**。agent 读过期/缺失 snapshot 或 artifact 时，需要拿到能直接执行的恢复路径，而不是 raw ENOENT 或本机 resolved path。修：`browser_tabs action=snapshot` 的 missing/expired snapshot 错误现在把重新 `observe --mode scan`、重新 snapshot、或从已保存 artifact 定向读取的建议放进结构化 `recovery.nextActions`；`browser_artifact` 缺失路径返回 typed `ARTIFACT_NOT_FOUND`，默认 message/details/recovery 不再暴露 resolved 绝对路径，仍保持 artifact scope 与 `pi-browser artifact`/`observe` 恢复动作。因 CLI daemon 是长驻进程，daemon protocol bump 到 `.3`，保证已启动旧 daemon 在下一次 tool invoke 时自动替换为当前实现。回归：`check-artifact-reader.mjs` 覆盖 missing artifact typed error、非泄露 details/recovery；`check-errors` 覆盖错误归类；真实 `node dist/cli/bin.js artifact ... --json` 和 `tabs --action snapshot ... --json` 验证恢复 envelope；`doctor --json` 验证 `expectedVersion/version` 为 `0.3.0+daemon.3`。

- 修复 **agent CLI frame/hook natural discovery 与 hook targets 输入摩擦（2026-06-08，agent CLI 标准化；MEDIUM）**。真实 blind eval 证明 `frame list/evaluate` 与 `hook list-targets/install-targets/collect/uninstall/performance` 自然路由能完成任务且无需回退 `--action/--params`，但两个 agent 都暴露同一发现问题：`schema frame --json` / `schema hook --json` 顶层仍只显示 advancedCompatibility，不列 help 中的 recommended natural subcommands；hook run 还发现 `--targets console,error` 被当作单个 target，重复 `--targets console --targets error` 只保留最后一个值，且文档示例误写 `errors`。修：action-tool root schema 继续忠实标记裸命令为 advancedCompatibility，但同步输出 `subcommands[]` 让 agent 机器可读发现 natural routes；natural `hook install-targets --targets` 明确建模为 target id 列表，支持逗号分隔和重复 flag，schema metadata 标出 `split:"comma"`，docs/skill 改用真实 target id `error`。回归：`local-commands.test` 覆盖 root schema natural subcommands 与 `install-targets` flag metadata；`flags-render.test` 覆盖 `console,error` 和重复 `--targets` 都等价于 legacy `params.targets:["console","error"]`；blind stage 已 teardown。

- 增强 **blind eval route-adoption rubric 覆盖 frame/hook（2026-06-08，agent CLI 标准化；LOW）**。wait/network 已有 blind route-adoption 证据，但 frame/hook 自然子命令落地后，blind prompt 与 triage skill 仍主要点名 wait/network，无法系统衡量 agent 是否继续回退 `--action/--params`。修：`blind-agent-prompt.md` 的 `CLI ROUTING ADOPTION` 改为覆盖 `wait`、`network`、`frame`、`hook` 四类 action-style commands，并列出 `frame list/evaluate`、`hook install-targets/collect` 示例；`pi-browser-blind-eval` skill 第 6 步同步要求四类 route adoption triage。`check-eval-workflows` 新增静态断言，防止 prompt/skill 退化；随后 frame/hook 真实 blind run 已写入 `blind-findings.md`，并反向驱动了 schema discovery / targets 输入修复。
