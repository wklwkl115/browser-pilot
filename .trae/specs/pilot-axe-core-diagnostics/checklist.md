# Checklist

- [x] 已检查 `axe-core` license、browser/MV3 compatibility、package 入口、bundle size 与运行 API。
- [x] 已选择并记录最小 diagnostics 方案、触发方式、预算和是否新增依赖。
- [x] 若新增依赖，`package.json` 与 lockfile 已更新；若不新增依赖，原因已记录。
- [x] axe diagnostics runner 在 browser-compatible 路径中运行，并支持 fail-closed fallback。
- [x] axe runner 有超时、最大结果数或等价预算控制。
- [x] axe diagnostics 输出包含 bounded issue counts、impact/rule counts、安全 sample issues 和 artifact 引用。
- [x] axe 详细结果写入本地 artifact，并遵循现有 privacy/redaction/readCommands 约定。
- [x] inline summary 不暴露敏感 token/cookie/authorization 值或长 raw DOM snippet。
- [x] axe provider 状态能真实表达未请求、executed、skipped、failed 或 degraded。
- [x] no-mode `browser_observe` 默认路径不会完整运行 axe。
- [x] axe 结果不会创建、删除或重排 actionables、refs、entities、relations、collections。
- [x] Browser Pilot actionability、hit-test、editable、visibility、AX/DOM fusion 仍为结构和执行权威。
- [x] `src/kernels/*` 未引入 `axe-core` 或 browser/runtime diagnostics 依赖。
- [x] command catalog 或 bridge protocol 未被不必要扩大；如新增公开入口，已有 schema/docs/tests 覆盖。
- [x] 新增 focused tests 覆盖 runner success、failure/unavailable、timeout 或 degraded fallback。
- [x] 新增 focused tests 覆盖 summary bounding、artifact shape、redaction 与 provider diagnostics honesty。
- [x] 已记录是否需要 observe regression benchmark case；若新增则相关 benchmark 通过。
- [x] extension/diagnostics 构建检查已通过，MV3 bundle 可用且体积变化已检查。
- [x] Edge live bounded axe diagnostics 实测成功，或有明确环境阻塞记录。
- [x] no-mode observe 默认路径实测或测试证明 canonical PageObservation 未明显退化。
- [x] `CODE_WIKI.md` 已同步 accessibility diagnostics、provider status 与依赖/runtime boundary。
- [x] `.trae/notes/abml-observe-long-term-optimization.md` 已同步 P4 axe-core 线路状态。
- [x] 新增 axe diagnostics focused tests 已通过。
- [x] `mise run affected` 已通过。
- [x] `mise run verify` 已通过，或有明确记录说明无法运行的原因与剩余风险。
