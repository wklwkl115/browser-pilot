# Checklist

- [x] 已检查 `Accessibility.getPartialAXTree` 参数、返回形态、browser compatibility、timeout/error 行为与 `fetchRelatives` 成本。
- [x] 已梳理 full AX、geometry fallback、backendNodeId bootstrap、pierce runtime 与 diagnostics 接入点。
- [x] 已选择并记录最小 partial AX 触发场景、预算、fallback 和风险。
- [x] partial AX helper 位于 browser runtime 层，未进入 `src/kernels/*`。
- [x] partial AX helper 支持 backendNodeId/resolved target 输入、timeout、max node count、fetchRelatives policy 和 fail-closed fallback。
- [x] partial AX 结果被规范化为 kernel 可消费的 plain AX-like data。
- [x] unsupported/error/timeout/empty/over-budget 均返回诚实 skipped/failed/degraded diagnostics。
- [x] local pierce/read refinement 仅针对请求 target/ref scope 使用 partial AX。
- [x] canonical no-mode observe 默认 full AX fusion 路径未被 partial AX 替换。
- [x] partial AX 不创建请求 scope 外的 page-wide AX-only entities。
- [x] scan-backed selector/ref/actionability/evidence 仍是执行权威。
- [x] `src/kernels/*` 未引入 browser runtime、CDP、commands 或 npm runtime dependency。
- [x] focused tests 覆盖 partial AX 成功 enrichment。
- [x] focused tests 覆盖 unsupported/error/timeout/empty fallback。
- [x] focused tests 覆盖 max node count/budget truncation 与 degraded diagnostics。
- [x] 已记录是否需要 observe regression benchmark case；若新增则相关 benchmark 通过。
- [x] `CODE_WIKI.md` 已同步 full AX vs partial AX 职责、触发条件、diagnostics 和 kernel/runtime boundary。
- [x] `.trae/notes/abml-observe-long-term-optimization.md` 已同步 P2 `getPartialAXTree` 线路状态。
- [x] 新增/相关 AX/pierce focused tests 已通过。
- [x] `mise run affected` 已通过。
- [x] `mise run verify` 已通过，或有明确记录说明无法运行的原因与剩余风险。
