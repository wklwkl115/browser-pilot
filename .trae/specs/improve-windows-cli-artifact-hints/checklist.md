# Checklist

- [x] 已检查 `package.json` bin、build/prepack/prepare 脚本、CLI built entry、npm pack 文件列表和 Windows shim 行为。
- [x] 已复现或模拟 Windows 全局 `browser-pilot --help` 模块解析路径，并记录失败原因和最小修复点。
- [x] 已盘点 observe/crawl/execute/artifact 结果中已有 schema、kind、jsonPath、artifact hints 和缺口。
- [x] 已盘点文档/help 中文件参数描述与实际 parser 行为差异。
- [x] `bin.browser-pilot` 指向的 built CLI entry 在 package/global 安装布局中存在。
- [x] 未构建 checkout 的 CLI 错误或 recovery guidance 清晰，不再只有 opaque module-not-found。
- [x] packaging/bin tests 覆盖 Windows 风格路径、built entry 存在性和 help invocation。
- [x] bridge dist 构建与 CLI packaging 互不破坏，`bridge/browser_pilot_bridge/dist/*` 可生成且进入期望文件列表。
- [x] artifact hint 形态包含 kind/schema version、summary path、primary items path、body/text path、provider-specific path、saved artifact path 中适用字段。
- [x] artifact hints 在 result/artifact projection 层 compact 生成，未复制大型 artifact 内容。
- [x] artifact hints 不广告不存在路径，并为不同 kind 提供可用 fallback path。
- [x] 现有 PageObservation/content/text/evidence jsonPath 保持兼容。
- [x] 文档/help 明确 `--command` 只接受内联 JSON。
- [x] 文档/help 推荐 Windows 用户使用实际支持的 `--program @file`、`--script-file` 或其他文件参数。
- [x] 文档治理检查防止不支持的 `--command @file` 作为推荐用法回归。
- [x] CLI/package layout focused tests 已覆盖 Windows bin entry、help invocation 和未构建 recovery。
- [x] artifact hints tests 已覆盖 observe/crawl/execute 常见 artifact 的 stable path hints 和不存在路径过滤。
- [x] regression/gov tests 确认现有 jsonPath 仍可读，artifact payload 未被大型 inline 复制。
- [x] `CODE_WIKI.md` 和必要 README/CLI help 已同步 Windows global CLI、dev fallback、artifact hints 和文件参数规则。
- [x] `npm run build`、`npm run build:bridge` 或等价 packaging 验证已通过。
- [x] `mise run affected` 已通过。
- [x] `mise run verify` 已通过，或有明确记录说明无法运行的原因与剩余风险。
