# Checklist

- [x] collection modeling 能识别同一 PageObservation 内重复的 `containerName`。
- [x] 非重复 collection 名称保持不变。
- [x] 重复 collection 名称会生成可区分、简短、用户可读的新名称。
- [x] 消歧逻辑 deterministic，输出不依赖非稳定遍历顺序。
- [x] 消歧 qualifier 来自 bounded structural context 或 deterministic fallback。
- [x] 消歧 qualifier 会经过安全语义文本和 concise-container-label 过滤。
- [x] 长 pricing/model-card preview 不会作为消歧 suffix。
- [x] `collection.containerName` 应用消歧后不再出现同名 `筛选` 冲突。
- [x] list region entity name 与对应 collection 名称保持一致或可通过 hints/evidence 追踪。
- [x] evidence/hints 保留 base name 或 disambiguation reason。
- [x] 新增或更新测试覆盖 duplicate `筛选` collection。
- [x] 新增或更新测试覆盖非重复 label 不被改动。
- [x] 新增或更新测试覆盖无安全上下文 deterministic fallback。
- [x] 新增或更新测试覆盖长 pricing card sample 不作为 suffix。
- [x] no-mode `browser_observe` 仍保持 canonical PageObservation 路径。
- [x] 实际 Krill AI pricing 页面复验中，原本两个 `筛选` collection 已可区分。
- [x] 实际复验中 pricing card preview 仍保留在 evidence/sample。
- [x] 实际复验中 pricing card preview 不进入 `containerName` 或 suffix。
- [x] 实际复验中 `<path`/`<svg` 污染仍为 0 matches。
- [x] 相关 focused tests 已通过。
- [x] `mise run affected` 已通过。
- [x] `mise run verify` 已通过，或有明确记录说明无法运行的原因与剩余风险。
