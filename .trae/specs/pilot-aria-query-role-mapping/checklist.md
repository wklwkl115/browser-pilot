# Checklist

- [x] 已检查 `aria-query` license、browser/MV3 compatibility、package 入口和 bundle size。
- [x] 已比较 `aria-query` 与 `dom-accessibility-api.getRole(el)`，并记录最小可行 role provider 方案。
- [x] 若需要新增依赖，`package.json` 与 lockfile 已更新；若不需要新增依赖，原因已记录。
- [x] scan role provider 通过 `src/scan/*` 构建链路或等价 wrapper 注入页面世界。
- [x] `capture-src/entries/*` 没有直接 import npm package。
- [x] provider 初始化失败、缺失或抛错时 scan 会 fallback 且仍能输出 PageObservation。
- [x] `roleOf(el)` 按 safe explicit role、provider implicit role、legacy fallback 的顺序工作。
- [x] `button`、`input`、`select`、`textarea` 等关键控件 fallback 未退化。
- [x] `<a href>` 被映射为 `link`。
- [x] `<a>` without `href` 不会仅因标签名被提升为 implicit `link`。
- [x] `input type=search`、`input list`、`summary/details`、`img alt=""` 等代表性 implicit role case 有测试覆盖。
- [x] structural landmarks 可产生语义 role，但不会仅因 role provider metadata 被标记为 clickable。
- [x] Browser Pilot clickable、editable、hit-test、visibility 与 event-handler 逻辑仍是 actionability 权威。
- [x] role provider 依赖未进入 `src/kernels/*`、command catalog、bridge protocol 或 generated output 目录。
- [x] scan summary、reference targets、controls_pairs 与 ABML entity role semantics 未发生未预期退化。
- [x] observe regression benchmark 包含 role provider 或 role mapping fallback safety case。
- [x] extension/scan 构建检查已通过，MV3 bundle 可用且体积变化已检查。
- [x] Edge no-mode observe 实测成功，artifact 中 role/actionables 未明显退化。
- [x] `CODE_WIKI.md` 已同步 scan role provider、role mapping pipeline 与依赖边界。
- [x] `.trae/notes/abml-observe-long-term-optimization.md` 已同步 P3 aria-query 线路状态。
- [x] 新增 role provider/scan/benchmark focused tests 已通过。
- [x] `mise run affected` 已通过。
- [x] `mise run verify` 已通过，或有明确记录说明无法运行的原因与剩余风险。
