# Checklist

- [x] 已盘点现有 network start/read/stop、reload/navigation、wait 和 artifact 保存路径。
- [x] 已盘点现有 `browser_artifact`、result `artifact_hints`、saved artifact shape 和 CLI metadata/help。
- [x] 已盘点 bridge/client/command 层 timing 或 diagnostics 字段。
- [x] 已盘点 observe regression benchmark case 格式并选择本轮扩样 archetypes。
- [x] network capture reload 用户入口已实现，且现有 low-level start/read/stop 仍可用。
- [x] capture reload flow 确保 capture 在 reload/navigation 前启动。
- [x] capture reload flow 返回 bounded summary 与 saved artifact path。
- [x] network help/result guidance 明确如何捕获早期 page-load 请求。
- [x] network focused tests 覆盖 start-before-reload 顺序、summary shape、artifact path 和失败恢复。
- [x] artifact inspect/paths 能力已实现并复用 `artifact_hints` 或 saved artifact metadata。
- [x] artifact inspect/paths 输出 kind/schemaVersion、saved path、jsonPaths、preferredReads、compact summary 和 path descriptions。
- [x] artifact inspect/paths 不 inline 大型 raw payload，并处理不存在路径。
- [x] artifact focused tests 覆盖 observe/crawl/execute artifact paths 与 malformed/missing artifact recovery。
- [x] latency telemetry 输出 bounded total/bridge/client/runtime timing 中可用字段，不记录敏感 payload。
- [x] timing diagnostics/telemetry 保持向后兼容，不破坏现有 result shape。
- [x] network capture reload one-shot batch/compound 路径可用，减少手动 round-trip。
- [x] batch/latency tests 覆盖 telemetry shape、字段 bounding、batch 顺序和 observe sensing-only 边界不变。
- [x] observe regression benchmark 新增 docs/article-like fixture。
- [x] observe regression benchmark 新增 dashboard/table/form fixture。
- [x] observe regression benchmark 新增 virtualized list fixture。
- [x] observe regression benchmark 新增 iframe/shadow DOM fixture 或等价离线模型。
- [x] GitHub-like repo/PR fixture 已新增，或已记录暂缓原因和后续扩展点。
- [x] README/CODE_WIKI 或 CLI help 已同步 network capture reload、artifact inspect/paths、latency telemetry 和 benchmark 扩样维护方式。
- [x] governance tests 防止只推荐易漏早期请求的手动 network flow。
- [x] artifact path guidance 不推荐不存在 JSON 路径。
- [x] network/artifact/latency/benchmark focused tests 已通过。
- [x] `mise run affected` 已通过。
- [x] `mise run verify` 已通过，或有明确记录说明无法运行的原因与剩余风险。
