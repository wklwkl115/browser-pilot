{
  "schemaVersion": 1,
  "evalId": "21-cross-tool-correlation-chain",
  "status": "passed",
  "toolCallCount": 6,
  "firstWrongToolChoice": null,
  "recoveredAfterFailure": false,
  "artifactSufficiency": "sufficient",
  "scopedFollowUpDiscipline": "passed",
  "evidence": {
    "summary": [
      "browser_observe summary/preview surfaced snapshotId, operationId, sourceMode, and selectionVersion evidence before any raw artifact read.",
      "browser_execute and browser_wait preserved operationId / waitId / selectionVersion* so the state-change chain stayed attributable across tools.",
      "browser_artifact followed correlation jsonPaths such as operation.operationId, snapshot.snapshotId, and data.waitId instead of opening the full artifact blindly."
    ],
    "artifacts": [
      ".pi/browser-artifacts/correlation-chain-observe.json",
      ".pi/browser-artifacts/correlation-chain-wait.json",
      ".pi/browser-artifacts/correlation-chain-artifact.json"
    ],
    "diagnostics": [
      "The workflow stayed inside existing canonical tools; no orchestration layer or new public browser tool was required.",
      "selectionVersionAtDispatch/Resolve remained available for target-drift diagnosis.",
      "Artifact-side correlationPaths were sufficient to guide targeted jsonPath reads."
    ]
  },
  "notes": [
    "This is a hand-run/static result-style record used to freeze expected evidence shape for the cross-tool correlation workflow.",
    "The main success criterion is not more tool power; it is lower evidence-navigation cost inside the existing tool surface."
  ]
}
