# Blind-agent eval findings (rolling, triaged)

Product of the `pi-browser-blind-eval` skill. Each finding is triaged `fixable | WAI | reliability`
per `eval-friction-triage-perception-vs-execution`. `runs` = how many independent blind runs (different
agent + task) surfaced it; treat `runs ≥ 2` as confirmed, `runs = 1` as a hypothesis to re-test before
acting. WAI items are recorded for transparency but are **not** work items.

**Before fixing anything**, a finding must be a TRUE, GENERAL project defect — confirmed root cause that
generalizes, no change-for-change, no overfit to the site/DOM/task/shape that surfaced it (fix +
regression stay general). If it can't clear that bar, leave it here as "needs more runs", don't patch.
See `eval-fixes-true-defect-no-overfit`.

> **Protocol (corrected 2026-06-06, see `blind-eval-protocol-realsite-skill-china`):** blind eval now
> runs on REAL, mainland-China-reachable sites (READ-ONLY) with the agent READING the
> `pi-browser-tools` skill. The first two runs below predate that and are **legacy** (no skill,
> `--help`-only, local/ad-hoc fixtures) — re-validate their findings under the corrected protocol; some
> (e.g. F3) are likely skill-mitigated. The execute-return-value finding (F1) recurs under BOTH.
>
> Runs: R1 = login→orders (ad-hoc fixture, legacy); R2 = spec 02 scan-execute-wait (interactive.html,
> legacy); **R3 = linux.do top-topics (REAL site, skill-guided)** — canonical corrected run.

## Pending live verification (next round)

These landed with unit/contract proof but want live confirmation. **Both need a daemon restart first**
(Node `src/` changes don't hot-reload — `src-reload-requires-process-restart`); the running operator
daemon predates them.

- **C2 (`browser_tabs` universal params) — ✅ LIVE-VERIFIED 2026-06-09.** Daemon restarted (pid 12076),
  `pi-browser tabs --action list --json` returned clean tab list, no `Validation failed` error.
  (Note: `--max-chars` is now CLI-internal per perf audit; Pi-native tool-level acceptance verified by
  `check-tools-contract` in quality:local.)
- **C1 (`http_replay` CSRF reflection) — operator-driven (blind is READ-ONLY and cannot log in).**
  Recipe: (1) be logged into a site using double-submit CSRF (XSRF-TOKEN cookie ↔ `X-*-CSRF` header)
  and open its tab; (2) restart the daemon; (3) capture an authenticated request (`browser_network`)
  or know an endpoint, then `browser_http_replay url=<endpoint> method=POST bindBrowserSession:true`
  **without manually adding the CSRF header**. **Pass:** `summary.request.csrfReflected:{cookie,header}`
  is present and the response is 200 (not 403 "missing csrf token"); a control run with
  `reflectCsrf:false` should reproduce the 403. Real-session evidence that motivated it is in the C1
  entry below.

## fixable (work items)

| # | finding | runs | evidence | candidate fix |
|---|---------|------|----------|---------------|
| F1 | **`browser_execute` does not return your script's value inline — even small values**: nested arrays/objects collapse to `{type,count/keyCount}` shape placeholders and strings truncate (~280 chars) under default `detailLevel`, forcing a second `browser_artifact` round-trip to read data the agent just computed. | **3 — CONFIRMED, survives the skill** (R1 nested-array collapse; R2 280-char truncation; R3 <2KB `{count,rows}` → `keyCount:4` placeholders) | R1: rows→cells = `{type:"array",count:3}`. R2: `originalLength:280, truncated:true`, buried in `summary.data.value`. R3: small return collapsed to `{type:"object",keyCount:4}`, needed `artifact --json-path data.rows`. R3 agent (skill-guided): "pure overhead for the most common extract-a-few-values use case — the only real inefficiency I hit." | **✅ RESOLVED 2026-06-06** — see "Resolved" below. |
| F2 | **Envelope verbosity drowns the answer**: `observe`/`execute` wrap a tiny result in a large envelope (privacy/operation/correlation/nextActions + `[Circular]`/`{type,keyCount}` placeholders). `observe mode:text` was ~34KB JSON for one fact; useful fields (`summary.data.value`, `textPreview`, `entities[]`) are buried. | 2 (R1 observe-scan noise; R2 34KB text envelope) | R1: 9-node page → 75KB scan artifact, `headings:"[Circular]"`. R2: 34KB for "Status: activated". | **✅ RESOLVED 2026-06-06** (dominant bloat) — see "Resolved"; one sub-item left by design. |
| F3 | **`tabs --json` without `--action` errors with raw schema noise**: `Invalid parameters — /: must have required properties action`. Top-level `--help` lists `tabs` but doesn't signal `--action` is mandatory; the `/:` is raw validator output. | 1 (R2, legacy) — **likely skill-mitigated**: R3 (skill-guided) did NOT hit this. | R2 cmd #4. | **✅ RESOLVED 2026-06-09** — see "Resolved" below. |
| F4 | **`wait --params` JSON shape is undocumented**: `--help` shows `--params <json>` with no schema; the agent guessed `{selector,timeoutMs}` / `{url,state}` and got lucky. | 2 (R1, R3) — survives the skill | R1 cmd #5-6; R3 "`wait --help` does NOT document the shape of `--params`… guessed correctly from convention." | **✅ RESOLVED 2026-06-06** — see "Resolved" below. |
| N1 | **network result `artifact_hints`/`nextActions` jsonPath omits the `data.` prefix**: the saved artifact is a bridgeResult (`{id,tabId,data,…}`) with entries under `data.items[…]`, but the network summarizer emits `entriesPath = "items"` → hints like `read_saved_artifact mode=json jsonPath=items[1].request` resolve to `notFound`; the working path is `data.items[1].request`. Scan correctly uses `data.content`; network is inconsistent. | 1 (R6 network) — **CODE-CONFIRMED** | Agent N: `items[1].request` → `notFound:true`; `data.items[1].request` worked. | **✅ RESOLVED 2026-06-06** — see "Resolved". |
| M1 | **`browser_observe --baseline` has no by-reference input → the headline treeDiff/diff/causal workflow is unreachable via the CLI for real pages**: schema is `Union([Array, Object])` only — a `snapshotId` string is REJECTED (contradicts the skill, which lists "…/snapshotId"), and the only accepted forms (full prior entity list / envelope, tens–hundreds of KB) blow the OS argv limit (`ENAMETOOLONG`) when passed inline on the CLI. There is `--output-path` to WRITE a baseline but no `--baseline-path`/`--baseline-snapshot-id` to feed one. | 1 (R6 causal/treeDiff) — **CODE-CONFIRMED** (`registerObserveTool.ts` baseline `Union([Array,Object])`, no string/ref) | Agent M: snapshotId + `{snapshotId}` + trimmed summary + 29-entity array all `anyOf` rejected; real `abml.entities` (~690KB) → `ENAMETOOLONG`. treeDiff/causal never obtained via CLI. | **✅ RESOLVED 2026-06-06** — see "Resolved". |
| E1 | **`browser_evidence` artifact hint jsonPath omits the `data.` prefix** (SAME CLASS as N1, different tool): the saved evidence artifact is the bridge result `{id,tabId,data,…}` with per-source data at `data.sources`, but the summarizer emits `jsonPath: "sources"` → `read_saved_artifact jsonPath=sources` resolves to `notFound`. | 1 (R7 evidence) — **CODE+LIVE-CONFIRMED** | Agent E: `sources` → `notFound` (file found, `bytes:107879`); `data.sources` worked. With N1 this is **n=2 of the class** (bridge-wrapped tools' hints must be data-rooted; web-security tools' artifacts are flat → bare paths correct, e.g. domFlow detects `wrapped`). | **✅ RESOLVED 2026-06-06** — see "Resolved". |
| H3 | **`browser_hook collect` buries the events + emits a useless artifact hint**: collect has NO dedicated distiller, so events collapse to `{type:object,keyCount}` stubs even at `--detail-level full`, and `nextActions` falls back to `read_saved_artifact jsonPath=operation.operationId` (an op id, not the events). The real data is at `data.events`, derivable only from the summary's own `keys` list. | 1 (R7 hook) | Agent H: "collect returns events collapsed to opaque keyCount stubs… the headline result isn't in the headline"; correct path `data.events` not hinted. | **✅ RESOLVED 2026-06-06** — see "Resolved". |
| B1 | **Real-site visible headline/card/list extraction used to fall back to custom `browser_execute` JS**: across news, video, and article-card sites, `observe --mode text` often returned empty `textPreview` / low-signal text or a large scan/ABML envelope, while `scan` exposed entities/templates but not a compact table of current viewport headline/link rows. | **10 — CONFIRMED historically; now RESOLVED by D1** | Historical 10-site batch artifact `.pi/browser-artifacts/eval-blind/real-site-10-run-summary.json`; post-fix sentinel: `browser_observe mode=scan` on `https://linux.do/latest` saved `C:\Users\HUAWEI\.pi\browser-artifacts\observe-scan-1781098809184.json`, and `browser_artifact mode=json jsonPath=data.rows` returned 14 DOM-ordered viewport-visible rows with `text/href/sameOrigin/rect/containerHint/selector`, without custom JS. | **✅ RESOLVED 2026-06-10** — scan now exposes a bounded generic viewport-visible text/link row projection (`data.rows` + compact summary table when budget permits). Hard boundary preserved: text/href/geometry/visibility/container hints only; no semantic headline/card/source/time/uploader inference, no ranking semantics. |
| B2 | **Non-trivial inline `browser_execute` scripts are CLI-fragile**: several blind agents needed long DOM extraction scripts; shell quoting, CSS selector quoting, or JS node-shape mistakes caused retries. | **5 — CONFIRMED ergonomics issue** | Bilibili shell quote EOF; IT之家 CSS selector quoting error; 36kr shell quote / JS empty-node retries; multiple runs requested a script-file/heredoc-friendly path. | **✅ RESOLVED 2026-06-06** — CLI-only `execute --script-file <path>`; API schema unchanged. |
| B3 | **Artifact read path discovery still costs extra turns**: agents repeatedly guessed wrong JSON paths (`data.data.*`, wrong `pick` shape) before reading the artifact root and discovering `data.*`. | **4 — CONFIRMED discoverability issue** | Bilibili `--pick` confusion; IT之家 `data.data.links` miss; Sohu `data.data` miss; 36kr jsonPath miss. | **✅ RESOLVED 2026-06-06** — artifact help clarifies `data.*` roots and repeated `--pick`; execute large-result artifacts now hint `data` / `data.<key>` reads. |
| B4 | **Artifact JSON access to long string fields is awkward**: `observe mode=text/content` saves primary text in string fields such as `data.content` or `data.markdown`; `browser_artifact --mode json --json-path data.content` often returns a preview/truncated wrapper, with no obvious string offset/read-full path. Agents switched to `search --context-chars` or custom JS for text already captured. | **2 — CONFIRMED in second 10-site batch** | Weather.com.cn: `data.content` originalLength ~2857 still preview/truncated; People.com.cn article: `data.markdown` existed but was preview/truncated, `data.text` absent, agent used search contexts. | **✅ RESOLVED 2026-06-06** — scalar string hits for `jsonPath` now support `offset`/`limit` character windows with `originalLength`/`nextOffset`. |
| B5 | **`wait` selector-timeout diagnostics are recoverable but not selector-miss-specific**: `WAIT_TIMEOUT` includes useful waitId/lease/CDP cleanup, but does not give a copyable diagnose command; `diagnose {waitId}` reports global tab/bridge state rather than original selector, match count, iframe candidates, or similar selector/text hints. | **2 — CONFIRMED across batches** | R1/R3 already noted wait params shape; second batch MOE selector miss: timeout returned waitId/attempts/historyLost, diagnose showed status/cleanup but not selector-specific recovery. News.cn also guessed optional wait params (`waitUntil`, `idleMs`). | **✅ RESOLVED 2026-06-06** — (1) timeout supervisor surfaces selector/state/main-document zero-match + recovery commands; (2) `wait.diagnose {waitId}` (or `{selector}`) now recovers the recent selector wait (active OR terminal, stored on every `finishPiBrowserWait`) and runs ONE bounded main-document selector probe → `selectorDiagnostics` {selector, matchCount, visibleCount, firstMatch/sampleMatches, expectedState, lastState, recoveryCommands} + `recentWaits` (last 20 terminal waits) + existing iframe diagnostics. Fuzzy "similar-selector" text suggestion remains an optional future (low value). |
| B6 | **Link entities/actionables omit normalized `href`/origin classification**: scan identifies link controls and selectors, but not resolved link URLs or same-origin/external status. Link inventory tasks still require custom JS. | **2 — CONFIRMED** | MCT run: scan actionables had text/selector/role but not href; agent used JS to classify same-origin links. Stats.gov.cn B6 run `7666c51a`: actionables/list_hints exposed labels/selectors/geometry only; agent used execute fallback for 8 HTTP(S) links with same-origin classification. | **✅ RESOLVED 2026-06-06** — link actionables now carry resolved `href` + mechanical `sameOrigin`; no semantic ranking/extraction. See "Resolved". |
| B7 | **Cookie analyze browser-session summary lacks browser cookie attributes**: the compact table covers token-analysis fields (`kind`, length, decoding counts) but omits redacted browser-cookie metadata such as `domain`, `path`, `secure`, `httpOnly`, `sameSite`, and expiry/session. | **2 — PARTIAL CONFIRMED** | Baidu run: cookie names/kinds visible but not domain/path/security posture. Sogou B7 run `6fe09b3d`: summary table present and useful for count/name/kind/length, but domain/path/security/expiry absent. | **✅ RESOLVED 2026-06-07** — see "Resolved" below. |
| B8 | **Hook/evidence/performance summaries need compact passive evidence samples**: summaries show counts/status, but not enough safe event/resource detail, so agents read artifacts for basic samples. | **2 — CONFIRMED** | QQ hook run: evidence useful but required artifact reads for samples. Mi.com B8 run `e02baace`: hook collect `detail:null`, performance samples were `{type:"object", keyCount:8}`, evidence aggregate omitted event/resource sample rows. | **✅ RESOLVED (scoped) 2026-06-06** — standalone `browser_hook getPerformanceEntries` now surfaces initiatorType counts + a compact resource/ms sample + `data.entries` hint (was generic `{type,keyCount}` collapse). The multi-source evidence AGGREGATE deliberately stays counts-only (adding samples there blew the token-economy budget +106%; its `data.sources` hint drills in). hook `detail:null` for lifecycle/snapshot events is WAI. See "Resolved". |
| B9 | **Download/media discoverability lacked visible media candidate list and final MIME validation**: observe exposed media refs/templates but not a directly usable candidate table; download could complete a non-image response for an image candidate without warning. | **2 — CONFIRMED, RESOLVED** | ChinaDaily run: JS extracted visible images before direct URL download. Huanqiu B9 run `710c4d04`: observe had media refs/templates only; JS fallback found selector/currentSrc/alt/headline/dimensions; both url/media downloads completed as `text/html` anti-bot pages, not images. | **B9b ✅ RESOLVED 2026-06-06**: `browser_download` now takes `expectMime` (media mode defaults to `image`) and flags a `mimeMismatch` diagnostic when the completed download's MIME doesn't match. **B9a ✅ RESOLVED 2026-06-10**: scan now emits bounded `data.media_candidates` plus compact summary/artifact hints for visible media identity (`selector/src/poster/alt/dims/geometry/sameOrigin`) only; no associated headline/ranking semantics. |
| B10 | **Artifact CLI/help remains mildly inconsistent with tool-style nextActions**: B3 made roots and `--pick` clearer, but nextActions still use `read_saved_artifact ... jsonPath=...` tool notation rather than copyable CLI; camelCase `--jsonPath` and generic `--detail-level` produce accepted-list errors without did-you-mean or artifact-specific guidance. | **2 — PARTIAL CONFIRMED** | ChinaDaily used `--jsonPath` once and failed; MCT tried `artifact --detail-level` and failed. CMA B10 run `cbba5138`: `--json-path` is discoverable after reading CLI docs; `--jsonPath` and `--detail-level` still fail with accepted-list only; nextActions need manual mapping. | **✅ RESOLVED 2026-06-06** — `parseArgs` now suggests the closest valid flag (normalized camelCase↔kebab match, then edit-distance ≤2): `--jsonPath` → "did you mean --json-path?", `--max-char` → "--max-chars". A common-but-absent flag like `--detail-level` gets a targeted hint ("size output with --limit / --offset / --max-chars"). Copyable-CLI-in-nextActions deferred (frontend-agnostic notation). See "Resolved". |

### Minor — ✅ both resolved 2026-06-06
- **screenshot metadata lacks pixel width/height** (R7 Agent E) — FIXED: `registerScreenshotTool` now decodes width/height from the captured image (PNG IHDR / JPEG SOF) into the summary; returns undefined rather than guessing. Live: `data.width:2058, data.height:1514`. Unit test `tests/unit/tools/screenshot-dimensions.test.ts`.
- **`browser_hook installTargets` `--params {targets}` undocumented** (R7 Agent H) — FIXED: verified `hook.install_targets` rejects a no-targets call (`hook.ts:137`), so marked `required:["targets"]` in `native_command_schema.json`; F4's per-action help now shows `installTargets requires targets`.

- **B11 — sidebar/overlay controls dominated `primary_actions` on heavy SPAs (resolved 2026-06-10).** The bilibili run showed fixed palette buttons (watchlater/PIP/scroll-to-top) occupying the primary action list while search/nav/cards were absent. Fix: scan now mechanically flags fixed/sticky small edge controls as `edgeUtility`, and the scan summary ranking penalizes those only for `focus.primary_actions`; the controls remain available in full `data.actionables`. Regression: `check-summaries` locks that a main search action outranks high-priority edge utility controls while the sidebar action is still present in the actionables table.

## Closed — not a tool bug

| # | finding | resolution |
|---|---------|------------|
| M2 / Rel1 | `browser_execute` channel hang — every execute (even `1+1`) `BRIDGE_TIMEOUT` while observe/network/command/wait work. | **HARNESS ARTIFACT, NOT A TOOL BUG (confirmed 2026-06-06).** R6 ran in a browser polluted by 12 lingering isolated-Edge processes from prior runs (teardown bug below). After fixing teardown, a clean-browser repro had `browser_execute` working immediately (`1+1 → 2`). Rel1 is merged into M2 and closed. |

## Harness notes (eval infra, not tool findings)

- **Memory kernel M5 acceptance — PASSED WITH ONE TIGHTENING 2026-06-11.** First attempted
  real-site target `https://linux.do/latest` was externally unavailable: the blind agent saw
  Cloudflare `502 Bad gateway` on the page and on `/latest.json`, so it was recorded as an
  invalid site-state run, not a tool failure. The valid READ-ONLY paired run used an isolated
  real `https://www.bilibili.com/` stage and child agents operating only through
  `pb-blind.mjs`, CLI `--help`, JSON outputs, and the `pi-browser-tools` skill. **Pair 1:**
  T1-cold extracted five visible Bilibili video/card titles and URLs with memory adoption all
  false; after the operator recorded a SOP from the successful T1, T1-warm surfaced
  `envelope.memory` (`browser-memory://sop/sop_20260611063905_9d25270d`), used the inline body,
  attempted read-through, and reported `usedInFinalAnswer:true`. `recordNudgeShown:false` and
  `recordCalled:false` stayed false in all blind runs, confirming the nudge is still not adopted
  by blind agents. **Pair 2:** T2-warm (different task: visible navigation/search affordances)
  succeeded; it saw the T1 card collapsed, did not read it, and did not use it in the answer.
  A first memory-off control was invalid because setting `PI_BROWSER_MEMORY=0` on individual CLI
  calls does not affect an already-running memory-on daemon; this exposed a harness boundary. The
  corrected control launched a fresh isolated daemon/browser with `PI_BROWSER_MEMORY=0`, produced
  no memory plane, and completed T2 successfully in roughly 10 wrapper invocations with
  `effect.mutations:0`. Operator decision: memory adoption is proven for relevant T1 warm; T2
  showed no success harm, but the irrelevant same-origin collapsed card was too eager, so M3c was
  tightened after the run to require current observe URL/intent token overlap before automatic
  `envelope.memory` injection. The tightening is locked by `npm run check:memory-plane`, including
  wrong-origin/no-agreement, stale-strike body suppression, and common-token flood negative
  controls.

- **Execution feedback adoption gate — SPLIT RESULT 2026-06-11.** Two implementation-blind
  subagents worked against the isolated local fixture stage
  (`.pi/browser-artifacts/eval-blind/stage.json`, fixture base `http://127.0.0.1:8296`) using only
  the `pi-browser-tools` skill, `--help`, JSON output, and `pb-blind.mjs`. **Form-fill fixture
  (`execution-feedback-form.html`) succeeded but did NOT adopt `pi.resolve` / `pi.setValue`:** the
  agent observed `#full-name`, wrote selector-based `browser_execute` JS, and verified
  `Status: filled:Pi Agent`. This fails Track C's public-guidance adoption bar; `pi.*` stays
  internal/underdocumented per `docs/archive/execution-feedback-layer-plan.full.md`. **Canvas fixture
  (`execution-feedback-canvas.html`) succeeded and DID adopt `input.pointer`:** the agent measured
  the blue rectangle with JS, then used `browser_command --command '{"cmd":"input.pointer",
  "gesture":"press","x":138.5,"y":159.3}'` and verified `Status: target hit`, without raw CDP
  `Input.dispatchMouseEvent` expansion. This supports public guidance for Track B physical input.

- **Scan focus entity-ref shape evidence — ✅ PASSED and landed 2026-06-08.** Two implementation-blind
  subagents worked against the isolated real `https://linux.do/` stage using only the
  `pi-browser-tools` skill, `--help`, JSON output, and `pb-blind.mjs` for a read-only "top 5 latest
  topics + reply counts" task. Both completed successfully and explicitly reported that they did
  **not** use `summary.focus.primary_actions[].entity`, `summary.focus.primary_entities`, or
  `summary.focus.referenced_entities`; their working path was scan structure/outline, saved artifact
  `data.list_hints`, and a final read-only DOM extraction for ordered row values. This supplied the
  required no-overfit evidence for changing scan `focus` entity projections from duplicated full
  objects to refs. The main unrelated friction was `data.list_hints` grouping/sampling rather than
  preserving exact visible DOM order, so simple top-N row extraction still falls back to custom JS.
  Stage was torn down cleanly (`browser pid 42792`, daemon pid `37908` killed; blind temp dirs removed).
- **CLI natural routing spike wait/network blind evidence — ✅ PASSED 2026-06-08.** Two implementation-blind subagents worked against the isolated real `https://linux.do/` stage using only the `pi-browser-tools` skill, `--help`, JSON output, and `pb-blind.mjs`. **Wait/content run:** completed the top-5 topic extraction, naturally used `wait selector --selector ...`, did not use legacy `wait --action selector --params ...`, and reported no CLI schema/usage errors; only friction was broad DOM selector choice and verbose success envelopes. **Network run:** completed a read-only network evidence task, naturally used `network status`, `network start`, `network list`, `network export-har`, `network stop`, and `wait network-idle`; it captured `total:373` requests, exported HAR evidence, and did not use or prefer legacy `--action` / `--params`. Skill↔tool fidelity was good in both runs. Residual non-blocking papercuts: successful output verbosity, `network list`'s `total` vs `entryCount` naming, and help not spelling out that `export-har` saves HAR under a JSON artifact path such as `data.log.entries`. These do not block the wait/network natural routing feasibility decision; they are candidate follow-up ergonomics items if they recur.
- **CLI natural routing spike frame/hook blind evidence — ✅ PASSED with fixes 2026-06-08.** Two implementation-blind subagents used only `pi-browser-tools` skill, `--help`, JSON output, and `pb-blind.mjs` against the isolated real `https://linux.do/` stage. **Frame run:** naturally used `frame list` and `frame evaluate` (no legacy `--action/--params`) to enumerate 2 frames and read both the main frame and an `about:blank` child frame; artifacts included `.pi/browser-artifacts/frame-result-1780907447876.json`, `frame-result-1780907464910.json`, and `frame-result-1780907465035.json`. **Hook run:** naturally used `hook list-targets`, `install-targets`, `status`, `list-sessions`, `collect`, `uninstall`, and `performance`; it installed the safe `error` target, collected hook lifecycle events, then cleaned up to `list-sessions count:0`, with artifacts `.pi/browser-artifacts/hook-result-1780907515452.json` and `hook-result-1780907531366.json`. Both runs proved the high-frequency natural routes are usable. Two real agent frictions were fixed immediately: bare `schema frame --json` / `schema hook --json` now include `subcommands[]` with natural route metadata instead of forcing agents to parse help, and natural `hook install-targets --targets` now accepts both `console,error` and repeated `--targets console --targets error` while docs/skill use the real target id `error` (not `errors`). Regression: CLI local/flag tests cover schema subcommands and target parsing; blind stage was torn down cleanly.
- **`launch-blind.mjs` false-failed before ready stage during the wait/network natural CLI spike — ✅ FIXED 2026-06-08.** Real-site launch against `https://linux.do/` started the isolated daemon and Edge, then reported `no tab connected to the isolated daemon`; root cause was launcher parsing `tabs --action list --json` as `summary.tabs` / `tabs` only while the current CLI returns the list under `{value:[...]}`. The harness now accepts `parsed.value`, writes partial `stage.json` states (`daemon-started`, `browser-started`) before readiness, writes `launch-failed` on errors, and `teardown-blind.mjs` reports the cleaned `stageState`. Validation: a failed partial stage could be torn down, and a fresh real linux.do stage reached `ready`; operator smoke against that stage confirmed natural `wait selector`, `wait network-idle`, `network start/list/export-har/status` routes execute through `pb-blind.mjs`. Regression: `check-eval-workflows` now asserts the partial-stage and `parsed.value` contracts. The blind-agent prompt and `pi-browser-blind-eval` skill now require a dedicated CLI ROUTING ADOPTION section recording natural subcommand vs legacy `--action`/`--params` use; true implementation-blind evidence for the wait/network spike is recorded above.
- **`teardown-blind.mjs` left lingering isolated Edge + locked temp dirs — ✅ FIXED 2026-06-06.** `taskkill /PID <browserPid> /T /F` killed only the tracked launch pid's tree, but the detached Edge re-parents its renderer/GPU/utility processes, so they survived — accumulating stale browsers (12 found) + locking `.pi/temp-profiles/blind-*` (silent rm failure). Dirty reuse then polluted eval signal (caused M2). Fix: `teardown-blind.mjs` now also kills every `msedge.exe`/`chrome.exe` whose command line references THIS run's unique profile dir (PowerShell CIM match), waits 800ms for handle release, then removes temp dirs. Self-tested: 0 lingering Edge after teardown (was 12).

## Needs more runs (n=1, hypothesis)

| # | finding | runs | evidence | status |
|---|---------|------|----------|--------|
| G12 | **`record candidate:` nudge is structurally unreachable from `browser_execute`** (`recordNudgeShown:false` in all M5 runs): the execute distiller builds its summary from `summarizeGenericValue` + operation/effect fields with NO `url`, and sets no `snapshot` — so `pageContext()` (`autoSurface.ts:77-89`) finds no origin on execute envelopes and the nudge can never fire from the most common "did something on a page" tool. The strategic write-ignition arc currently depends on operator action. | 1 (M5 bilibili pairs) — **CODE-CONFIRMED** (`registerExecuteTool.ts:195-222` distiller has no url field; `autoSurface.ts:113-114` returns early without origin) | All M5 blind runs: `recordNudgeShown:false`, `recordCalled:false`; operator recorded the T1 SOP manually. | candidate fix with independent value: execute envelope `target` should carry the acting tab's page url (execute results currently have zero page context); that makes the nudge reachable, and ONLY then can D6's nudge-removal clause be adjudicated with real evidence (shown-but-unused ⇒ delete; shown-and-used ⇒ keep) |

## Resolved

- **G11 — simple-token array CLI flags did not accept comma-separated values (2026-06-09, dogfooding the `pi-browser` CLI; LOW, accept-natural-aliases class).** `cookie-analyze --secret-candidates "a,b,c"` silently parsed as ONE candidate (verified: `testedSecretCandidateCount:1`). Root cause: `cli/flags.ts` only comma-splits an array flag when its FlagSpec carries `split:"comma"`, and only `hook installTargets --targets` had it (a prior per-hit fix). Fix: `buildCommandFlagSpecs` now sets `split:"comma"` for a curated allowlist of array flags whose values are simple tokens that never contain a comma (secretCandidates/secrets/wordlist/words/paths/urls/templates/templateIds/templatePaths/workflowPaths/paramNames/hosts/tags/excludeTags/severities/authors/schemes/ports/knownFiles/probeTypes) — an ALLOWLIST not a denylist, so a missed flag just keeps the old behavior rather than corrupting a comma-containing value (headers/values/jsonValues/mutations/*payloads/raw* are excluded). Repeated flags and `@file`/`-` still work. Regression: `flags-render.test.ts` (+1); live-verified `--secret-candidates "a,b,c,d,e"` → 5 candidates.

- **T1 — async `browser_execute` on a BACKGROUND tab stalled into `BRIDGE_TIMEOUT` (timer throttling) (2026-06-09, discovered dogfooding the `pi-browser` CLI on a real reversing task; MEDIUM, execute reliability).** When the user switched away (target tab no longer foreground), an async script (`await new Promise(r=>setTimeout(...))`, fetch-with-AbortController, or a poll loop) on that tab hit a 15s `BRIDGE_TIMEOUT` ("ACK received, script may still be running"); a synchronous `1+1` on the same tab succeeded. **Root cause:** browsers intentionally throttle background-tab page timers, so the MAIN-world `chrome.scripting.executeScript` async script never resolves. (Also explains the earlier Turnstile-polling `BRIDGE_TIMEOUT`×4.) **Investigated the real levers (not a hint):** launch flag `--disable-background-timer-throttling` needs a browser relaunch and conflicts with the daily-browser-attach model; plain chrome.debugger attach does NOT lift it (verified); CDP `Emulation.setFocusEmulationEnabled({enabled:true})` makes a single tab emulate foreground and DOES lift it (verified `setTimeout(800)→809ms`) without stealing the user's real focus, relaunching, or touching global policy. **Fix (`bridge_src/service_worker/exec.ts`):** (1) when the target tab is `active===false`, route straight to CDP (skip the throttling executeScript, which also avoids double-executing a side-effecting script); (2) enable `Emulation.setFocusEmulationEnabled` before the CDP `Runtime.evaluate`. Cost: background-tab execs attach chrome.debugger → a "being debugged" infobar (foreground tabs unaffected). Regression: `check-pi-browser-bridge.mjs` sandbox behavior test (background tab skips executeScript; focus emulation precedes eval; result returned); live-verified post-reload (setTimeout/fetch+AbortController/poll all complete on a backgrounded tab). A merely-diagnostic "switch the tab foreground" hint was prototyped and **rejected** — the user wanted the throttle actually lifted, not worked around.

- **A1 — large `mode=json` artifact reads were text-truncated into UNPARSEABLE JSON (2026-06-09, discovered dogfooding the `pi-browser` CLI on a real reversing task, not a blind run; MEDIUM, artifact-read ergonomics class — same family as N1/B3/B4).** `browser_artifact mode=json jsonPath=data.items` — the exact read a network/evidence result's own `nextActions` suggest — returned content that fails `JSON.parse` on any capture with enough requests. **Root cause:** `compactJsonValue` windows by item-count + 800-char string previews (valid), but the result then goes through `jsonResult`→`jsonPreview = truncateText(stableJson(value))` — the TEXT head/tail truncator — which cuts mid-JSON-token and inserts a literal `[truncated N chars]` marker; the CLI then can't parse it and falls back to a `{message}` envelope (inconsistent with small reads). Both frontends affected (Pi-native also gets the truncated `content[0].text`). **Fix (contained to `inlineJsonToolResult`, used only by browser_artifact + browser_screenshot — NOT the shared truncator):** new `fitInlineJsonToBudget` structurally shrinks the windowed array `items` / object keys (binary search for the largest fitting prefix; preserves `count`, sets `nextOffset`/`budgetTrimmed`/`truncatedKeys` for paging), and the serialize budget is floored at the fitted size so the text truncator never cuts → output is ALWAYS valid JSON. Small reads + screenshot untouched. Regression: `tests/unit/tools/inline-json-budget.test.ts` (+4); live-verified `data.items` now parses with a windowed `items` + `nextOffset`. Note: network capture items serialize ~7KB each (full headers + 800-char body preview), so they barely fit the ~8KB artifact budget → the full-items read returns ~1 item/page; this is budget characteristic, not a defect (the network summary's samples/counts give the overview, and narrowing the jsonPath — e.g. `data.items[N].request.url` — is the intended path).

- **W1 — `wait.loadState {state:"networkidle"}` reroute was extension-only, so a STALE loaded extension reintroduced the hard `INVALID_RULE` (2026-06-09, mined from a REAL CTF web-sec session `sessions/2026-06-09T02-37-51…jsonl`, not a blind run; MEDIUM).** The bilibili reversing session (205 tool calls — execute ×58, network ×29, artifact ×18) hit `browser_wait {action:"loadState", params:{state:"networkidle"}}` → `INVALID_RULE: wait.loadState unsupported state` **3 times**. The loadState+networkidle reroute has shipped since 2026-06-05 — but **only in the bridge service worker** (`bridge_src/service_worker/wait.ts`), and the built dist confirmed it was present, so the failure means the agent's *loaded* extension was a stale build predating the fix (consistent with the same-day extension disconnect/reload friction). **A fix that lives only in the extension is defeated by extension version skew.** **Fix:** the Node driver is always current (daemon restart), so the reroute now ALSO runs there — `rerouteLoadStateNetworkIdle` at the `executeBrowserWaitWithSupervisor` entry rewrites a `wait.loadState` whose state resolves to `networkidle` into a `wait.networkIdle` command before dispatch, mirroring the bridge normalization and making it robust to stale extensions (covers Pi-native + CLI). Regression: `check-fake-ws.mjs` asserts the dispatched command becomes `wait.networkIdle` (no `state` key) and resolves. **Two other session signals triaged (per the G2 lesson — find the cause, don't trust the symptom):** (1) `browser_execute BRIDGE_TIMEOUT`×4 — `for(40×){…await sleep}` polls for a Cloudflare Turnstile token, exceeding the 15s bridge window ("ACK received, script may still be running"); this is agent technique (long polls should chunk <15s or use `browser_wait` for the async condition), runs=1, **candidate not work item** per `eval-fixes-true-defect-no-overfit`. (2) `TAB_NOT_FOUND`×5 — Cloudflare challenge pages navigate/reload and rotate the tabId; retryable and the agent recovered, the skill already warns tabId is unstable across navigation. **WAI.**

- **F3 — a required-param miss (e.g. `tabs`/`network` without `--action`) reported raw validator noise instead of a friendly, self-correcting message (2026-06-09, agent-UX governance pass; LOW but general).** `pi-browser tabs --json` → `Invalid parameters — /: must have required properties action` — the `/:` path and "must have required properties" wording force the agent to decode the validator instead of just supplying the value. **Root cause:** `validateToolArgs` (`src/frontend/validation.ts`) already translated the *unknown*-key case (`describeUnknownProperties` → names the key + lists accepted), but the symmetric *missing-required* case fell through to the raw TypeBox error. **Fix (mirrors the existing pattern, shared layer so BOTH frontends benefit):** new `describeMissingRequired(schema, value)` names each missing required key and surfaces its `description` — which for action tools carries the `One of: …` value list — so the message is now `Invalid parameters — missing required parameter "action" (One of: list, snapshot, switch, …)` (and `(start | stop | status | …)` for network). The unknown-key message format is unchanged (preserved precedence). General (any strict schema with required props), schema-derived (not hand-listed), no contract change. Regression: `tests/unit/frontend/validation.test.ts` (+2 — missing-required names the param & surfaces the enum description; multiple missing listed; asserts the raw "must have required propert…" wording is gone). Verified live: `tabs --json` / `network --json` now emit the friendly message.

- **C4 — `browser_memory` was the last hand-rolled tool hard-rejecting the universal output params (2026-06-07, from the "mechanical-choice" design review, not a single session).** A 3-subagent code sweep of the whole param surface found 20/22 tools already accept the universal output params via the `sharedTabScopedToolParams` mixin; the only holdouts were `browser_artifact` (closed by C3) and **`browser_memory`** — its hand-rolled schema had `maxChars` but not `detailLevel`/`redact`, so `browser_memory {action:"recall", detailLevel:"summary"}` would hard-reject with `additional properties`. **Fix:** `browser_memory` now accepts `detailLevel` (no-op — its output shape is fixed by action/mode) and `redact` (threaded through to the result redactor) — completing the C2/C3 class so **every** browser_* tool now accepts the universal output triad (detailLevel/maxChars/redact). Regression: `check-tools-contract` (browser_memory must accept detailLevel/redact — C4). `npm run check` green; tool-contract doc regenerated. **Design conclusion (from the sweep):** a single shared *pre-validation* normalization layer is NOT architecturally possible — the framework validates `additionalProperties:false` BEFORE our `execute()` runs and the contract forbids loosening it — so the mechanical-friction class decomposes into three independently-handled mechanisms, now all systematized: (1) universal-param acceptance = the mixin on every tool (closed by C2/C3/C4); (2) top-level↔`params` placement = the H2 metadata-driven fold (action tools only — the only tools with that shape); (3) enum/value casing+separator tolerance = normalized at each consuming site (~42 already done; the remaining enum hard-rejects are added per real-session hit, NOT mass-built speculatively — Agent C's "agents might pass 'URL'/'Click'" is runs=0, so per `eval-fixes-true-defect-no-overfit` it stays deferred until a real hit, the same cadence that produced the networkidle/htmlMode aliases).

- **H2 — action tools (wait/network/hook/frame) hard-reject per-action keys placed at the TOP LEVEL instead of nested in `params` (2026-06-07, same REAL CTF session as C3; MEDIUM, accept-natural-input class).** `browser_wait {action:"navigate", url:"…"}` → `Validation failed: root: must not have additional properties`; the agent recovered by moving to `params:{url}`, costing a round. **Root cause (code-verified):** `registerNativeActionTool` builds `strictToolParameters({action, params, …shared})` (root additionalProperties:false), so any per-action key at the top level is rejected even though the bridge would read it. **Fix (governance-compliant — stays strict, no `additionalProperties:true`):** each action tool now also declares its per-action REQUIRED keys (derived from the native metadata — the same source F4 surfaces, NOT hand-listed: wait→url/selector, hook→targets/expression/eventType/listenerId/selector, frame→frameId/expression/source/identifier) as optional top-level params, and `execute` folds any that are present into `params` (params still wins if both set). Unknown/typo keys are STILL rejected (strict preserved). General across all 4 action tools, data-driven (not overfit to `url`), and matches the existing accept-natural-input precedents (frame.evaluate `script`→`expression`; G8 `--selector` hint). Functionally verified: top-level `url` accepted, nested `params` still works, bogus top-level key still rejected. Regression: `check-tools-contract` (native action tools must fold top-level per-action keys — H2). `npm run check` green.

- **C3 — `browser_artifact` hard-rejects the universal `detailLevel` param (2026-06-07, mined from a REAL CTF web-security session transcript `sessions/2026-06-07T04-32-05…jsonl`, not a blind run; MEDIUM, same class as C2).** The session (a heavy, clean bilibili charged-video reversing run: browser_execute ×19, browser_artifact ×12, browser_network ×11, browser_http_replay ×7, browser_hook ×4 — exercising the full web-sec stack, WBI signing, session-bound playurl replay) hit a hard `Validation failed for tool "browser_artifact": root: must not have additional properties` on `{path, mode:"json", jsonPath:"data.data", detailLevel:"summary", maxChars, redact}`. **Root cause (code-verified):** `registerArtifactTool` hand-rolls `strictToolParameters({...})` and declares `maxChars`/`redact` but NOT `detailLevel` — so the universal output param agents pass on every other browser_* tool hard-rejects here. Exactly the C2 defect on a different tool. **Fix:** `browser_artifact` now accepts `detailLevel` as a no-op (its output shape is set by `mode`, not detail-leveled — same resolution as C2 for browser_tabs); the value is ignored, not errored. Regression: `check-tools-contract` (browser_artifact schema must include `detailLevel` — H1/C3). `npm run check` green; generated tool-contract doc regenerated. The session had 96 tool results / a handful of hard errors; the others were either the agent's own script semantics (`ok:false`/`JSON.parse` of an HTML error page), a Windows `findstr` encoding error (not our tool), or already-fixed (below).
  - **Two other session frictions triaged (verified in code, per the G2 lesson — don't trust the symptom, find the cause):** (1) `browser_wait {action:"navigate", url:"…"}` → `root: must not have additional properties` → **NOW FIXED as H2 below.** (2) `wait.loadState state:"networkidle"` → `INVALID_RULE: unsupported state` — this is **already fixed in current code**: `wait.ts` `dispatchPiBrowserWait` reroutes a `loadState` with `state:"networkidle"` to `waitForNetworkIdle` (added 2026-06-05 from an earlier real session). The session hit it because the operator daemon was a stale build predating that fix (`src-reload-requires-process-restart`); restart clears it. No code change (same situation as the earlier D1 already-fixed case).

- **G10 — the causal `nextActions` hint under-reports "N requests fired since baseline" by using the capped `requests.length` instead of the true delta count (2026-06-07, R-G5 blind run on linux.do, sonnet, NEW dimension = network/causal plane — zero prior eval coverage; MEDIUM).** The whole causal flow worked end-to-end: the agent did `network start` → `observe scan` (baseline) → read-only `/latest` SPA navigation → `observe scan --baseline-snapshot-id <id>` → read `causal.requests` (12 attributed) + the recorder summary; baseline-by-snapshotId on the CLI worked; `nextActions` correctly pointed at `envelope.causal.requests`. **Real defect found:** the inline `causal.requests` preview is capped at `MAX_CAUSAL_REQUESTS` (12) while `causal.requestCount` carries the true delta total (375 here); the scan hint said `${causal.requests.length} request(s) fired since baseline` → it announced "12 fired" when 375 actually fired — a 30× undercount on any real SPA navigation that fires >12 requests since the baseline. This is also the root of the agent's loudest confusion (it couldn't tell whether the unseen requests were pageable). **Fix:** new pure-core `causalRequestsFiredCount` (`requestCount ?? requests.length`) + `causalFiredHint` in `abml-core/causal.ts`; `observeRunners` now uses the hint, which reports the TRUE count and, when the inline preview is capped, says the full set is in `browser_network list` (it is not pageable from the causal block — a bounded preview by design). General (any baseline observation with >12 since-baseline requests), code-verified. Regression: `abml/causal.test` (`causalRequestsFiredCount`/`causalFiredHint` use the true total, suffix only when truncated, none for unavailable). `npm run check` green.
  - **R-G5 findings triaged as NOT defects / WAI (verified in code, per the G2 lesson):** (1) the agent called `causal.requestCount=375` vs `requests[12]` a "HIGH semantic bug" and guessed a 40-cap — verified: `MAX_CAUSAL_REQUESTS=12`, `requestCount` is the standard "true total vs shown subset" disclosure (only emitted when truncated), not a bug; the only real issue was the hint undercount (G10 above). (2) `artifact --json-path data.causal` → notFound: causal lives at the artifact top level (`causal`) and mirrored under `envelope.causal` (both resolve); `data.causal` was the agent's wrong guess, and the hint already says `envelope.causal.requests`. (3) causal includes a background `message-bus` POST that fired between baseline and action = WAI: causal is the PASSIVE network delta "since baseline," documented as such (not a precise event→request graph; P1 `triggered` is the timing/low-confidence attribution layer). (4) `wait --action load` error doesn't list valid actions — LOW, left as a noted backlog papercut (runs=1; agent recovered via `--help`). Timing: every call returned promptly, zero hang/timeout — execute/observe/network/artifact all solid on this run too.

- **G9 — skill iframe route underspecified: `frame evaluate` params + which field is the `frameId` (2026-06-07, R-G4 blind run on runoob "try it", sonnet, NEW dimension = iframe/`browser_frame` — zero prior eval coverage; LOW).** The frame tool path itself worked cleanly first-try (`frame list` → `frame evaluate`, 7 calls, all <1s, zero reliability issues) — the agent read the child `iframeResult` frame's rendered `<h1>`/`<p>` that a top scan can't see. The only friction: the skill Routes row said only `browser_frame list → browser_frame evaluate` with no indication that `evaluate` needs `frameId`+`expression`, nor which `frame list` field supplies the id, so the agent had to detour to `--help`. **Fix (doc-only, like G4):** the Routes row now spells out `frame list` → read `frames[].frameId` → `frame evaluate --params '{"frameId","expression"}'`, and notes a top scan does NOT structurally cover child frames. Verified against the `frame.evaluate` schema (requires frameId+expression). `npm run check` green; skill re-validated. **Frame/iframe tool path verdict: solid — no capability gap, no reliability issue.**
  - **R-G4 finding triaged as WAI (verified in code, per the G2 lesson):** the agent flagged that top-level `observe mode=text` appends iframe text after a `--- iframe ---` marker as possibly "misleading" (could make an agent think observe covers iframe content). Verified: that marker is emitted ONLY in `mode=text` (`buildScanScript` `collectText` joins same-origin iframe text with the labeled separator) — a deliberate, clearly-labeled convenience (one call gets the iframe's visible text), NOT structured AX coverage. It is honest (the marker names the source) and helpful; the agent still correctly used `frame evaluate` for structured access. No change — adding a nextActions hint would be speculative token cost the project guards against.

- **G8 — `wait --action selector` rejects the natural `--selector` flag with no pointer to `--params` (2026-06-07, R-G3 blind run on bilibili.com, sonnet; LOW).** The agent tried `wait --action selector --selector ".feed-card"` → `unknown flag "--selector"; accepted: --action, --params, …`; the right form is `--action selector --params '{"selector":"…"}'`. Action tools (wait/network/hook/frame) take per-action keys inside an opaque `--params` JSON (the F4 `--help` per-action keys already document this, but the agent reached for a flag first). Same "accept natural aliases, don't hard-reject" class as the prior CLI papercuts ([[tool-ergonomics-accept-natural-aliases]]). **Fix (minimal, uses the existing B10 `ABSENT_FLAG_HINTS` mechanism):** an unknown `--selector` now yields a targeted hint naming the action tools and the `--params` shape, instead of only the accepted-flag list — converting a dead-end into one-step guidance. Scoped to the single CONFIRMED key (`selector`); `--url`/`--state`/etc. are runs=0 and deliberately NOT pre-added (no speculative change, per `eval-fixes-true-defect-no-overfit`). Regression: `flags-render.test` B10 block (`--selector` error mentions `--params`). `npm run check` green. Task itself fully succeeded (5 cards + UP + views + duration + nav). **bilibili/MDN also re-confirmed G2/G7 solid live:** every `execute` returned in 200–400ms, zero hangs across both runs; artifact `mode=search --query` worked cleanly and was clear from the rewritten skill doc.
  - **R-G2/G3 findings triaged as NOT defects (verified in code, per the G2 "don't trust the agent's guess" lesson):** (1) MDN `artifact --mode json --json-path data.headings` → `notFound` while `--mode text` works — NOT a json-mode bug: `resolveInputPath` allows absolute paths, and `data.headings` simply isn't where headings live in that artifact (text mode returns the whole file regardless of path; json mode correctly reports a missing path). (2) `observe --mode scan` summary truncation / `summaryOmitted` / low `node_count` on large pages (bilibili, MDN, github) = WAI: `fitSummaryBudget` (~8KB) intentionally compacts the summary and points to the full saved artifact; the agent's `node_count:12` alongside a 1.87MB artifact is self-contradictory (conflated outputs), not a reliable signal. (3) bilibili heavy-SPA cards not in the first scan's AX tree → recovered via `wait selector` + JS extract = the intended scan-structure / JS-values split (WAI). (4) GitHub plain-text README not rendered as DOM = WAI.

- **G7 — `browser_artifact` silently ignores `query` outside `mode=search` (2026-06-07, R-G2 blind run on github.com/torvalds/linux, sonnet subagent, longer multi-step task; MEDIUM).** The agent (correctly completing all 6 task items) hit two artifact-search frictions: (Friction 4) tried `artifact --search "…"` → `unknown flag` (it is `--mode search --query`, "search" is a mode not a flag); (Friction 6) passed `mode=json --json-path data.markdown --query "Linux kernel"` expecting it to LOCATE the term inside the markdown value, but `query` is a search-mode-only param so it was **silently ignored** and the file head was returned, forcing manual binary-search via `offset` windows. Same artifact-search-discoverability class as last round's G5 (regex skipped on a single 600KB line — but a PLAIN query already windows long lines; the agent reached for `--regex` and got `regexTruncatedLines`). **Root cause (code-verified):** `readBrowserArtifact` already auto-promotes `jsonPath`→`mode=json` (the H2 precedent) but had no symmetric handling for `query`; with `query` present and mode resolved to json/text it just dropped it. **Fix (mirrors H2, two halves):** (1) a bare `query` (no mode, no jsonPath) now auto-promotes to `mode=search` instead of returning text from offset 0; (2) `query` combined with a json/jsonPath read now throws `ARTIFACT_QUERY_REQUIRES_SEARCH_MODE` with factual remediation (use `mode=search` — a plain query windows even one very long line via `contextChars` — or window a known value with `jsonPath`+`columnOffset/columnLimit`), converting a silent no-op into actionable guidance. Skill artifact paragraph rewritten to state `mode=search`+`query` is how you find text (plain query windows long single lines; `regex` only for short lines; `query` is ignored elsewhere). General (the capability to locate text in a huge single-line value already existed — this fixes discoverability + the silent-ignore, not a new feature), matches [[tool-ergonomics-accept-natural-aliases]]. Regression: `artifactReader-advanced` (bare query→search; query+jsonPath→error; plain-text search windows a 600KB single line). `npm run check` green; docs regenerated (new error code). G7 LIVE-confirmed indirectly: G2 fix verified solid on this run — every `execute` call returned in 200–400ms, zero hang/timeout across ~12 calls on a real heavy page.

- **G2 — `browser_execute` hangs to a full BRIDGE_TIMEOUT (with no result) when `chrome.scripting.executeScript` stalls (2026-06-07, R-G blind run on linux.do, skill-guided; HIGH).** The blind agent's standard "scan → JS-extract" flow broke: every `browser_execute` call — even a trivial `document.querySelectorAll('*').length` — returned `BRIDGE_TIMEOUT "ACK received, script may still be running"` after the full timeout, while `browser_observe scan/text/html` on the SAME page succeeded. **Root cause (code-verified, true general defect — not the login-modal the agent guessed):** `browser_execute` runs through `handleWsExec` (`bridge_src/service_worker/exec.ts`), whose primary path is `chrome.scripting.executeScript` (MAIN world) with a persistent-CDP `Runtime.evaluate` fallback — the SAME mechanism `browser_observe` uses reliably. executeScript can hang when the debugger is attached / the renderer never acks the MAIN-world injection on some pages; the fallback exists exactly for this. But `EXECUTE_SCRIPT_TIMEOUT_MS` was set to the **whole caller `timeoutMs`** (`data.timeoutMs ?? 2500`), which also equals the outer client pending-request timeout — so a hung executeScript consumed the entire budget and the proven fallback never ran before the client gave up. The `?? 2500` default (no timeoutMs) was the original fast-fail intent; reusing the large supplied `timeoutMs` for the race silently broke it. **Fix:** the fast-path executeScript is now capped to ≤1/3 of the budget **and** ≤2500ms (`Math.max(100, Math.min(2500, Math.floor(TOTAL_EXEC_TIMEOUT_MS/3)))`), reserving the remaining budget for the CDP fallback (which inherits the full `TOTAL_EXEC_TIMEOUT_MS`). Restores the original fast-fail intent and makes the fallback reachable whenever executeScript stalls. General (any page/site where MAIN-world injection stalls), not overfit. Regression: `check-pi-browser-bridge` `testExecHangingScriptFallsBackToCdpWithinBudget` (a never-resolving executeScript must fall back to the CDP result within the caller budget, and the fallback must inherit the full budget). Needs `npm run build:bridge` (contract reads source; dist rebuilt for runtime). `npm run check` green.

- **G4 — `browser_observe mode=html` `htmlMode` legal values undocumented in the skill (2026-06-07, R-G blind run; LOW).** The agent guessed `--html-mode simplified` and got `flag "--html-mode" must be one of: fragment, raw, text, inner, outer`. The skill's Routes row showed `htmlMode` as a bare token with no enum. **Fix:** the skill Routes row now lists `htmlMode ∈ fragment|raw|text|inner|outer` (CLI `--html-mode`), verified against the `registerObserveTool` schema. Pure discoverability; no API change.

- **G round — other findings triaged (2026-06-07, R-G blind run on linux.do).** G1 (`--load-state networkidle` → correct is `--action networkIdle`; Playwright-habit confusion), G6 (`--regex` is a switch + `--query <pattern>`, not `--regex <pattern>`), and G5 (`artifact --mode search --regex` on a single 600KB+ HTML line) remain n=1 observations only. They are not backlog items after the 2026-06-10 debt-zeroing decision; recurrence must first establish a general defect and then open a fresh execution contract. G3 (`observe scan` returns near-empty entities when an AX modal/login dialog overlays the page) is **WAI**: ABML is AX-grounded and a modal correctly scopes the AX tree to the dialog; the agent's recovery (`observe mode=html`) is the intended path. Task itself succeeded (top-5 topics + reply counts read from the server-rendered HTML).

- **B7 — `browser_cookie_analyze` browser-session cookie attributes (2026-06-07, n=2 real-site blind backlog).** The cookie provider contract now returns a structured provider result `{header,cookies,url}` instead of only a flattened Cookie header; HTTP replay/crawl/fuzz/recon/sqlmap/nuclei continue to consume only `header`, while cookie analysis consumes `cookies[]` metadata. `browser_cookie_analyze bindBrowserSession:true` now carries non-sensitive Chrome cookie attributes into each result (`domain`, `path`, `secure`, `httpOnly`, `sameSite`, `session`, `expirationDate`, `storeId`, `partitionKey`) and the compact summary table exposes `domain/path/secure/httpOnly/sameSite/session/expires` next to token facts. Token values remain hashed/redacted; no native protocol or public tool name changed. Regression: `webSecurity/shared/http` structured provider-result unit, `cookieAnalyze` structured provider unit, `check-summaries` cookie attribute columns, `check:web-security`; C2 live verification also passed via `browser_tabs list maxChars/detailLevel` after restart.

- **C2 — `browser_tabs` hard-rejected the universal output params (`maxChars`/`detailLevel`/`redact`) (2026-06-06, mined from REAL session error logs across many transcripts).** Source: swept all `D:/Pi/agent/sessions/*.jsonl` for `browser_*` results with `isError` (19 total). The dominant real, current, cross-agent class: agents pass `maxChars`/`detailLevel` to `browser_tabs` and get `Validation failed … must not have additional properties` — **≥7 occurrences across ≥5 distinct sessions/agents through 2026-06-05** (e.g. `{action:list, timeoutMs, maxChars}`, `{action:list, detailLevel, timeoutMs}`). **Root cause (code-verified):** every other browser_* tool spreads `sharedTabScopedToolParams()` (which declares browserSessionId/tabId/detailLevel/outputPath/timeoutMs/maxChars/redact); `browser_tabs` alone hand-rolled its `strictToolParameters({...})` and omitted them, so `additionalProperties:false` rejected the universal params agents reasonably assume are global. (The related `redact` gap on `browser_command`/`browser_execute` from 2026-06-01 logs was already closed — `redact` is in the mixin now; xiaoyi 06-06 `browser_command redact:false` works. `browser_tabs` was the last holdout.) **Fix:** `registerTabsTool` now spreads `sharedTabScopedToolParams({ includeOutputPath:false })` (tabs writes no artifact) keeping its tab-specific params; `maxChars` is honored on the sizable outputs (list/snapshot); `detailLevel`/`redact` are accepted as no-ops (tab metadata is a small, non-secret JSON list, not a distilled/redacted summary). General (removes a special-case omission, aligns tabs with all tools — matches the "accept natural aliases, don't hard-reject" pattern), not overfit. Regression: `check-tools-contract` (browser_tabs must spread `sharedTabScopedToolParams`). `npm run check` green, token-economy 0.0607 unchanged, docs regenerated. (Other 19-error findings were deliberate negative tests `{bogus_field}`, hallucinated tools `browser_tool_discovery`, stale profile-gated `browser_crawl not found`, or already-good targeted errors like `engine=nuclei does not accept maxRequests` — none are current general defects.)

- **C1 — `browser_http_replay` double-submit CSRF reflection (2026-06-06, from a REAL CTF web-security session transcript, not a blind run).** Source: analysis of a real non-blind agent session (`sessions/2026-06-06T14-28-19…jsonl`, target = a real chat app, hunting IDOR/SSRF) that exercised the full web-security stack (browser_execute ×17, http_replay ×4, network/artifact/oast/command…), zero `isError`. **Friction (strong, general):** `http_replay` with `bindBrowserSession:false` → `403 missing csrf token`; the agent then **abandoned the tool and hand-wrote in-page `fetch`** to read the CSRF cookie + reflect it as `x-csrf-token`, repeating that boilerplate ~10× for IDOR enumeration, and even when it returned to `http_replay` it had to manually paste `x-csrf-token`. **Root cause (verified in code):** `sendReplayLikeRequest` (`shared/replay.ts:306`) binds only the Cookie header via `bindBrowserSession`; it does not reflect the ubiquitous double-submit CSRF cookie→header pattern. **Fix:** `deriveCsrfReflection` (`shared/http.ts`) parses the already-bound Cookie header, finds the CSRF cookie (default `xsrf-token/csrf-token/csrftoken/_csrf/csrf_token`, overridable), percent-decodes it, and maps it to the matching header (`XSRF-TOKEN→X-XSRF-TOKEN`, else `X-CSRF-Token`); `reflectCsrfIntoHeaders` wires it in. **Not WAI-violating** (completes an existing tool's session binding — deterministic mechanical web knowledge, not a click/type/extract verb): default-on (only under `bindBrowserSession:true`), visible (`csrfReflected` reports cookie/header NAMES only — token value never surfaced; default header names stay in `redactHeaders`' redacted set), overridable (`reflectCsrf:false`/`csrfCookie`/`csrfHeader`), never overrides a caller-supplied CSRF header. Scoped to `sendReplayLikeRequest` (http_replay only); no native-protocol change. Regression: `tests/unit/webSecurity/shared/http.test.ts` + `tests/unit/webSecurity/shared/replay-csrf.test.ts` + `check-summaries` (`csrfReflected`). Live re-verification on an authed CSRF site is left to a future real-site/blind check.
  - **B7 (cookie attributes) re-deprioritized by the same session:** the agent never used `browser_cookie_analyze`; its natural path to cookie attributes was `browser_command cookies getAll` (10 fields, used fluently). So adding attributes to cookie_analyze has lower marginal value than C1 and is not a current work item.

- **B6 + B8 + B9b + B10 — second-batch n=2 ergonomics fixes (2026-06-06, all live/contract-verified); B9a resolved in debt-zeroing follow-up.**
  - **B6** (link href/origin): `buildScanScript` now attaches resolved `href` to `<a>`/`<area>` actionables; the scan summary's actionables table adds `href` + a mechanical `sameOrigin` column (`scan.ts` `linkSameOrigin`). Perception/identity only — no ranking/extraction (B1 line respected). Regression `check-summaries` (`scan.actionables` columns + value); live: article.html link actionables carry resolved href + sameOrigin.
  - **B8** (perf/evidence samples) — scoped: standalone `browser_hook getPerformanceEntries` gets a distiller (`summaries/hook.ts` `summarizeHookPerformance` → initiatorType counts + resource/ms sample + `data.entries` hint), routed via `hookToolDistiller`. The multi-source evidence AGGREGATE stays counts-only BY DESIGN — adding per-source samples there blew the token-economy guardrail (+106%, caught by `check:token-economy`); its `data.sources` hint is the drill-in path. hook `detail:null` for lifecycle events is WAI. Regression `check-summaries` (`hook.performance (B8)`); live (linux.do): 17 entries → counts+sample, no `{type,keyCount}` collapse.
  - **B9b** (download MIME mismatch): `browser_download` takes `expectMime` (media mode → `image`); after a completed download it compares the Chrome download `mime` and flags a `mimeMismatch` diagnostic (`registerTransferTools.annotateDownloadMimeMismatch` + surfaced in `summaries/transfer.ts`). Diagnostic honesty, not a hard fail — the file is still saved. Regression `check-summaries` (`transfer.mimeMismatch (B9b)`). **B9a** was later resolved by `data.media_candidates`, bounded to visible media identity (selector/src/poster/alt/dims/geometry/sameOrigin), not headline/ranking semantics.
  - **B10** (CLI did-you-mean): `parseArgs` suggests the nearest valid flag (normalized camelCase↔kebab, then edit-distance ≤2) and gives a targeted hint for common-but-absent flags (`--detail-level` → use `--limit/--offset/--max-chars`). General — helps every flag typo. Regression `tests/unit/cli/flags-render.test.ts` (`B10`); live: `--jsonPath`/`--max-char`/`--detail-level` all give the right guidance.
  - **B7 (cookie attributes) DEFERRED**: verified the attrs are discarded upstream — the browser-session cookieProvider returns a flattened Cookie header (`register/shared.ts` `browserCookiesToHeader`), which has no domain/path/secure/httpOnly/sameSite. Surfacing them needs a provider-contract change to pass structured cookie metadata to the analyzer (moderate, shared code), not a summary column add. Not in this clean batch.

- **B4 + B5 partial — artifact JSON scalar string windows + selector-timeout recovery hints (2026-06-06, second real-site blind batch).** B4 root cause was `compactJsonValue` hard-truncating scalar strings at 800 chars while `offset`/`limit` only paged arrays; `browser_artifact mode=json jsonPath=data.content offset=N limit=M` now returns a character window object `{type:"string", text, offset, limit, nextOffset, originalLength, truncated*}` for scalar string hits, without changing nested-object compaction or redaction. B5 root cause was wait supervisor timeout details exposing only generic lease/CDP state; `wait.selector` timeout supervisor payload now includes `selectorTimeout` with selector, state, main-document zero-match when known, and copyable recovery commands (`diagnose`, re-observe scan, html/frame follow-up). Rel1 is closed as M2 harness artifact. Regression: `check:artifact` and `check:fake-ws`.
  - **B5 DOM-probe follow-up — COMPLETED 2026-06-07** (was deferred above). `wait.diagnose` is now selector-miss-specific instead of global-only: the wait coordinator remembers each wait's terminal record (`rememberTerminal` on every `finishPiBrowserWait`, pruned to 200 records / 5 min), and `diagnosePiBrowser` recovers the recent selector wait by `waitId` (active or terminal; or an explicit `selector`), runs ONE bounded main-document `hook.evaluate` selector probe — `matchCount`/`visibleCount`/`firstMatch`/`sampleMatches`/syntax-validity — and returns `selectorDiagnostics` {selector, expectedState, lastState (the timeout's last snapshot), current (the probe), recoveryCommands} plus `recentWaits` (last 20 terminal waits) alongside the existing iframe/frame diagnostics. No public tool-surface change (internal bridge `wait.diagnose` enrichment; `browser_wait action=diagnose` unchanged). Honest scope: this gives match-count + iframe + last-state evidence; it does NOT do fuzzy "similar selector" text suggestion (optional, low value). Regression: `check:pi-browser-bridge` dynamic contract (`testWaitDiagnoseIframeProbeContract` now registers a selector wait, finishes it TIMEOUT, diagnoses by waitId, and asserts `selectorDiagnostics.{waitId,selector,current.matchCount,lastState.found}` + `recentWaits`); needs `npm run build:bridge` (contract reads `bridge/.../dist`). `npm run check` green.

- **B2 + B3 — CLI script-file ergonomics + artifact path discoverability (2026-06-06, 10-site blind batch).** B2 fixed narrowly at the CLI layer: `pi-browser execute --script-file <path>` reads cwd-relative/absolute JavaScript into the existing `script` parameter, rejects combination with `--script`, and deletes the synthetic param before schema validation; tool API/schema unchanged. B3 fixed without artifact schema drift: `browser_artifact` descriptions now tell agents common roots are `data`, `data.<key>` and that CLI `--pick` is repeated per path, not a JSON-array string; large/non-inline `browser_execute` results add `artifact_hints.preferredReads` for `data` and up to three top-level `data.<key>` fields. Regression: `tests/unit/cli/flags-render.test.ts` (script-file parse/help + artifact help wording) and `tests/unit/tools/execute-artifact-hints.test.ts` (execute hints). B1 intentionally deferred to RFC/minimal primitive; no semantic extractor added.

- **H3 — `browser_hook collect` events buried + wrong hint (2026-06-06, R7 hook, live-confirmed).** Collect fell through the `browser_hook` tool distiller (`domFlowDistiller`, which only handles getNodeListeners/Chain/SinkHints) to the generic summarizer → events collapsed to `{type,keyCount}` stubs + `nextActions` fell back to `operation.operationId`. Fix: new `summaries/hook.ts` `summarizeHookCollectData` (event-type counts via `topCounts`, a compact `seq/type/detail` sample table, and a data-rooted `data.events` artifact hint), routed from a new `hookToolDistiller` for `hook.collect`. Regression: `check-summaries` (`hook.collect (H3)` — eventTypes counts, sample detail, `data.events` hint). Live (linux.do): `eventTypes:[{console.log:2},…]`, `total:5`, sample rows `[seq,type,detail]`, `nextActions: read_saved_artifact mode=json jsonPath=data.events`.

- **E1 — `browser_evidence` artifact hint missing the `data.` prefix (2026-06-06, R7 evidence, code+live-confirmed; same class as N1).** The evidence summarizer emitted `jsonPath: "sources"` but the saved artifact (the bridge result `{id,tabId,data,…}`) holds it at `data.sources` → the hint resolved to `notFound`. Fix: `summaries/evidence.ts` → `data.sources`. Regression: `check-summaries` (evidence hint must be `data.sources`). Class audit (R7): bridge-wrapped tools (scan/network/evidence/hook) need data-rooted hints — scan ✅, network ✅ (N1), evidence ✅ (E1); web-security tools (crawl/recon/fuzz/domFlow) have FLAT artifacts so bare paths are correct (domFlow even auto-detects `wrapped`). Remaining class gap: hook has no distiller (H3).

- **N1 — network artifact-read hints missing the `data.` prefix (2026-06-06, R6 network, code-confirmed).** `summaries/network.ts` `entriesContainerPath` emitted `items`/`entries`/… (relative to the bridge result's `.data`), but `read_saved_artifact jsonPath=…` hints resolve against the SAVED ARTIFACT whose root is the whole bridge result `{id,tabId,data,…}` — so the hint pointed at a non-existent root `items[…]` (`notFound`). Scan was already correct (`data.content`). Fix: prefix the emitted paths with `data.` (`data.items[1].request`). Regression: `check-summaries` (`network.artifactHints (N1)` — first hint is `data.items`, every hint is data-rooted). Verified live (linux.do): hints now `data.items[…]`, resolve in the saved artifact on the first try, and carry H2's `mode=json`.

- **M1 — `browser_observe` baseline (treeDiff/diff/causal) unreachable by reference via the CLI (2026-06-06, R6 causal/treeDiff, code-confirmed).** `--baseline` only accepted an inline Array/Object (the full prior entity list/envelope, tens–hundreds of KB) → on the CLI it blew the OS argv limit (`ENAMETOOLONG`); a bare `snapshotId` string was rejected by the schema (contradicting the skill). The daemon already resolved a baseline OBJECT carrying `{snapshotId}` (from its observation store) or `{saved:{path}}` (read a file) — but that was undocumented and awkward (JSON-on-CLI / Windows quoting). Fix: two CLI-friendly SCALAR flags on `browser_observe` — `--baseline-snapshot-id <id>` and `--baseline-path <file>` — mapped onto the existing resolution; zero daemon change. Skill updated (pass baseline BY REFERENCE, don't inline). Regression: `tests/unit/cli/flags-render.test.ts` (`M1` — observe exposes both flags). Verified live (linux.do): `--baseline-snapshot-id <id>` after a navigation produced `diff`+`treeDiff` (74 appeared / 354 disappeared) with no error; `causal` present (correctly `unavailable` because the baseline predated `browser_network start`).

- **H1 + H2 — execute nextActions 误导 + artifact --json-path 无 mode 静默失效（2026-06-06，n=2 bilibili+linux.do）。** 两条一起修：**H1** — execute distill 在数据已内联（小返回值，F1 已修）时标记 `dataInline:true`；`artifactReadActions` 检测该标志并抑制关联路径 nextActions hint（operationId 等），因为 agent 已有数据无需读 artifact。**H2（两个 fix）** — ① `artifactReadActions` 的 preferredReads hint 改为 `read_saved_artifact mode=json jsonPath=X`（补全 `mode=json`，agent 可直接翻译成 CLI 调用）；② `readBrowserArtifact` 在 `--json-path` 或 `--pick` 存在但未指定 mode 时**自动升级到 json 模式**（之前静默回退 text，忽略 jsonPath，整个文件被返回）。回归：`check-summaries`（nextActions 格式 `includes("jsonPath=…")`，兼容新旧格式）；`check:artifact` 已通过（json 模式自动升级不破坏任何现有路径）。全量 check 绿，token-economy 0.06 未损。

## Resolved (prior)

- **F1 — `browser_execute` small return-value rendering (2026-06-06).** Root cause: `src/tools/summaries/generic.ts` `compactSample` is depth-2 + 5-sample + 240-char limited, so a value nested inside an object/array (R1 rows→cells, R3 `{count,rows}`) collapsed to `{type,count/keyCount}` even when the whole return was a few hundred bytes. Fix: `summarizePlainValue` now inlines the WHOLE return value VERBATIM (JSON-cloned) when it serializes within `INLINE_VALUE_CHARS` (4 KB); genuinely large payloads still collapse to a compact shape (samples stay small) with the full value in the saved artifact, and `fitSummaryBudget` (~8 KB) remains the hard guard. Redaction still applies (it runs after distill). Regression locked in `tests/contracts/tools/check-summaries.mjs` (`generic.inline.*`). Verified live through the rebuilt daemon on a real, China-reachable site (linux.do): a `{count,rows:[{title,replies}…]}` extraction now returns inline in ONE call — no second `browser_artifact` round-trip. NOT "fixed" by an execution helper (that would be WAI); this is pure output-ergonomics.

- **F4 — `wait --params` (and all action tools') per-action shape undocumented (2026-06-06).** Generality first (per `eval-fixes-true-defect-no-overfit`): root cause confirmed NOT wait-specific — `wait/network/hook/frame` all share `params: NativeCommandParamsSchema = Type.Object({}, {additionalProperties:true})` (loose), so `--params <json>` showed no shape for any of them; a wait-only fix would have overfit. Fix (source-of-truth driven, zero-drift): the `sync:protocol` generator now joins each action with its command's `required`/`requiredAny`/`notes` from `bridge/native_command_schema.json` into `nativeActionMetadata`, and `pi-browser <tool> --help` renders a per-action `--params` keys section (`cli/index.ts` `nativeActionParamsHelp`). Derived for EVERY action of every action tool — never hand-listed; `check:protocol` guards drift. Honest limit: the schema encodes required keys + notes (covers the F4 cases: wait.selector→selector, wait.navigate→url, hook.evaluate→expression, frame.evaluate→frameId,expression), NOT full optional/typed shapes (network actions have none → shown as empty, nothing fabricated); full param specs would be a larger source enrichment, deferred unless evals demand it. Regression: `tests/unit/cli/flags-render.test.ts` (`F4: nativeActionParamsHelp …`). Pure discoverability — `--params <json>` still works unchanged; not an execution/param-model change.

- **F2 — envelope verbosity / `[Circular]` (2026-06-06).** Measured the real envelope (deterministic local fixture, isolating structure from content) before guessing — 20 KB total, of which `summary.preview` was **6767 B (91% of summary)** plus two `[Circular]` placeholders. Three evidence-backed, general fixes: **(A)** the budget-overflow `preview` fallback re-dumped the whole compacted summary (up to ~8 KB) even though the page model is already lifted to envelope top-level (`entities`/`gist`/`outline`/`diff`/…) + saved artifact — capped it to an ≤800-char orientation snippet (`resultMiddleware.PREVIEW_FALLBACK_CHARS`). **(B)** `observeRunners` duplicated `diff`/`treeDiff`/`snapshotProjection` into BOTH summary top-level AND `focus` by shared reference (redaction collapsed the 2nd to `[Circular]`) — removed the redundant `focus` copies (the lift reads summary top-level; no consumer reads `focus.*`). **(C)** the scan summarizer shared the `headings`/`top_layer` arrays between summary top-level and `focus` — now distinct copies. Result (live, rebuilt daemon): **20 KB → 14 KB (−29%)**, `summary` 7435 → 1557 B, `preview` 6767 → 916 B, **`[Circular]` 2 → 0**; all signal preserved (entities/gist/etc. unchanged at envelope top-level). Regression: `check-summaries` (preview cap ≤800 + no `[Circular]` in the fallback) and `check-abml-scan-envelope` (headings/top_layer never `[Circular]`). **Left by design (honest):** entity objects are still intentionally shared across `primary_actions[].entity` / `primary_entities` / `envelope.entities` (contracted — `check-abml-scan-entities` requires the embedded handle), so redaction still renders the duplicate references as `[Circular]` in non-overflow envelopes; "fixing" that would either re-emit every entity 3× (balloon the envelope) or break the embedded-entity contract — per `eval-fixes-true-defect-no-overfit` that regression isn't worth it, and `[Circular]` there means "this entity is shown in full in `envelope.entities`", not data loss.

## reliability (work items)

*(none currently)*

## WAI (transparency only — NOT work items)

| # | finding | rationale |
|---|---------|-----------|
| W1 | "No `click`/`type`/`fill` verb; had to hand-write the form-fill via native value setter + `input`/`change` dispatch." | By deliberate project decision: the project provides the ABML page MODEL; the agent writes execution code. JS-first via `browser_execute`, trusted-event escape via `browser_command` CDP. The B2 action arm was tried and reverted. See `eval-friction-triage-perception-vs-execution`, `public-tool-surface-no-action-verbs`. (R2 agent reported "no friction finding how to act" — the model is fine; only the *return-value rendering* F1 hurt.) |
