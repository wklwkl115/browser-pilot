# Blind-agent eval findings (rolling, triaged)

Product of the `pi-browser-blind-eval` skill. Each finding is triaged `fixable | WAI | reliability`
per `eval-friction-triage-perception-vs-execution`. `runs` = how many independent blind runs (different
agent + task) surfaced it; treat `runs ≥ 2` as confirmed, `runs = 1` as a hypothesis to re-test before
acting. WAI items are recorded for transparency but are **not** work items.

**Before fixing anything**, a finding must be a TRUE, GENERAL project defect — confirmed root cause that
generalizes, no change-for-change, no overfit to the site/DOM/task/shape that surfaced it (fix +
regression stay general). If it can't clear that bar, leave it here as "needs more runs", don't patch.
See `eval-fixes-true-defect-no-overfit`.

> **Protocol (corrected 2026-06-06, see `blind-eval-protocol-realsite-skill-china`):** blind eval now
> runs on REAL, mainland-China-reachable sites (READ-ONLY) with the agent READING the
> `pi-browser-tools` skill. The first two runs below predate that and are **legacy** (no skill,
> `--help`-only, local/ad-hoc fixtures) — re-validate their findings under the corrected protocol; some
> (e.g. F3) are likely skill-mitigated. The execute-return-value finding (F1) recurs under BOTH.
>
> Runs: R1 = login→orders (ad-hoc fixture, legacy); R2 = spec 02 scan-execute-wait (interactive.html,
> legacy); **R3 = linux.do top-topics (REAL site, skill-guided)** — canonical corrected run.

## Pending live verification (next round)

These landed with unit/contract proof but want live confirmation. **Both need a daemon restart first**
(Node `src/` changes don't hot-reload — `src-reload-requires-process-restart`); the running operator
daemon predates them.

- **C2 (`browser_tabs` universal params) — any run, trivial.** After restart, call
  `pi-browser tabs list --max-chars 5000 --detail-level summary` (or `browser_tabs {action:list,
  maxChars:5000, detailLevel:"summary"}`). **Pass:** returns the tab list with NO
  `Validation failed … must not have additional properties`. A blind run naturally exercises
  `browser_tabs`, so a maxChars/detailLevel-bearing call there confirms it for free.
- **C1 (`http_replay` CSRF reflection) — operator-driven (blind is READ-ONLY and cannot log in).**
  Recipe: (1) be logged into a site using double-submit CSRF (XSRF-TOKEN cookie ↔ `X-*-CSRF` header)
  and open its tab; (2) restart the daemon; (3) capture an authenticated request (`browser_network`)
  or know an endpoint, then `browser_http_replay url=<endpoint> method=POST bindBrowserSession:true`
  **without manually adding the CSRF header**. **Pass:** `summary.request.csrfReflected:{cookie,header}`
  is present and the response is 200 (not 403 "missing csrf token"); a control run with
  `reflectCsrf:false` should reproduce the 403. Real-session evidence that motivated it is in the C1
  entry below.

## fixable (work items)

| # | finding | runs | evidence | candidate fix |
|---|---------|------|----------|---------------|
| F1 | **`browser_execute` does not return your script's value inline — even small values**: nested arrays/objects collapse to `{type,count/keyCount}` shape placeholders and strings truncate (~280 chars) under default `detailLevel`, forcing a second `browser_artifact` round-trip to read data the agent just computed. | **3 — CONFIRMED, survives the skill** (R1 nested-array collapse; R2 280-char truncation; R3 <2KB `{count,rows}` → `keyCount:4` placeholders) | R1: rows→cells = `{type:"array",count:3}`. R2: `originalLength:280, truncated:true`, buried in `summary.data.value`. R3: small return collapsed to `{type:"object",keyCount:4}`, needed `artifact --json-path data.rows`. R3 agent (skill-guided): "pure overhead for the most common extract-a-few-values use case — the only real inefficiency I hit." | **✅ RESOLVED 2026-06-06** — see "Resolved" below. |
| F2 | **Envelope verbosity drowns the answer**: `observe`/`execute` wrap a tiny result in a large envelope (privacy/operation/correlation/nextActions + `[Circular]`/`{type,keyCount}` placeholders). `observe mode:text` was ~34KB JSON for one fact; useful fields (`summary.data.value`, `textPreview`, `entities[]`) are buried. | 2 (R1 observe-scan noise; R2 34KB text envelope) | R1: 9-node page → 75KB scan artifact, `headings:"[Circular]"`. R2: 34KB for "Status: activated". | **✅ RESOLVED 2026-06-06** (dominant bloat) — see "Resolved"; one sub-item left by design. |
| F3 | **`tabs --json` without `--action` errors with raw schema noise**: `Invalid parameters — /: must have required properties action`. Top-level `--help` lists `tabs` but doesn't signal `--action` is mandatory; the `/:` is raw validator output. | 1 (R2, legacy) — **likely skill-mitigated**: R3 (skill-guided) did NOT hit this. | R2 cmd #4. | Lower priority. Friendly message: `tabs requires --action (one of: list, switch, create, …)`; or list required flags in top-level `--help`. |
| F4 | **`wait --params` JSON shape is undocumented**: `--help` shows `--params <json>` with no schema; the agent guessed `{selector,timeoutMs}` / `{url,state}` and got lucky. | 2 (R1, R3) — survives the skill | R1 cmd #5-6; R3 "`wait --help` does NOT document the shape of `--params`… guessed correctly from convention." | **✅ RESOLVED 2026-06-06** — see "Resolved" below. |
| N1 | **network result `artifact_hints`/`nextActions` jsonPath omits the `data.` prefix**: the saved artifact is a bridgeResult (`{id,tabId,data,…}`) with entries under `data.items[…]`, but the network summarizer emits `entriesPath = "items"` → hints like `read_saved_artifact mode=json jsonPath=items[1].request` resolve to `notFound`; the working path is `data.items[1].request`. Scan correctly uses `data.content`; network is inconsistent. | 1 (R6 network) — **CODE-CONFIRMED** | Agent N: `items[1].request` → `notFound:true`; `data.items[1].request` worked. | **✅ RESOLVED 2026-06-06** — see "Resolved". |
| M1 | **`browser_observe --baseline` has no by-reference input → the headline treeDiff/diff/causal workflow is unreachable via the CLI for real pages**: schema is `Union([Array, Object])` only — a `snapshotId` string is REJECTED (contradicts the skill, which lists "…/snapshotId"), and the only accepted forms (full prior entity list / envelope, tens–hundreds of KB) blow the OS argv limit (`ENAMETOOLONG`) when passed inline on the CLI. There is `--output-path` to WRITE a baseline but no `--baseline-path`/`--baseline-snapshot-id` to feed one. | 1 (R6 causal/treeDiff) — **CODE-CONFIRMED** (`registerObserveTool.ts` baseline `Union([Array,Object])`, no string/ref) | Agent M: snapshotId + `{snapshotId}` + trimmed summary + 29-entity array all `anyOf` rejected; real `abml.entities` (~690KB) → `ENAMETOOLONG`. treeDiff/causal never obtained via CLI. | **✅ RESOLVED 2026-06-06** — see "Resolved". |
| E1 | **`browser_evidence` artifact hint jsonPath omits the `data.` prefix** (SAME CLASS as N1, different tool): the saved evidence artifact is the bridge result `{id,tabId,data,…}` with per-source data at `data.sources`, but the summarizer emits `jsonPath: "sources"` → `read_saved_artifact jsonPath=sources` resolves to `notFound`. | 1 (R7 evidence) — **CODE+LIVE-CONFIRMED** | Agent E: `sources` → `notFound` (file found, `bytes:107879`); `data.sources` worked. With N1 this is **n=2 of the class** (bridge-wrapped tools' hints must be data-rooted; web-security tools' artifacts are flat → bare paths correct, e.g. domFlow detects `wrapped`). | **✅ RESOLVED 2026-06-06** — see "Resolved". |
| H3 | **`browser_hook collect` buries the events + emits a useless artifact hint**: collect has NO dedicated distiller, so events collapse to `{type:object,keyCount}` stubs even at `--detail-level full`, and `nextActions` falls back to `read_saved_artifact jsonPath=operation.operationId` (an op id, not the events). The real data is at `data.events`, derivable only from the summary's own `keys` list. | 1 (R7 hook) | Agent H: "collect returns events collapsed to opaque keyCount stubs… the headline result isn't in the headline"; correct path `data.events` not hinted. | **✅ RESOLVED 2026-06-06** — see "Resolved". |
| B1 | **Real-site visible headline/card/list extraction still falls back to custom `browser_execute` JS**: across news, video, and article-card sites, `observe --mode text` often returned empty `textPreview` / low-signal text or a large scan/ABML envelope, while `scan` exposed entities/templates but not a compact table of current viewport headline/link rows. | **10 — CONFIRMED, survives the skill; DEFERRED/RFC** | 10-site batch artifact `.pi/browser-artifacts/eval-blind/real-site-10-run-summary.json`; every successful run completed by custom execute JS or artifact follow-up for per-item values. Sites: bilibili, ithome, sina, qq news, sohu, cctv, thepaper, 36kr, ifeng, chinanews. | **Do not implement a semantic headline/card/news extractor now.** If revisited, constrain it to a generic viewport-visible text/link row projection: text, href, geometry/visibility, container/section hints only; no site-specific selectors, no source/time/uploader inference, no ranking semantics. |
| B2 | **Non-trivial inline `browser_execute` scripts are CLI-fragile**: several blind agents needed long DOM extraction scripts; shell quoting, CSS selector quoting, or JS node-shape mistakes caused retries. | **5 — CONFIRMED ergonomics issue** | Bilibili shell quote EOF; IT之家 CSS selector quoting error; 36kr shell quote / JS empty-node retries; multiple runs requested a script-file/heredoc-friendly path. | **✅ RESOLVED 2026-06-06** — CLI-only `execute --script-file <path>`; API schema unchanged. |
| B3 | **Artifact read path discovery still costs extra turns**: agents repeatedly guessed wrong JSON paths (`data.data.*`, wrong `pick` shape) before reading the artifact root and discovering `data.*`. | **4 — CONFIRMED discoverability issue** | Bilibili `--pick` confusion; IT之家 `data.data.links` miss; Sohu `data.data` miss; 36kr jsonPath miss. | **✅ RESOLVED 2026-06-06** — artifact help clarifies `data.*` roots and repeated `--pick`; execute large-result artifacts now hint `data` / `data.<key>` reads. |
| B4 | **Artifact JSON access to long string fields is awkward**: `observe mode=text/content` saves primary text in string fields such as `data.content` or `data.markdown`; `browser_artifact --mode json --json-path data.content` often returns a preview/truncated wrapper, with no obvious string offset/read-full path. Agents switched to `search --context-chars` or custom JS for text already captured. | **2 — CONFIRMED in second 10-site batch** | Weather.com.cn: `data.content` originalLength ~2857 still preview/truncated; People.com.cn article: `data.markdown` existed but was preview/truncated, `data.text` absent, agent used search contexts. | **✅ RESOLVED 2026-06-06** — scalar string hits for `jsonPath` now support `offset`/`limit` character windows with `originalLength`/`nextOffset`. |
| B5 | **`wait` selector-timeout diagnostics are recoverable but not selector-miss-specific**: `WAIT_TIMEOUT` includes useful waitId/lease/CDP cleanup, but does not give a copyable diagnose command; `diagnose {waitId}` reports global tab/bridge state rather than original selector, match count, iframe candidates, or similar selector/text hints. | **2 — CONFIRMED across batches** | R1/R3 already noted wait params shape; second batch MOE selector miss: timeout returned waitId/attempts/historyLost, diagnose showed status/cleanup but not selector-specific recovery. News.cn also guessed optional wait params (`waitUntil`, `idleMs`). | **PARTIAL ✅ RESOLVED 2026-06-06** — timeout supervisor now surfaces selector, state, main-document zero-match when known, and recovery commands. Full DOM/iframe/similar-selector probing remains deferred. |
| B6 | **Link entities/actionables omit normalized `href`/origin classification**: scan identifies link controls and selectors, but not resolved link URLs or same-origin/external status. Link inventory tasks still require custom JS. | **2 — CONFIRMED** | MCT run: scan actionables had text/selector/role but not href; agent used JS to classify same-origin links. Stats.gov.cn B6 run `7666c51a`: actionables/list_hints exposed labels/selectors/geometry only; agent used execute fallback for 8 HTTP(S) links with same-origin classification. | **✅ RESOLVED 2026-06-06** — link actionables now carry resolved `href` + mechanical `sameOrigin`; no semantic ranking/extraction. See "Resolved". |
| B7 | **Cookie analyze browser-session summary lacks browser cookie attributes**: the compact table covers token-analysis fields (`kind`, length, decoding counts) but omits redacted browser-cookie metadata such as `domain`, `path`, `secure`, `httpOnly`, `sameSite`, and expiry/session. | **2 — PARTIAL CONFIRMED** | Baidu run: cookie names/kinds visible but not domain/path/security posture. Sogou B7 run `6fe09b3d`: summary table present and useful for count/name/kind/length, but domain/path/security/expiry absent. | **✅ RESOLVED 2026-06-07** — see "Resolved" below. |
| B8 | **Hook/evidence/performance summaries need compact passive evidence samples**: summaries show counts/status, but not enough safe event/resource detail, so agents read artifacts for basic samples. | **2 — CONFIRMED** | QQ hook run: evidence useful but required artifact reads for samples. Mi.com B8 run `e02baace`: hook collect `detail:null`, performance samples were `{type:"object", keyCount:8}`, evidence aggregate omitted event/resource sample rows. | **✅ RESOLVED (scoped) 2026-06-06** — standalone `browser_hook getPerformanceEntries` now surfaces initiatorType counts + a compact resource/ms sample + `data.entries` hint (was generic `{type,keyCount}` collapse). The multi-source evidence AGGREGATE deliberately stays counts-only (adding samples there blew the token-economy budget +106%; its `data.sources` hint drills in). hook `detail:null` for lifecycle/snapshot events is WAI. See "Resolved". |
| B9 | **Download/media discoverability lacks visible media candidate list and final MIME validation**: observe exposes media refs/templates but not a directly usable candidate table; download can complete a non-image response for an image candidate without warning. | **2 — CONFIRMED** | ChinaDaily run: JS extracted visible images before direct URL download. Huanqiu B9 run `710c4d04`: observe had media refs/templates only; JS fallback found selector/currentSrc/alt/headline/dimensions; both url/media downloads completed as `text/html` anti-bot pages, not images. | **B9b ✅ RESOLVED 2026-06-06**: `browser_download` now takes `expectMime` (media mode defaults to `image`) and flags a `mimeMismatch` diagnostic when the completed download's MIME doesn't match (e.g. an anti-bot `text/html` page for an image). **B9a (media candidate list) DEFERRED** — closest to B1's deferred semantic-extractor boundary; if done, bound strictly to media-element identity (selector/currentSrc/alt/dims), no associated-headline/ranking. See "Resolved". |
| B10 | **Artifact CLI/help remains mildly inconsistent with tool-style nextActions**: B3 made roots and `--pick` clearer, but nextActions still use `read_saved_artifact ... jsonPath=...` tool notation rather than copyable CLI; camelCase `--jsonPath` and generic `--detail-level` produce accepted-list errors without did-you-mean or artifact-specific guidance. | **2 — PARTIAL CONFIRMED** | ChinaDaily used `--jsonPath` once and failed; MCT tried `artifact --detail-level` and failed. CMA B10 run `cbba5138`: `--json-path` is discoverable after reading CLI docs; `--jsonPath` and `--detail-level` still fail with accepted-list only; nextActions need manual mapping. | **✅ RESOLVED 2026-06-06** — `parseArgs` now suggests the closest valid flag (normalized camelCase↔kebab match, then edit-distance ≤2): `--jsonPath` → "did you mean --json-path?", `--max-char` → "--max-chars". A common-but-absent flag like `--detail-level` gets a targeted hint ("size output with --limit / --offset / --max-chars"). Copyable-CLI-in-nextActions deferred (frontend-agnostic notation). See "Resolved". |

### Minor — ✅ both resolved 2026-06-06
- **screenshot metadata lacks pixel width/height** (R7 Agent E) — FIXED: `registerScreenshotTool` now decodes width/height from the captured image (PNG IHDR / JPEG SOF) into the summary; returns undefined rather than guessing. Live: `data.width:2058, data.height:1514`. Unit test `tests/unit/tools/screenshot-dimensions.test.ts`.
- **`browser_hook installTargets` `--params {targets}` undocumented** (R7 Agent H) — FIXED: verified `hook.install_targets` rejects a no-targets call (`hook.ts:137`), so marked `required:["targets"]` in `native_command_schema.json`; F4's per-action help now shows `installTargets requires targets`.

## Closed — not a tool bug

| # | finding | resolution |
|---|---------|------------|
| M2 / Rel1 | `browser_execute` channel hang — every execute (even `1+1`) `BRIDGE_TIMEOUT` while observe/network/command/wait work. | **HARNESS ARTIFACT, NOT A TOOL BUG (confirmed 2026-06-06).** R6 ran in a browser polluted by 12 lingering isolated-Edge processes from prior runs (teardown bug below). After fixing teardown, a clean-browser repro had `browser_execute` working immediately (`1+1 → 2`). Rel1 is merged into M2 and closed. |

## Harness notes (eval infra, not tool findings)

- **`teardown-blind.mjs` left lingering isolated Edge + locked temp dirs — ✅ FIXED 2026-06-06.** `taskkill /PID <browserPid> /T /F` killed only the tracked launch pid's tree, but the detached Edge re-parents its renderer/GPU/utility processes, so they survived — accumulating stale browsers (12 found) + locking `.pi/temp-profiles/blind-*` (silent rm failure). Dirty reuse then polluted eval signal (caused M2). Fix: `teardown-blind.mjs` now also kills every `msedge.exe`/`chrome.exe` whose command line references THIS run's unique profile dir (PowerShell CIM match), waits 800ms for handle release, then removes temp dirs. Self-tested: 0 lingering Edge after teardown (was 12).

## Needs more runs (n=1, hypothesis)

*(none currently)*

## Resolved

- **B7 — `browser_cookie_analyze` browser-session cookie attributes (2026-06-07, n=2 real-site blind backlog).** The cookie provider contract now returns a structured provider result `{header,cookies,url}` instead of only a flattened Cookie header; HTTP replay/crawl/fuzz/recon/sqlmap/nuclei continue to consume only `header`, while cookie analysis consumes `cookies[]` metadata. `browser_cookie_analyze bindBrowserSession:true` now carries non-sensitive Chrome cookie attributes into each result (`domain`, `path`, `secure`, `httpOnly`, `sameSite`, `session`, `expirationDate`, `storeId`, `partitionKey`) and the compact summary table exposes `domain/path/secure/httpOnly/sameSite/session/expires` next to token facts. Token values remain hashed/redacted; no native protocol or public tool name changed. Regression: `webSecurity/shared/http` structured provider-result unit, `cookieAnalyze` structured provider unit, `check-summaries` cookie attribute columns, `check:web-security`; C2 live verification also passed via `browser_tabs list maxChars/detailLevel` after restart.

- **C2 — `browser_tabs` hard-rejected the universal output params (`maxChars`/`detailLevel`/`redact`) (2026-06-06, mined from REAL session error logs across many transcripts).** Source: swept all `D:/Pi/agent/sessions/*.jsonl` for `browser_*` results with `isError` (19 total). The dominant real, current, cross-agent class: agents pass `maxChars`/`detailLevel` to `browser_tabs` and get `Validation failed … must not have additional properties` — **≥7 occurrences across ≥5 distinct sessions/agents through 2026-06-05** (e.g. `{action:list, timeoutMs, maxChars}`, `{action:list, detailLevel, timeoutMs}`). **Root cause (code-verified):** every other browser_* tool spreads `sharedTabScopedToolParams()` (which declares browserSessionId/tabId/detailLevel/outputPath/timeoutMs/maxChars/redact); `browser_tabs` alone hand-rolled its `strictToolParameters({...})` and omitted them, so `additionalProperties:false` rejected the universal params agents reasonably assume are global. (The related `redact` gap on `browser_command`/`browser_execute` from 2026-06-01 logs was already closed — `redact` is in the mixin now; xiaoyi 06-06 `browser_command redact:false` works. `browser_tabs` was the last holdout.) **Fix:** `registerTabsTool` now spreads `sharedTabScopedToolParams({ includeOutputPath:false })` (tabs writes no artifact) keeping its tab-specific params; `maxChars` is honored on the sizable outputs (list/snapshot); `detailLevel`/`redact` are accepted as no-ops (tab metadata is a small, non-secret JSON list, not a distilled/redacted summary). General (removes a special-case omission, aligns tabs with all tools — matches the "accept natural aliases, don't hard-reject" pattern), not overfit. Regression: `check-tools-contract` (browser_tabs must spread `sharedTabScopedToolParams`). `npm run check` green, token-economy 0.0607 unchanged, docs regenerated. (Other 19-error findings were deliberate negative tests `{bogus_field}`, hallucinated tools `browser_tool_discovery`, stale profile-gated `browser_crawl not found`, or already-good targeted errors like `engine=nuclei does not accept maxRequests` — none are current general defects.)

- **C1 — `browser_http_replay` double-submit CSRF reflection (2026-06-06, from a REAL CTF web-security session transcript, not a blind run).** Source: analysis of a real non-blind agent session (`sessions/2026-06-06T14-28-19…jsonl`, target = a real chat app, hunting IDOR/SSRF) that exercised the full web-security stack (browser_execute ×17, http_replay ×4, network/artifact/oast/command…), zero `isError`. **Friction (strong, general):** `http_replay` with `bindBrowserSession:false` → `403 missing csrf token`; the agent then **abandoned the tool and hand-wrote in-page `fetch`** to read the CSRF cookie + reflect it as `x-csrf-token`, repeating that boilerplate ~10× for IDOR enumeration, and even when it returned to `http_replay` it had to manually paste `x-csrf-token`. **Root cause (verified in code):** `sendReplayLikeRequest` (`shared/replay.ts:306`) binds only the Cookie header via `bindBrowserSession`; it does not reflect the ubiquitous double-submit CSRF cookie→header pattern. **Fix:** `deriveCsrfReflection` (`shared/http.ts`) parses the already-bound Cookie header, finds the CSRF cookie (default `xsrf-token/csrf-token/csrftoken/_csrf/csrf_token`, overridable), percent-decodes it, and maps it to the matching header (`XSRF-TOKEN→X-XSRF-TOKEN`, else `X-CSRF-Token`); `reflectCsrfIntoHeaders` wires it in. **Not WAI-violating** (completes an existing tool's session binding — deterministic mechanical web knowledge, not a click/type/extract verb): default-on (only under `bindBrowserSession:true`), visible (`csrfReflected` reports cookie/header NAMES only — token value never surfaced; default header names stay in `redactHeaders`' redacted set), overridable (`reflectCsrf:false`/`csrfCookie`/`csrfHeader`), never overrides a caller-supplied CSRF header. Scoped to `sendReplayLikeRequest` (http_replay only); no native-protocol change. Regression: `tests/unit/webSecurity/shared/http.test.ts` + `tests/unit/webSecurity/shared/replay-csrf.test.ts` + `check-summaries` (`csrfReflected`). Live re-verification on an authed CSRF site is left to a future real-site/blind check.
  - **B7 (cookie attributes) RE-DEPRIORITIZED to backlog by the same session:** the agent never used `browser_cookie_analyze`; its natural path to cookie attributes was `browser_command cookies getAll` (10 fields, used fluently). So adding attributes to cookie_analyze has lower marginal value than C1 — kept in backlog, not abandoned.

- **B6 + B8 + B9b + B10 — second-batch n=2 ergonomics fixes (2026-06-06, all live/contract-verified); B7 + B9a deferred with reasons.**
  - **B6** (link href/origin): `buildScanScript` now attaches resolved `href` to `<a>`/`<area>` actionables; the scan summary's actionables table adds `href` + a mechanical `sameOrigin` column (`scan.ts` `linkSameOrigin`). Perception/identity only — no ranking/extraction (B1 line respected). Regression `check-summaries` (`scan.actionables` columns + value); live: article.html link actionables carry resolved href + sameOrigin.
  - **B8** (perf/evidence samples) — scoped: standalone `browser_hook getPerformanceEntries` gets a distiller (`summaries/hook.ts` `summarizeHookPerformance` → initiatorType counts + resource/ms sample + `data.entries` hint), routed via `hookToolDistiller`. The multi-source evidence AGGREGATE stays counts-only BY DESIGN — adding per-source samples there blew the token-economy guardrail (+106%, caught by `check:token-economy`); its `data.sources` hint is the drill-in path. hook `detail:null` for lifecycle events is WAI. Regression `check-summaries` (`hook.performance (B8)`); live (linux.do): 17 entries → counts+sample, no `{type,keyCount}` collapse.
  - **B9b** (download MIME mismatch): `browser_download` takes `expectMime` (media mode → `image`); after a completed download it compares the Chrome download `mime` and flags a `mimeMismatch` diagnostic (`registerTransferTools.annotateDownloadMimeMismatch` + surfaced in `summaries/transfer.ts`). Diagnostic honesty, not a hard fail — the file is still saved. Regression `check-summaries` (`transfer.mimeMismatch (B9b)`). **B9a (media candidate list) deferred** (boundary vs B1).
  - **B10** (CLI did-you-mean): `parseArgs` suggests the nearest valid flag (normalized camelCase↔kebab, then edit-distance ≤2) and gives a targeted hint for common-but-absent flags (`--detail-level` → use `--limit/--offset/--max-chars`). General — helps every flag typo. Regression `tests/unit/cli/flags-render.test.ts` (`B10`); live: `--jsonPath`/`--max-char`/`--detail-level` all give the right guidance.
  - **B7 (cookie attributes) DEFERRED**: verified the attrs are discarded upstream — the browser-session cookieProvider returns a flattened Cookie header (`register/shared.ts` `browserCookiesToHeader`), which has no domain/path/secure/httpOnly/sameSite. Surfacing them needs a provider-contract change to pass structured cookie metadata to the analyzer (moderate, shared code), not a summary column add. Not in this clean batch.

- **B4 + B5 partial — artifact JSON scalar string windows + selector-timeout recovery hints (2026-06-06, second real-site blind batch).** B4 root cause was `compactJsonValue` hard-truncating scalar strings at 800 chars while `offset`/`limit` only paged arrays; `browser_artifact mode=json jsonPath=data.content offset=N limit=M` now returns a character window object `{type:"string", text, offset, limit, nextOffset, originalLength, truncated*}` for scalar string hits, without changing nested-object compaction or redaction. B5 root cause was wait supervisor timeout details exposing only generic lease/CDP state; `wait.selector` timeout supervisor payload now includes `selectorTimeout` with selector, state, main-document zero-match when known, and copyable recovery commands (`diagnose`, re-observe scan, html/frame follow-up). Deferred: DOM similar-selector/iframe probing at timeout. Rel1 is closed as M2 harness artifact. Regression: `check:artifact` and `check:fake-ws`.

- **B2 + B3 — CLI script-file ergonomics + artifact path discoverability (2026-06-06, 10-site blind batch).** B2 fixed narrowly at the CLI layer: `pi-browser execute --script-file <path>` reads cwd-relative/absolute JavaScript into the existing `script` parameter, rejects combination with `--script`, and deletes the synthetic param before schema validation; tool API/schema unchanged. B3 fixed without artifact schema drift: `browser_artifact` descriptions now tell agents common roots are `data`, `data.<key>` and that CLI `--pick` is repeated per path, not a JSON-array string; large/non-inline `browser_execute` results add `artifact_hints.preferredReads` for `data` and up to three top-level `data.<key>` fields. Regression: `tests/unit/cli/flags-render.test.ts` (script-file parse/help + artifact help wording) and `tests/unit/tools/execute-artifact-hints.test.ts` (execute hints). B1 intentionally deferred to RFC/minimal primitive; no semantic extractor added.

- **H3 — `browser_hook collect` events buried + wrong hint (2026-06-06, R7 hook, live-confirmed).** Collect fell through the `browser_hook` tool distiller (`domFlowDistiller`, which only handles getNodeListeners/Chain/SinkHints) to the generic summarizer → events collapsed to `{type,keyCount}` stubs + `nextActions` fell back to `operation.operationId`. Fix: new `summaries/hook.ts` `summarizeHookCollectData` (event-type counts via `topCounts`, a compact `seq/type/detail` sample table, and a data-rooted `data.events` artifact hint), routed from a new `hookToolDistiller` for `hook.collect`. Regression: `check-summaries` (`hook.collect (H3)` — eventTypes counts, sample detail, `data.events` hint). Live (linux.do): `eventTypes:[{console.log:2},…]`, `total:5`, sample rows `[seq,type,detail]`, `nextActions: read_saved_artifact mode=json jsonPath=data.events`.

- **E1 — `browser_evidence` artifact hint missing the `data.` prefix (2026-06-06, R7 evidence, code+live-confirmed; same class as N1).** The evidence summarizer emitted `jsonPath: "sources"` but the saved artifact (the bridge result `{id,tabId,data,…}`) holds it at `data.sources` → the hint resolved to `notFound`. Fix: `summaries/evidence.ts` → `data.sources`. Regression: `check-summaries` (evidence hint must be `data.sources`). Class audit (R7): bridge-wrapped tools (scan/network/evidence/hook) need data-rooted hints — scan ✅, network ✅ (N1), evidence ✅ (E1); web-security tools (crawl/recon/fuzz/domFlow) have FLAT artifacts so bare paths are correct (domFlow even auto-detects `wrapped`). Remaining class gap: hook has no distiller (H3).

- **N1 — network artifact-read hints missing the `data.` prefix (2026-06-06, R6 network, code-confirmed).** `summaries/network.ts` `entriesContainerPath` emitted `items`/`entries`/… (relative to the bridge result's `.data`), but `read_saved_artifact jsonPath=…` hints resolve against the SAVED ARTIFACT whose root is the whole bridge result `{id,tabId,data,…}` — so the hint pointed at a non-existent root `items[…]` (`notFound`). Scan was already correct (`data.content`). Fix: prefix the emitted paths with `data.` (`data.items[1].request`). Regression: `check-summaries` (`network.artifactHints (N1)` — first hint is `data.items`, every hint is data-rooted). Verified live (linux.do): hints now `data.items[…]`, resolve in the saved artifact on the first try, and carry H2's `mode=json`.

- **M1 — `browser_observe` baseline (treeDiff/diff/causal) unreachable by reference via the CLI (2026-06-06, R6 causal/treeDiff, code-confirmed).** `--baseline` only accepted an inline Array/Object (the full prior entity list/envelope, tens–hundreds of KB) → on the CLI it blew the OS argv limit (`ENAMETOOLONG`); a bare `snapshotId` string was rejected by the schema (contradicting the skill). The daemon already resolved a baseline OBJECT carrying `{snapshotId}` (from its observation store) or `{saved:{path}}` (read a file) — but that was undocumented and awkward (JSON-on-CLI / Windows quoting). Fix: two CLI-friendly SCALAR flags on `browser_observe` — `--baseline-snapshot-id <id>` and `--baseline-path <file>` — mapped onto the existing resolution; zero daemon change. Skill updated (pass baseline BY REFERENCE, don't inline). Regression: `tests/unit/cli/flags-render.test.ts` (`M1` — observe exposes both flags). Verified live (linux.do): `--baseline-snapshot-id <id>` after a navigation produced `diff`+`treeDiff` (74 appeared / 354 disappeared) with no error; `causal` present (correctly `unavailable` because the baseline predated `browser_network start`).

- **H1 + H2 — execute nextActions 误导 + artifact --json-path 无 mode 静默失效（2026-06-06，n=2 bilibili+linux.do）。** 两条一起修：**H1** — execute distill 在数据已内联（小返回值，F1 已修）时标记 `dataInline:true`；`artifactReadActions` 检测该标志并抑制关联路径 nextActions hint（operationId 等），因为 agent 已有数据无需读 artifact。**H2（两个 fix）** — ① `artifactReadActions` 的 preferredReads hint 改为 `read_saved_artifact mode=json jsonPath=X`（补全 `mode=json`，agent 可直接翻译成 CLI 调用）；② `readBrowserArtifact` 在 `--json-path` 或 `--pick` 存在但未指定 mode 时**自动升级到 json 模式**（之前静默回退 text，忽略 jsonPath，整个文件被返回）。回归：`check-summaries`（nextActions 格式 `includes("jsonPath=…")`，兼容新旧格式）；`check:artifact` 已通过（json 模式自动升级不破坏任何现有路径）。全量 check 绿，token-economy 0.06 未损。

## Resolved (prior)

- **F1 — `browser_execute` small return-value rendering (2026-06-06).** Root cause: `src/tools/summaries/generic.ts` `compactSample` is depth-2 + 5-sample + 240-char limited, so a value nested inside an object/array (R1 rows→cells, R3 `{count,rows}`) collapsed to `{type,count/keyCount}` even when the whole return was a few hundred bytes. Fix: `summarizePlainValue` now inlines the WHOLE return value VERBATIM (JSON-cloned) when it serializes within `INLINE_VALUE_CHARS` (4 KB); genuinely large payloads still collapse to a compact shape (samples stay small) with the full value in the saved artifact, and `fitSummaryBudget` (~8 KB) remains the hard guard. Redaction still applies (it runs after distill). Regression locked in `tests/contracts/tools/check-summaries.mjs` (`generic.inline.*`). Verified live through the rebuilt daemon on a real, China-reachable site (linux.do): a `{count,rows:[{title,replies}…]}` extraction now returns inline in ONE call — no second `browser_artifact` round-trip. NOT "fixed" by an execution helper (that would be WAI); this is pure output-ergonomics.

- **F4 — `wait --params` (and all action tools') per-action shape undocumented (2026-06-06).** Generality first (per `eval-fixes-true-defect-no-overfit`): root cause confirmed NOT wait-specific — `wait/network/hook/frame` all share `params: NativeCommandParamsSchema = Type.Object({}, {additionalProperties:true})` (loose), so `--params <json>` showed no shape for any of them; a wait-only fix would have overfit. Fix (source-of-truth driven, zero-drift): the `sync:protocol` generator now joins each action with its command's `required`/`requiredAny`/`notes` from `bridge/native_command_schema.json` into `nativeActionMetadata`, and `pi-browser <tool> --help` renders a per-action `--params` keys section (`cli/index.ts` `nativeActionParamsHelp`). Derived for EVERY action of every action tool — never hand-listed; `check:protocol` guards drift. Honest limit: the schema encodes required keys + notes (covers the F4 cases: wait.selector→selector, wait.navigate→url, hook.evaluate→expression, frame.evaluate→frameId,expression), NOT full optional/typed shapes (network actions have none → shown as empty, nothing fabricated); full param specs would be a larger source enrichment, deferred unless evals demand it. Regression: `tests/unit/cli/flags-render.test.ts` (`F4: nativeActionParamsHelp …`). Pure discoverability — `--params <json>` still works unchanged; not an execution/param-model change.

- **F2 — envelope verbosity / `[Circular]` (2026-06-06).** Measured the real envelope (deterministic local fixture, isolating structure from content) before guessing — 20 KB total, of which `summary.preview` was **6767 B (91% of summary)** plus two `[Circular]` placeholders. Three evidence-backed, general fixes: **(A)** the budget-overflow `preview` fallback re-dumped the whole compacted summary (up to ~8 KB) even though the page model is already lifted to envelope top-level (`entities`/`gist`/`outline`/`diff`/…) + saved artifact — capped it to an ≤800-char orientation snippet (`resultMiddleware.PREVIEW_FALLBACK_CHARS`). **(B)** `observeRunners` duplicated `diff`/`treeDiff`/`snapshotProjection` into BOTH summary top-level AND `focus` by shared reference (redaction collapsed the 2nd to `[Circular]`) — removed the redundant `focus` copies (the lift reads summary top-level; no consumer reads `focus.*`). **(C)** the scan summarizer shared the `headings`/`top_layer` arrays between summary top-level and `focus` — now distinct copies. Result (live, rebuilt daemon): **20 KB → 14 KB (−29%)**, `summary` 7435 → 1557 B, `preview` 6767 → 916 B, **`[Circular]` 2 → 0**; all signal preserved (entities/gist/etc. unchanged at envelope top-level). Regression: `check-summaries` (preview cap ≤800 + no `[Circular]` in the fallback) and `check-abml-scan-envelope` (headings/top_layer never `[Circular]`). **Left by design (honest):** entity objects are still intentionally shared across `primary_actions[].entity` / `primary_entities` / `envelope.entities` (contracted — `check-abml-scan-entities` requires the embedded handle), so redaction still renders the duplicate references as `[Circular]` in non-overflow envelopes; "fixing" that would either re-emit every entity 3× (balloon the envelope) or break the embedded-entity contract — per `eval-fixes-true-defect-no-overfit` that regression isn't worth it, and `[Circular]` there means "this entity is shown in full in `envelope.entities`", not data loss.

## reliability (work items)

*(none currently)*

## WAI (transparency only — NOT work items)

| # | finding | rationale |
|---|---------|-----------|
| W1 | "No `click`/`type`/`fill` verb; had to hand-write the form-fill via native value setter + `input`/`change` dispatch." | By deliberate project decision: the project provides the ABML page MODEL; the agent writes execution code. JS-first via `browser_execute`, trusted-event escape via `browser_command` CDP. The B2 action arm was tried and reverted. See `eval-friction-triage-perception-vs-execution`, `public-tool-surface-no-action-verbs`. (R2 agent reported "no friction finding how to act" — the model is fine; only the *return-value rendering* F1 hurt.) |
